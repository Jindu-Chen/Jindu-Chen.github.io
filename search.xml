<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>国民技术N32L406之GCC环境搭建</title>
    <url>/2023/11/27/%E5%9B%BD%E6%B0%91%E6%8A%80%E6%9C%AFGCC%E5%BC%80%E5%8F%91%E7%8E%AF%E5%A2%83%E6%90%AD%E5%BB%BA/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>arm</category>
        <category>环境搭建</category>
        <category>gcc</category>
      </categories>
      <tags>
        <tag>n32</tag>
      </tags>
  </entry>
  <entry>
    <title>常用编译/链接选项说明及其应用分析</title>
    <url>/2024/01/02/%E7%BC%96%E8%AF%91%E9%93%BE%E6%8E%A5%E9%80%89%E9%A1%B9%E6%84%8F%E4%B9%89%E8%A7%A3%E9%87%8A/</url>
    <content><![CDATA[<h2 id="GCC编译选项CFLAGS参数及说明"><a href="#GCC编译选项CFLAGS参数及说明" class="headerlink" title="GCC编译选项CFLAGS参数及说明"></a>GCC编译选项CFLAGS参数及说明</h2><ul>
<li><p><strong><code>-ffunction-sections</code></strong> : 告知编译器将每个函数放置在单独的小节中。使得可以在链接过程中只保留实际使用到的函数，未用到的函数将不会被链接，大大缩减生成可执行文件的大小</p>
</li>
<li><p><strong><code>-fdata-sections</code></strong> : 标准的编译选项，与-ffunction-sections类似，告知编译器将全局和静态变量放置在单独的小节中。</p>
</li>
<li><p><strong><code>--data-sections</code></strong> : 扩展的编译选项，只能在特定的编译器中使用，如GCC。</p>
</li>
<li><p><strong><code>-std=gnu99 -mabi=aapcs</code></strong> : 指定使用C语言的GNU 99标准，-mabi&#x3D;aapcs指定使用ARM架构的AAPCS（ARM Architecture Procedure Call Standard）ABI。</p>
</li>
</ul>
<h2 id="GCC链接选项LDFLAGS参数及说明"><a href="#GCC链接选项LDFLAGS参数及说明" class="headerlink" title="GCC链接选项LDFLAGS参数及说明"></a>GCC链接选项LDFLAGS参数及说明</h2><ul>
<li><p><strong><code>--specs=nosys.specs</code></strong> : 禁用标准库的系统调用，在嵌入式程序中应用。</p>
<blockquote>
<p>当编译时提示：<code>undefined reference to _sbrk&#39; 或 undefined reference to _write&#39;d</code> 等等时，可以加上此条选项</p>
</blockquote>
</li>
<li><p><strong><code>--specs=nano.specs</code></strong> : 不使用C语言标准库，改用微库，大大减小可执行文件大小。效果类似于应用newlib</p>
<blockquote>
<p>注意：使用微库后，不支持浮点数格式化标准输出</p>
</blockquote>
</li>
<li><p><strong><code>-Wl,--gc-sections</code></strong> : 告知链接器删除未使用的代码和数据段，搭配<code>-fdata-sections</code>和<code>-ffunction-sections</code>使用</p>
</li>
<li><p><strong><code>--specs=rdimon.specs</code></strong> : 不了解，在嵌入式MCU中加与不加都一样</p>
</li>
<li><p><strong><code>-Wl,–whole-archive</code></strong> ：告诉链接器将指定的静态库中的所有对象文件都链接到生成的目标文件中，即使这些对象文件中定义的符号在目标文件中没有被使用</p>
</li>
<li><p><strong><code>-Wl,–no-whole-archive</code></strong> ：选项告诉链接器只链接到静态库中那些定义了目标文件中使用的符号的对象文件</p>
</li>
<li><p><strong><code>-L 和 -l</code></strong> ：用于链接库文件，<code>-L</code>用于指定库文件的路径（如果库文件不在默认的搜索路径），<code>-l</code>用于指定要链接的库的名称（只需要提供库的名称，不需要lib前缀或.a、.so）</p>
<blockquote>
<p>例如，-L&#x2F;path&#x2F;to&#x2F;library<br>例如，对于libxxx.a或libxxx.so，只需要写-lxxx</p>
</blockquote>
</li>
</ul>
<h2 id="非典型应用示例"><a href="#非典型应用示例" class="headerlink" title="非典型应用示例"></a>非典型应用示例</h2><h3 id="控制链接器链接静态库文件"><a href="#控制链接器链接静态库文件" class="headerlink" title="控制链接器链接静态库文件"></a>控制链接器链接静态库文件</h3><ul>
<li>以下为工程中链接选项的片段：<ul>
<li>其将以下指示的静态库中所有对象文件都链接到目标文件中</li>
<li>而后将链接器恢复为只链接到静态库中定义了目标文件中使用的符号的对象文件<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># xxx</span><br><span class="line">-Wl,--whole-archive \</span><br><span class="line">libs/libbootstrap.a libs/libbroadcast.a \</span><br><span class="line">libs/libsamgr.a libs/libtoken_static.a libs/libnative_file.a \</span><br><span class="line">libs/libhilog_lite.a libs/libhiview_lite.a libs/libhievent_lite.a \</span><br><span class="line">-Wl,--no-whole-archive</span><br><span class="line"># xxx</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
</ul>
<h2 id="编译纠错修改以及一些注意点"><a href="#编译纠错修改以及一些注意点" class="headerlink" title="编译纠错修改以及一些注意点"></a>编译纠错修改以及一些注意点</h2><ul>
<li><p>当编译提示:<code>- undefined reference to _init</code>，类似时，检查是否添加了编译选项<code>--nostdlib</code>，不链接标准库，却又没指定库，导致的编译错误</p>
</li>
<li><p>提示<code>- exit.c:(.text.exit+0x2c): undefined reference to _exit</code>，考虑在.ld链接文件的.text段的末尾加上<code>_exit = .;</code></p>
</li>
<li><p>开启两级优化时, 需要注意C程序中汇编函数的调用可能会被优化掉, 可以考虑加上__attribute__ ((used))</p>
</li>
<li><p>需要注意编译等级的选择，可能会影响到程序的运行，代码编写时需要严谨</p>
</li>
<li><p>应用 <strong><code>-fdata-sections</code></strong> 可能会导致程序出错</p>
<ul>
<li>编程时尽量避免在不同的源文件中定义相同名称的全局变量或函数，避免链接错误</li>
<li>确保先正确初始化全局变量或函数，然后再引用</li>
</ul>
</li>
<li><p>编译选项和链接选项是分开处理的，编译选项只影响编译过程，链接选项只影响链接过程。而，在GCC中，可以在同一条命令中指定编译选项和链接选项，GCC会在编译和链接过程中分别应用这些选项。但不建议这样做</p>
</li>
</ul>
<h2 id="Makefile应用实例"><a href="#Makefile应用实例" class="headerlink" title="Makefile应用实例"></a>Makefile应用实例</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 指定交叉编译工具链的 编译器、连接器、库管理器、</span><br><span class="line">CROSS_COMPILE=arm-none-eabi-</span><br><span class="line">CC=$(CROSS_COMPILE)gcc</span><br><span class="line">LD=$(CROSS_COMPILE)ld</span><br><span class="line">AR=$(CROSS_COMPILE)ar</span><br><span class="line">AS=$(CROSS_COMPILE)as</span><br><span class="line">OC=$(CROSS_COMPILE)objcopy</span><br><span class="line">OD=$(CROSS_COMPILE)objdump</span><br><span class="line">SZ=$(CROSS_COMPILE)size</span><br><span class="line"></span><br><span class="line"># 指定目标硬件的架构和浮点运算单元   -mfpu=fpv4-sp-d16 -mfloat-abi=hard </span><br><span class="line"># O0:禁用优化，O1：基本优化，O2, O3, Os</span><br><span class="line"># g0:禁用调试信息, g1, g2, g3, g</span><br><span class="line">MCU = -mcpu=cortex-m4 -mthumb \</span><br><span class="line">	-ffunction-sections \</span><br><span class="line">	-fdata-sections \</span><br><span class="line">	-Os -ggdb</span><br><span class="line"></span><br><span class="line"># 定义编译C源文件的编译选项：禁用共享变量、函数和数据分段、优化级别、调试信息级别、开启所有警告、目标架构、预定义宏</span><br><span class="line">CFLAGS= -c -fno-common \</span><br><span class="line">	--specs=rdimon.specs \</span><br><span class="line">	-std=gnu99 -mabi=aapcs \</span><br><span class="line">	-Wall \</span><br><span class="line">	$(MCU) \</span><br><span class="line">	$(C_DEFS)</span><br><span class="line"></span><br><span class="line"># 定义链接器脚本、链接选项、目标文件格式转换选项、反汇编等等</span><br><span class="line">LDSCRIPT=n32l40x_flash.ld</span><br><span class="line">LDFLAGS = -Wl,--gc-sections --data-sections -mabi=aapcs $(MCU) -T$(LDSCRIPT) \</span><br><span class="line">	--specs=nosys.specs \</span><br><span class="line">	--specs=nano.specs \</span><br><span class="line">	-x assembler-with-cpp -Wa,-mimplicit-it=thumb</span><br><span class="line">OCFLAGS	= -Obinary</span><br><span class="line">ODFLAGS	= -S</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>C语言</category>
        <category>嵌入式</category>
        <category>Linux</category>
      </categories>
      <tags>
        <tag>gcc</tag>
        <tag>链接</tag>
        <tag>编译</tag>
      </tags>
  </entry>
  <entry>
    <title>通过VSCode + GDB 搭建WSL下的嵌入式mcu调试环境</title>
    <url>/2023/12/04/%E9%80%9A%E8%BF%87VSCode-GDB-%E6%90%AD%E5%BB%BAWSL%E4%B8%8B%E7%9A%84%E5%B5%8C%E5%85%A5%E5%BC%8Fmcu%E8%B0%83%E8%AF%95%E7%8E%AF%E5%A2%83/</url>
    <content><![CDATA[<h1 id="软件环境准备"><a href="#软件环境准备" class="headerlink" title="软件环境准备"></a>软件环境准备</h1><ul>
<li>Windows下J-Link软件安装</li>
<li>WSL</li>
<li>VSCode<ul>
<li>插件安装：C&#x2F;C++, WSL,</li>
</ul>
</li>
</ul>
<h1 id="操作步骤"><a href="#操作步骤" class="headerlink" title="操作步骤"></a>操作步骤</h1><ol>
<li>将ARM仿真器与目标嵌入式设备连线</li>
<li>在windows下J-Link的安装目录下，找到JLinkGDBServer.exe，点击打开，选择目标调试嵌入式设备</li>
</ol>
<p><img src="/../pictures/2023-12-04%20J-Link.png" alt="GDB Server config"><br><br><br><img src="/../pictures/2023-12-04%20j-link%20GDB.png" alt="J-Link GDB Server"></p>
<ol start="3">
<li>在Windows下使用VSCode登陆WSL，打开源码所在工程文件夹，作为VSCode工作区</li>
<li>点击”Run and Debug”，选择GDB（C）调试，并提示需要配置launch.json文件，配置完成即可开始调试</li>
</ol>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#launch.json文件</span><br><span class="line">&#123;</span><br><span class="line">    // Use IntelliSense to learn about possible attributes.</span><br><span class="line">    // Hover to view descriptions of existing attributes.</span><br><span class="line">    // For more information, visit: https://go.microsoft.com/fwlink/?linkid=830387</span><br><span class="line">    &quot;version&quot;: &quot;0.2.0&quot;,</span><br><span class="line">    &quot;configurations&quot;: [</span><br><span class="line">        &#123;</span><br><span class="line">            &quot;name&quot;: &quot;Debug with J-Link&quot;,</span><br><span class="line">            &quot;type&quot;: &quot;cppdbg&quot;,</span><br><span class="line">            &quot;request&quot;: &quot;launch&quot;,</span><br><span class="line">            &quot;program&quot;: &quot;$&#123;workspaceFolder&#125;/build/app.elf&quot;,  // .elf文件路径</span><br><span class="line">            &quot;miDebuggerPath&quot;: &quot;/home/chenjd/harmony/gcc-arm-none-eabi-9-2019-q4-major/bin/arm-none-eabi-gdb&quot;,   // WSL下的arm-none-eabi-gdb工具链路径</span><br><span class="line">            &quot;miDebuggerServerAddress&quot;: &quot;localhost:2331&quot;,        // J-Link端口，通常为2331</span><br><span class="line">            &quot;cwd&quot;: &quot;$&#123;workspaceFolder&#125;&quot;,</span><br><span class="line">            &quot;MIMode&quot;: &quot;gdb&quot;</span><br><span class="line">        &#125;,</span><br><span class="line">    ]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="补充说明"><a href="#补充说明" class="headerlink" title="补充说明"></a>补充说明</h1>]]></content>
      <categories>
        <category>嵌入式</category>
        <category>环境搭建</category>
        <category>WSL</category>
      </categories>
      <tags>
        <tag>WSL</tag>
        <tag>VSCode</tag>
        <tag>GDB</tag>
        <tag>J-Link</tag>
      </tags>
  </entry>
  <entry>
    <title>IAP/OTA流程概述</title>
    <url>/2023/11/28/IAP-OTA%E6%B5%81%E7%A8%8B%E6%A6%82%E8%BF%B0/</url>
    <content><![CDATA[<h3 id="DCD配置"><a href="#DCD配置" class="headerlink" title="DCD配置"></a>DCD配置</h3><h3 id="注意点"><a href="#注意点" class="headerlink" title="注意点"></a>注意点</h3><ul>
<li><p>内部Flash和片外Flash的擦除单位可能不一致，2K、4K字节不等</p>
</li>
<li><p>写Flash须考虑4字节对齐</p>
</li>
<li><p>有可能导致内核复位后触发HardFault异常的原因</p>
<ul>
<li>跳转前未关中断；中断控制器没复位清零；中断向量表偏移未设置正确；</li>
</ul>
</li>
<li><p>同一个bootloader和app，有时上电无法跳转，程序跑飞，此时断电后马上上电，又能跳转成功？</p>
</li>
</ul>
]]></content>
      <categories>
        <category>MCU</category>
      </categories>
  </entry>
  <entry>
    <title>Linux U-Boot移植概述-基于I-MX6ULL</title>
    <url>/2023/12/05/Linux-U-Boot%E7%A7%BB%E6%A4%8D%E6%A6%82%E8%BF%B0-%E5%9F%BA%E4%BA%8EI-MX6ULL/</url>
    <content><![CDATA[<h2 id="U-Boot简介"><a href="#U-Boot简介" class="headerlink" title="U-Boot简介"></a>U-Boot简介</h2><p>是一段用于启动Linux内核的bootloader裸机程序，其主要作用是：初始化必要的外设，将Linux内核从源存储拷贝到内存中，最终启动内核</p>
<h3 id="基于I-MX6ULL的U-Boot的一些开发概念"><a href="#基于I-MX6ULL的U-Boot的一些开发概念" class="headerlink" title="基于I.MX6ULL的U-Boot的一些开发概念"></a>基于I.MX6ULL的U-Boot的一些开发概念</h3><pre><code>1. I.MX通过置位BOOT引脚进行配置选择从SD卡、EMMC、NAND FLASH等等的启动方式

2. 生成的U-Boot.bin文件需要加上头部（IVT、DCD等数据），然后再烧录到启动存储中

3. I.MX上电后先跑芯片内置BootROM程序，把U-Boot加载到内存后，再跳转执行U-Boot

4. U-Boot提供命令行交互界面，其可以选择系统从网络启动、从EMMC启动等等

5. 通常情况下，是参考原厂开发板做硬件，而后在原厂提供的BSP包上做修改
</code></pre>
<h3 id="U-Boot在I-MX6ULL中的启动流程简述"><a href="#U-Boot在I-MX6ULL中的启动流程简述" class="headerlink" title="U-Boot在I.MX6ULL中的启动流程简述"></a>U-Boot在I.MX6ULL中的启动流程简述</h3><h2 id="补充"><a href="#补充" class="headerlink" title="补充"></a>补充</h2><h3 id="u-boot对内核的参数传递示例"><a href="#u-boot对内核的参数传递示例" class="headerlink" title="u-boot对内核的参数传递示例"></a>u-boot对内核的参数传递示例</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/* boot从网络启动 */</span><br><span class="line">setenv bootargs &#x27;console=ttymxc0,115200 root=/dev/nfs nfsroot=192.168.14.18:/home/chenjd/linux/nfs/rootfs,proto=tcp rw ip=192.168.14.99:192.168.14.18:192.168.14.1:255.255.255.0::eth0:off&#x27;</span><br><span class="line">setenv bootcmd &#x27;tftp 80800000 zImage; tftp 83000000 imx6ull-14x14-emmc-7-1024x600-c.dtb; bootz 80800000 - 83000000&#x27;</span><br><span class="line">saveenv</span><br><span class="line">boot</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/* boot从emmc启动 */</span><br><span class="line">setenv bootargs &#x27;console=ttymxc0,115200 root=/dev/mmcblk1p2 rootwait rw&#x27;</span><br><span class="line">setenv bootcmd &#x27;fatload mmc 1:1 80800000 zImage; fatload mmc 1:1 83000000 imx6ull-14x14-emmc-7-1024x600-c.dtb; bootz 80800000 - 83000000&#x27;</span><br><span class="line">saveenv</span><br><span class="line">boot</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Linux</category>
        <category>I.MX6ULL</category>
      </categories>
  </entry>
  <entry>
    <title>Linux内核移植概述--基于I.MX6ULL</title>
    <url>/2023/12/20/Linux%E5%86%85%E6%A0%B8%E7%A7%BB%E6%A4%8D%E6%A6%82%E8%BF%B0-%E5%9F%BA%E4%BA%8EI-MX6ULL/</url>
    <content><![CDATA[<h2 id="基本知识概要"><a href="#基本知识概要" class="headerlink" title="基本知识概要"></a>基本知识概要</h2><h2 id="Linux内核移植步骤简述"><a href="#Linux内核移植步骤简述" class="headerlink" title="Linux内核移植步骤简述"></a>Linux内核移植步骤简述</h2><ul>
<li>在Linux内核文件中，找到参考的板子配置文件——一般是半导体厂商基于其开发板的配置文件</li>
<li>以此份配置文件为模板，编译生成对应的zImage和.dtb文件</li>
<li>应用厂商开发板的zImage和.dtb文件，在自己的板子上启动Linux内核，查看能否正常启动</li>
<li>一般情况下，参考官方开发板设计的硬件，都能够正常启动</li>
<li>根据硬件与开发板的不同，修改相应的驱动；官方Linux内核一般会提供好各种相应的驱动，我们只需要修改设备树的一些配置信息即可</li>
</ul>
<h2 id="Linux内核移植详解步骤——I-MX6ULL"><a href="#Linux内核移植详解步骤——I-MX6ULL" class="headerlink" title="Linux内核移植详解步骤——I.MX6ULL"></a>Linux内核移植详解步骤——I.MX6ULL</h2><h3 id="在Linux内核中添加私有板子配置"><a href="#在Linux内核中添加私有板子配置" class="headerlink" title="在Linux内核中添加私有板子配置"></a>在Linux内核中添加私有板子配置</h3><ul>
<li>复制一份xxx_defconfig配置文件，并重命名，将其作为私有的默认配置文件<ul>
<li>通过在工程根目录下执行<code>make xxx_defconfig</code>配置Linux内核选项</li>
</ul>
</li>
<li>添加私有板子对应设备树文件<ul>
<li>创建私有.dts文件：<code>cd arch/arm/boot/dts</code> -&gt; <code>cp imx6ull-14x14-evk.dts imx6ull-xxx-emmc.dts</code></li>
<li>添加至编译：<code>vim arch/arm/boot/dts/Makefile</code>，在<code>dtb-$(CONFIG_SOC_IMX6ULL)</code>配置项中加入<code>imx6ull-xxx-emmc.dts</code>，使得编译设备树时，会从imx6ull-xxx-emmc.dts编译出对应的imx6ull-xxx-emmc.dtb文件</li>
</ul>
</li>
<li>编译得到zImage镜像文件和imx6ull-xxx-emmc.dtb设备树文件<ul>
<li>清理工程：<code>make ARCH=arm CROSS_COMPILE=arm-linux-gnueabihf- distclean</code></li>
<li>配置Linux内核：<code>make ARCH=arm CROSS_COMPILE=arm-linux-gnueabihf- xxx_defconfig</code></li>
<li>可选，打开图形化配置界面：<code>make ARCH=arm CROSS_COMPILE=arm-linux-gnueabihf- menuconfig</code></li>
<li>执行编译：<code>make ARCH=arm CROSS_COMPILE=arm-linux-gnueabihf- all -j16</code><ul>
<li>可选的，将上述指令添加到脚本中，进行自动化执行，避免重复的输入</li>
</ul>
</li>
<li><strong>编译完成后，在<code>arch/arm/boot</code>下生成zImage，在<code>arch/arm/boot/dts</code>下生成imx6ull-xxx-emmc.dtb文件</strong></li>
</ul>
</li>
</ul>
<h3 id="内核测试"><a href="#内核测试" class="headerlink" title="内核测试"></a>内核测试</h3><ul>
<li><p>启动板子，在u-boot命令模式中，加载所编译的zImage和.dtb文件</p>
<ul>
<li><code>tftp 80800000 zImage</code></li>
<li><code>tftp 83000000 imx6ull_xxx_emmc.dtb</code></li>
<li><code>bootz 80800000 - 83000000</code></li>
</ul>
</li>
<li><p>出现以下相似内容，表示Linux内核启动成功</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">...</span><br><span class="line">Booting Linux on physical CPU 0x0</span><br><span class="line">Linux version 4.1.15 ...</span><br><span class="line">...</span><br></pre></td></tr></table></figure></li>
</ul>
<h4 id="一些疑惑解答"><a href="#一些疑惑解答" class="headerlink" title="一些疑惑解答"></a>一些疑惑解答</h4><ul>
<li>在boot加载Linux内核时，所使用到的地址80800000和83000000是怎么来的？是固定地址？<ul>
<li>地址80800000是内核映像的起始地址，boot使用memcpy()将内核映像从存储设备加载到内存地址80800000中</li>
<li>地址83000000是内核栈的起始地址，其是内核存储临时数据的区域，boot将内核栈初始化为地址83000000<ul>
<li>设备树是ARM平台上的重要配置文件，其描述了系统资源的布局和属性；内核启动时需要访问设备树，以根据其参数初始化硬件设备</li>
<li>83000000是ARM平台上u-boot内核栈的常用起始地址，位于内存的低端，将设备树加载在此地址 -&gt; 确保内核在访问设备树时不会发生内存访问冲突</li>
</ul>
</li>
<li>在u-boot中，此二地址可以通过配置文件修改<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># configs/&lt;board&gt;defconfig</span><br><span class="line">CONFIG_SYS_LOAD_ADDR = 0x80800000</span><br><span class="line">CONFIG_SYS_INIT_SP_ADDR = 0x83000000</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
</ul>
<h2 id="Linux内核顶层Makefile详解"><a href="#Linux内核顶层Makefile详解" class="headerlink" title="Linux内核顶层Makefile详解"></a>Linux内核顶层Makefile详解</h2><h2 id="Linux内核启动流程"><a href="#Linux内核启动流程" class="headerlink" title="Linux内核启动流程"></a>Linux内核启动流程</h2>]]></content>
      <categories>
        <category>Linux</category>
        <category>I.MX6ULL</category>
      </categories>
  </entry>
  <entry>
    <title>Linux知识小贴士</title>
    <url>/2023/11/28/Linux%E7%9F%A5%E8%AF%86%E5%B0%8F%E8%B4%B4%E5%A3%AB/</url>
    <content><![CDATA[<h4 id="内网但非同一网段环境下开启NFS服务"><a href="#内网但非同一网段环境下开启NFS服务" class="headerlink" title="内网但非同一网段环境下开启NFS服务"></a>内网但非同一网段环境下开启NFS服务</h4><ul>
<li>在Linux主机和Windows主机之间开启NFS服务 之 Windows10通过NFS挂载Linux目录<ul>
<li>在Linux主机上配置NFS服务器<ul>
<li>输入命令<code>sudo apt-get install nfs-kernel-server rpcbind</code>安装NFS服务器和PRCBIND</li>
<li>创建共享目录：<code>sudo mkdir /home/jd_chen/nfs_share</code>，其中&#x2F;home&#x2F;jd_chen&#x2F;nfs_share为想要共享的目录<ul>
<li><code>chmod -R 777 /home/jd_chen/nfs_share</code>将所有权限递归应用于目标目录的所有文件和子目录</li>
</ul>
</li>
<li>配置NFS服务：<code>sudo vim /etc/exports</code><ul>
<li>在文件中添加<code>/home/jd_chen/nfs_share *(rw,sync,no_root_squash)</code></li>
<li>其中*表示允许所有的网络段访问，rw表示访问者具有可读写权限，sync表示将缓存写入设备中，表示同步缓存，no_root_squash表示访问者具有root权限</li>
</ul>
</li>
<li>启动NFS服务：<code>sudo service nfs-kernel-server start</code>或者<code>systemctl start nfs-kernel-server</code><ul>
<li><code>systemctl status nfs-kernel-server</code>验证NFS服务是否已启动</li>
<li><code>systemctl enable nfs-kernel-server</code>配置在系统启动时自动启动NFS服务</li>
<li><code>showmount -e</code>查看NFS共享目录</li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
</ul>
<br>
  - 在Windows下配置NFS客户湍服务
    - 打开`控制面板\程序\程序和功能`，点击`启用或关闭Windows功能`，找到`NFS服务`，勾选`NFS客户端`和`管理工具`
    - 重启电脑
<br>
  - 在Windows下挂载NFS目录 
    - 按下`Win+R`组合键打开运行对话框
    - 输入`mount 192.168.112.240:/home/jd_chen/nfs_share X:`
      - 其中192.168.112.240为Linux主机的ip地址，/home/jd_chen/nfs_share为需要共享的目录，X:为映射到Windows本地的盘符
  - 将命令mount改为umount即可卸载挂载，或者右键盘符->断开连接

<hr>
<ul>
<li>在Linux主机使用挂载Linux服务器的NFS共享目录<ul>
<li>客户端执行<code>yum install -y nfs-utils</code></li>
<li>执行<code>mount &lt;服务器IP&gt;:&lt;share_path&gt; &lt;挂载点&gt;</code><ul>
<li>如：<code>mount 192.168.1.1:/mnt/nfs /mnt/share</code><ul>
<li>表示将服务器 192.168.1.1 上的 &#x2F;mnt&#x2F;nfs 目录挂载到客户端 &#x2F;mnt&#x2F;share 目录</li>
</ul>
</li>
<li>如：<code>mount -t nfs -o nolock,nfsvers=3 192.168.1.1:/mnt/nfs /mnt/share</code><ul>
<li>其中-t nfs指定文件系统类型为NFS，-o nolock表示禁用文件锁定功能，nfsvers&#x3D;3指定使用NFS版本3</li>
</ul>
</li>
</ul>
</li>
<li>执行<code>df -h</code>验证NFS共享是否可用</li>
</ul>
</li>
</ul>
<hr>
<ul>
<li>嵌入式Linux开发中，配置NFS挂载根文件系统</li>
</ul>
<hr>
<h4 id="远程端口流量通过SSH映射到本地"><a href="#远程端口流量通过SSH映射到本地" class="headerlink" title="远程端口流量通过SSH映射到本地"></a>远程端口流量通过SSH映射到本地</h4><p>ssh -L 4000:localhost:4000 <a href="mailto:&#x6a;&#x64;&#x5f;&#99;&#104;&#101;&#110;&#64;&#49;&#x39;&#x32;&#x2e;&#x31;&#x36;&#56;&#x2e;&#x31;&#x31;&#x32;&#46;&#x32;&#x34;&#x30;">&#x6a;&#x64;&#x5f;&#99;&#104;&#101;&#110;&#64;&#49;&#x39;&#x32;&#x2e;&#x31;&#x36;&#56;&#x2e;&#x31;&#x31;&#x32;&#46;&#x32;&#x34;&#x30;</a></p>
<h4 id="Linux给根目录扩容"><a href="#Linux给根目录扩容" class="headerlink" title="Linux给根目录扩容"></a>Linux给根目录扩容</h4>]]></content>
      <categories>
        <category>Linux</category>
      </categories>
  </entry>
  <entry>
    <title>LiteOS中断管理之SysTick</title>
    <url>/2023/12/10/LiteOS%E4%B8%AD%E6%96%AD%E7%AE%A1%E7%90%86%E4%B9%8BSysTick/</url>
    <content><![CDATA[<p>通常用户对LiteOS的初始化调用如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">main():</span><br><span class="line">    LOS_KernelInit();       // 内核初始化</span><br><span class="line">    LiteOS_Task_sample();   // 用户业务代码，创建初始任务等</span><br><span class="line">    LOS_Start();            // 开启调度</span><br></pre></td></tr></table></figure>

<h1 id="LiteOS的中断函数映射管理"><a href="#LiteOS的中断函数映射管理" class="headerlink" title="LiteOS的中断函数映射管理"></a>LiteOS的中断函数映射管理</h1><h2 id="LiteOS中断映射表初始化"><a href="#LiteOS中断映射表初始化" class="headerlink" title="LiteOS中断映射表初始化"></a>LiteOS中断映射表初始化</h2><ul>
<li>函数调用顺序：<code>LOS_KernelInit() -&gt; ArchInit() -&gt; HalHwiInit()</code></li>
</ul>
<p>如下代码<code>SCB-&gt;VTOR = (UINT32)(UINTPTR)hwiForm;</code>将中断向量映射基地址设为自定义变量地址</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># arch/arm/cortex-m4/iar/los_interrupt.c &gt; HalHwiInit()</span><br><span class="line"></span><br><span class="line">#if (LOSCFG_USE_SYSTEM_DEFINED_INTERRUPT == 1)</span><br><span class="line">    UINT32 index;</span><br><span class="line">    HWI_PROC_FUNC *hwiForm = (HWI_PROC_FUNC *)ArchGetHwiFrom();</span><br><span class="line">    hwiForm[0] = 0;             /* [0] Top of Stack */</span><br><span class="line">    hwiForm[1] = (HWI_PROC_FUNC)Reset_Handler; /* [1] reset */</span><br><span class="line">    for (index = 2; index &lt; OS_VECTOR_CNT; index++) &#123; /* 2: The starting position of the interrupt */</span><br><span class="line">        hwiForm[index] = (HWI_PROC_FUNC)HalHwiDefaultHandler;</span><br><span class="line">    &#125;</span><br><span class="line">    /* Exception handler register */</span><br><span class="line">    hwiForm[NonMaskableInt_IRQn + OS_SYS_VECTOR_CNT]   = (HWI_PROC_FUNC)HalExcNMI;</span><br><span class="line">    hwiForm[HARDFAULT_IRQN + OS_SYS_VECTOR_CNT]        = (HWI_PROC_FUNC)HalExcHardFault;</span><br><span class="line">    hwiForm[MemoryManagement_IRQn + OS_SYS_VECTOR_CNT] = (HWI_PROC_FUNC)HalExcMemFault;</span><br><span class="line">    hwiForm[BusFault_IRQn + OS_SYS_VECTOR_CNT]         = (HWI_PROC_FUNC)HalExcBusFault;</span><br><span class="line">    hwiForm[UsageFault_IRQn + OS_SYS_VECTOR_CNT]       = (HWI_PROC_FUNC)HalExcUsageFault;</span><br><span class="line">    hwiForm[SVCall_IRQn + OS_SYS_VECTOR_CNT]           = (HWI_PROC_FUNC)HalExcSvcCall;</span><br><span class="line">    hwiForm[PendSV_IRQn + OS_SYS_VECTOR_CNT]           = (HWI_PROC_FUNC)HalPendSV;</span><br><span class="line">    hwiForm[SysTick_IRQn + OS_SYS_VECTOR_CNT]          = (HWI_PROC_FUNC)SysTick_Handler;</span><br><span class="line"></span><br><span class="line">    /* Interrupt vector table location */</span><br><span class="line">    SCB-&gt;VTOR = (UINT32)(UINTPTR)hwiForm;</span><br><span class="line">#endif</span><br></pre></td></tr></table></figure>

<h1 id="配置SysTick中断服务函数"><a href="#配置SysTick中断服务函数" class="headerlink" title="配置SysTick中断服务函数"></a>配置SysTick中断服务函数</h1><ul>
<li>函数调用顺序：<code>LOS_KernelInit() -&gt; OsTickTimerInit() -&gt; OsTickTimerInit() -&gt; g_sysTickTimer-&gt;init(OsTickHandler) -&gt; SysTickStart(OsTickHandler)</code></li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># kernel/src/los_tick.c</span><br><span class="line">LITE_OS_SEC_TEXT VOID OsTickHandler(VOID)</span><br><span class="line">&#123;</span><br><span class="line">#if (LOSCFG_BASE_CORE_TICK_WTIMER == 0)</span><br><span class="line">    OsUpdateSysTimeBase();</span><br><span class="line">#endif</span><br><span class="line"></span><br><span class="line">    LOS_SchedTickHandler();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># arch/arm/cortex-m4/gcc/los_timer.c</span><br><span class="line">STATIC UINT32 SysTickStart(HWI_PROC_FUNC handler)</span><br><span class="line">&#123;</span><br><span class="line">    UINT32 ret;</span><br><span class="line">    ArchTickTimer *tick = &amp;g_archTickTimer;</span><br><span class="line"></span><br><span class="line">    tick-&gt;freq = OS_SYS_CLOCK;</span><br><span class="line"></span><br><span class="line">#if (LOSCFG_USE_SYSTEM_DEFINED_INTERRUPT == 1)</span><br><span class="line">#if (LOSCFG_PLATFORM_HWI_WITH_ARG == 1)</span><br><span class="line">    OsSetVector(tick-&gt;irqNum, handler, NULL);</span><br><span class="line">#else</span><br><span class="line">    OsSetVector(tick-&gt;irqNum, handler);     // 设置SysTick的中断向量</span><br><span class="line">#endif</span><br><span class="line">#endif</span><br><span class="line"></span><br><span class="line">    ret = SysTick_Config(LOSCFG_BASE_CORE_TICK_RESPONSE_MAX);   // 在此处配置的SysTick中断无效，后续开启调度时会reload</span><br><span class="line">    if (ret == 1) &#123;</span><br><span class="line">        return LOS_ERRNO_TICK_PER_SEC_TOO_SMALL;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    return LOS_OK;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># arch/arm/common/los_common_interrupt.c</span><br><span class="line">VOID OsSetVector(UINT32 num, HWI_PROC_FUNC vector)</span><br><span class="line">&#123;</span><br><span class="line">    if ((num + OS_SYS_VECTOR_CNT) &lt; OS_VECTOR_CNT) &#123;</span><br><span class="line">        g_hwiForm[num + OS_SYS_VECTOR_CNT] = HalInterrupt;</span><br><span class="line">        g_hwiHandlerForm[num + OS_SYS_VECTOR_CNT] = vector;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


<h1 id="初始化SysTick时基"><a href="#初始化SysTick时基" class="headerlink" title="初始化SysTick时基"></a>初始化SysTick时基</h1><ul>
<li>函数调用顺序：<code>LOS_Start() -&gt; ArchStartSchedule() -&gt; OsSchedStart() -&gt; OsSchedSetNextExpireTime() -&gt; OsTickTimerReload() -&gt; SysTickReload()</code></li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># arch/arm/cortex-m4/gcc/los_timer.c</span><br><span class="line"># 调用此函数，传入的值实际上是</span><br><span class="line">STATIC UINT64 SysTickReload(UINT64 nextResponseTime)</span><br><span class="line">&#123;</span><br><span class="line">    if (nextResponseTime &gt; g_archTickTimer.periodMax) &#123;</span><br><span class="line">        nextResponseTime = g_archTickTimer.periodMax;</span><br><span class="line">    &#125;</span><br><span class="line">    SysTick-&gt;CTRL &amp;= ~SysTick_CTRL_ENABLE_Msk;</span><br><span class="line">    SysTick-&gt;LOAD = (UINT32)(nextResponseTime - 1UL); /* set reload register */</span><br><span class="line">    SysTick-&gt;VAL = 0UL; /* Load the SysTick Counter Value */</span><br><span class="line">    NVIC_ClearPendingIRQ(SysTick_IRQn);</span><br><span class="line">    SysTick-&gt;CTRL |= SysTick_CTRL_ENABLE_Msk;</span><br><span class="line">    return nextResponseTime;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="初始化调用逻辑总结"><a href="#初始化调用逻辑总结" class="headerlink" title="初始化调用逻辑总结"></a>初始化调用逻辑总结</h1><ol>
<li>在<code>arch/arm/cortex-m4/iar/los_interrupt.c &gt; HalHwiInit()</code>中执行中断向量表初始化<blockquote>
<p>但在此处初始化的SysTick_Handler向量是弱函数，并且其在后面会被替换</p>
</blockquote>
</li>
<li>在<code>LOS_KernelInit() -&gt; OsTickTimerInit() -&gt; OsTickTimerInit()</code>重设SysTick中断服务函数</li>
<li>在<code>LOS_Start() -&gt; ArchStartSchedule() -&gt; OsSchedStart()</code>中调用 重置SysTick中断时基为<code>系统时钟/调度间隔Tick值 （如：时钟源 / 1000 = 1ms时基）</code></li>
<li>最后开启系统调度</li>
</ol>
<h1 id="补充说明"><a href="#补充说明" class="headerlink" title="补充说明"></a>补充说明</h1><ul>
<li>LiteOS的内核初始化和系统启动都对Systick进行了设置，内核初始化时设置了Systick的重装载值为24位最大值，即0xFFFFFF</li>
<li>系统启动时，重置Systick累计的节拍，并设置重装载值为 <code>SystemCoreClock / 1000</code>，其中SystemCoreClock为系统时钟源频率。</li>
</ul>
]]></content>
      <categories>
        <category>MCU</category>
        <category>LiteOS</category>
      </categories>
  </entry>
  <entry>
    <title>MCU开发知识点记录与总结</title>
    <url>/2023/11/28/MCU%E5%BC%80%E5%8F%91%E5%B0%8F%E8%B4%B4%E5%A3%AB/</url>
    <content><![CDATA[<h2 id="内核相关部分"><a href="#内核相关部分" class="headerlink" title="内核相关部分"></a>内核相关部分</h2><h3 id="全局开关中断操作"><a href="#全局开关中断操作" class="headerlink" title="全局开关中断操作"></a>全局开关中断操作</h3><pre><code><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">__disable_irq();    // 将 PRIMASK 寄存器的值设置为 1，禁止所有中断</span><br><span class="line">__enable_irq();     // 将 PRIMASK 寄存器的值设置为 0，开启所有中断</span><br><span class="line">__set_PRIMASK(1);   // PRIMASK 值为 1 ，所有可屏蔽中断的优先级为 1，表示所有可屏蔽中断均不能被中断处理程序处理，只能由 NMI 处理程序处理</span><br><span class="line">__set_PRIMASK(0);</span><br><span class="line"></span><br><span class="line">// 在 Cortex-M 架构中，FAULTMASK 寄存器控制所有异常的优先级</span><br><span class="line">__set_FAULTMASK(1);  // FAULTMASK 寄存器的值为 1 时，所有异常的优先级为 1，表示所有异常均不能被中断处理程序处理，只能由 NMI 处理程序处理</span><br><span class="line">__set_FAULTMASK(0);</span><br><span class="line"></span><br><span class="line">CPSID F        //关异常，FAULTMASK=1</span><br><span class="line">CPSIE F        //开异常，FAULTMASK=0</span><br></pre></td></tr></table></figure>
</code></pre>
<h3 id="MCU获取CPU使用率原理"><a href="#MCU获取CPU使用率原理" class="headerlink" title="MCU获取CPU使用率原理"></a>MCU获取CPU使用率原理</h3><ul>
<li>理论来说，裸机程序不能获取CPU使用率</li>
<li>在RTOS下，可以通过计时器来测量CPU在空闲任务的运行时间，计算其占整体运行时间的比率即可得到CPU使用率<ul>
<li>CPU使用率 &#x3D; (CPU总运行时间 - CPU空闲时间) &#x2F; CPU总运行时间</li>
<li>通过使用计时器，可以计算上电运行以后的 CPU总使用率，也可以计算 一个时间周期内 CPU的使用率</li>
</ul>
</li>
</ul>
<h2 id="外设相关部分"><a href="#外设相关部分" class="headerlink" title="外设相关部分"></a>外设相关部分</h2><h3 id="GPIO的输出速度"><a href="#GPIO的输出速度" class="headerlink" title="GPIO的输出速度"></a>GPIO的输出速度</h3><ul>
<li>根据外部设备要求进行配置<ul>
<li>常规的控制LED灯、按键等，采用2MHz即可</li>
<li>GPIO模拟SPI、I2C等总线，可以配置为10MHz</li>
<li>控制DDR、USB等超高速接口，配置50MHZ</li>
</ul>
</li>
<li>GPIO的输出速度越高，其驱动电路的功耗就越高</li>
</ul>
<h3 id="内部与外部晶振"><a href="#内部与外部晶振" class="headerlink" title="内部与外部晶振"></a>内部与外部晶振</h3><ul>
<li>内部晶振精度较外部晶振低</li>
<li>一般情况下，使用内部晶振足以满足常用需求，包括spi、iic、uart的外设通信</li>
<li>如果要求时钟精度高，或应用于精密仪器等场景，或用到高速外设如usb、sdio的高精度时钟要求场景，考虑用外部晶振<ul>
<li>SDIO 接口要求时钟信号的精度为 2% 或以下<ul>
<li>存储卡、扩展卡、无线模块</li>
<li>高速图像传感器、高速存储卡</li>
</ul>
</li>
<li>USB 接口要求时钟信号的精度为 5% 或以下<ul>
<li>鼠标、键盘、打印机、网络摄像头、耳机</li>
<li>高速摄像头、高速打印机、高品质音频设备</li>
</ul>
</li>
</ul>
</li>
</ul>
<h3 id="外设接口的通信频率"><a href="#外设接口的通信频率" class="headerlink" title="外设接口的通信频率"></a>外设接口的通信频率</h3><h4 id="MCU常用外设的通信速率范围"><a href="#MCU常用外设的通信速率范围" class="headerlink" title="MCU常用外设的通信速率范围"></a>MCU常用外设的通信速率范围</h4><table>
<thead>
<tr>
<th>接口类型</th>
<th>通信频率</th>
<th>精度</th>
</tr>
</thead>
<tbody><tr>
<td>SDIO</td>
<td>25 MHz~50 MHz</td>
<td>2% 以下</td>
</tr>
<tr>
<td>USB</td>
<td>12 MHz~480 MHz</td>
<td>5% 以下</td>
</tr>
<tr>
<td>I2C</td>
<td>100 kHz~400 kHz</td>
<td>1% 以下</td>
</tr>
<tr>
<td>SPI</td>
<td>1 MHz~20 MHz</td>
<td>1% 以下</td>
</tr>
<tr>
<td>UART</td>
<td>1200 bps~921600 bps</td>
<td>1% 以下</td>
</tr>
</tbody></table>
<h3 id="UART外设"><a href="#UART外设" class="headerlink" title="UART外设"></a>UART外设</h3><ul>
<li>UART(通用异步收发传输器)是一种设备、通信的基础，规定了数据的格式和帧结构；RS232和RS485是基于UART的电气接口标准，其规定了电气特性和物理特性</li>
<li>TTL电平的通信速率：MCU的UART外设<strong>理论最高速率取决于其外设的时钟源</strong><ul>
<li>可通过芯片手册查看时钟树，获取外设的时钟频率</li>
</ul>
</li>
<li>RS232为点对点通信，其标准传输速率须在20Kbps下，抗干扰能力差，传输最大距离不超过15m</li>
<li>RS485的理论最高传输速率为10Mbps，可一对多通信，理论最大传输距离1500米(9600bps)</li>
<li>测试较高波特率场景要求时，可以<strong>通过示波器抓取uart外设波形，计算其误差是否满足</strong>，或者通过逻辑分析仪在收发两端分别测试验证</li>
</ul>
<p>以下转述参考文章: <a href="https://zhuanlan.zhihu.com/p/534135274">UART波特率对时钟精度的要求有多高？</a></p>
<blockquote>
<p>分频误差：波特率是根据系统时钟分频产生的，而系统时钟和波特率可能不是整数倍的关系，所以分频时也会产生误差。但是，当系统时钟较高，或者波特率较低时，这个误差影响很小。</p>
</blockquote>
<blockquote>
<p>时钟误差：波特率的主要误差还是取决于系统时钟的误差。串口通讯对时钟误差的要求有多高呢？<br>每一个字节都有一个起始位做同步，所以误差只会在一个字节内累计。在最后一个位采样时，误差最大，允许极限误差为0.5位。<br>按照每个字节10位计算，最大允许误差为±0.5&#x2F;10&#x3D;±0.05，即±5%。<br>考虑到串口通讯涉及收发两端，两端都可能存在误差，所以，每端的误差最好控制在±2.5%以内。当数据位、校验位等较多时，要求的时钟误差就更高一些。<br>考虑到上面所说的分频误差，一般来说，当时钟误差小于±2%时，串口通信是比较可靠的。</p>
</blockquote>
<h4 id="UART通信协议"><a href="#UART通信协议" class="headerlink" title="UART通信协议"></a>UART通信协议</h4><ul>
<li>空闲状态</li>
<li>起始位</li>
<li>数据位</li>
<li>奇偶检验位</li>
<li>停止位</li>
</ul>
<h4 id="空闲位和空闲中断"><a href="#空闲位和空闲中断" class="headerlink" title="空闲位和空闲中断"></a>空闲位和空闲中断</h4><ul>
<li><p>空闲位：在UART通信中两个字节或者两个帧之间的 <strong>一位高电平</strong>，用于表示线路的空闲状态；当接收端检测到线路从”1”跳转到”0”(起始位)时，可以知道下一字节开始传输</p>
<ul>
<li>当一次发送多个字节时，每字节间会有个空闲位(可配置？看实际设计)，用于区分字节<br></li>
</ul>
</li>
<li><p>空闲中断：在接收到数据之后，如果<strong>在一个字节的时间内</strong>没有再接收到数据，就会产生空闲中断</p>
<ul>
<li>其一般用于接收不定长数据，用于<strong>标识一个数据包&#x2F;一帧数据</strong></li>
</ul>
</li>
</ul>
]]></content>
      <categories>
        <category>MCU</category>
      </categories>
  </entry>
  <entry>
    <title>RT-Thread启动流程简述</title>
    <url>/2023/11/28/RT-Thread%E5%90%AF%E5%8A%A8%E6%B5%81%E7%A8%8B%E7%AE%80%E8%BF%B0/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>MCU</category>
        <category>RTOS</category>
      </categories>
  </entry>
  <entry>
    <title>STM32启动流程概述</title>
    <url>/2023/11/28/STM32%E5%90%AF%E5%8A%A8%E6%B5%81%E7%A8%8B%E6%A6%82%E8%BF%B0/</url>
    <content><![CDATA[<h1 id="Keil环境下的启动文件分析"><a href="#Keil环境下的启动文件分析" class="headerlink" title="Keil环境下的启动文件分析"></a>Keil环境下的启动文件分析</h1><h2 id="初始化栈顶指针"><a href="#初始化栈顶指针" class="headerlink" title="初始化栈顶指针"></a>初始化栈顶指针</h2><ul>
<li>内核从0x0800 0000读取栈顶地址，并将该地址存入MSP中。<blockquote>
<p>栈顶地址的值为0x2000 xxxx，工程所生成bin文件的前四个字节即为栈顶地址。<br>从0x2000 0000到0x2000 xxxx即为程序所运行的范围，该段内存分布为：RW段、ZI段：其中RW段为可读写的非0数据段，ZI段包括了0数据段、堆区、栈区。</p>
</blockquote>
</li>
<li>从0x08000004取出复位中断函数<code>Reset_Handler</code>地址，装载至PC指针，跳转执行</li>
</ul>
<h2 id="复位中断"><a href="#复位中断" class="headerlink" title="复位中断"></a>复位中断</h2><p><strong>Keil环境代码如下：</strong></p>
<ul>
<li>先进行系统时钟初始化<blockquote>
<p>SystemInit()函数定义在system_xxx.c中，主要为初始化系统时钟RCC、重定位中断向量表</p>
</blockquote>
</li>
<li>跳转至_main()</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">; Reset handler</span><br><span class="line">Reset_Handler   PROC</span><br><span class="line">                EXPORT  Reset_Handler             [WEAK]  </span><br><span class="line">                IMPORT  __main</span><br><span class="line">                IMPORT  SystemInit</span><br><span class="line">                LDR     R0, =SystemInit   </span><br><span class="line">                BLX     R0               </span><br><span class="line">                LDR     R0, =__main</span><br><span class="line">                BX      R0    </span><br><span class="line">                ENDP</span><br></pre></td></tr></table></figure>

<h2 id="main-函数"><a href="#main-函数" class="headerlink" title="_main()函数"></a>_main()函数</h2><ul>
<li><p>__scatterload<br>  将初始化的可读写数据段拷贝从Flash拷贝到SRAM，<br>  初始化清零未初始化数据段</p>
</li>
<li><p>__rt_entry<br>  负责初始化堆栈，完成库函数的初始化，<br>  最后跳转至main()函数</p>
</li>
</ul>
<h1 id="GCC编译环境下的启动文件分析"><a href="#GCC编译环境下的启动文件分析" class="headerlink" title="GCC编译环境下的启动文件分析"></a>GCC编译环境下的启动文件分析</h1><p><strong>程序跳转至复位中断后，执行以下操作：</strong></p>
<ul>
<li>将初始化的可读写数据段拷贝从Flash拷贝到SRAM</li>
<li>初始化清零未初始化数据段</li>
<li>调用系统时钟初始化函数（SystemInit）</li>
<li>调用静态构造函数（__libc_init_array） ——参考自CHATGPT<ul>
<li>调用所有静态构造函数-&gt;调用.preinit_array段中的所有函数指针-&gt;调用.init段中的_init函数-&gt;调用.init_array段中的所有函数指针-&gt;确保在程序执行主函数 main 之前，所有需要初始化的内容都已经完成。</li>
<li>其在链接脚本中有体现：  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">.preinit_array     :</span><br><span class="line">&#123;</span><br><span class="line">  PROVIDE_HIDDEN (__preinit_array_start = .);</span><br><span class="line">  KEEP (*(.preinit_array*))</span><br><span class="line">  PROVIDE_HIDDEN (__preinit_array_end = .);</span><br><span class="line">&#125; &gt;FLASH</span><br><span class="line">.init_array :</span><br><span class="line">&#123;</span><br><span class="line">  PROVIDE_HIDDEN (__init_array_start = .);</span><br><span class="line">  KEEP (*(SORT(.init_array.*)))</span><br><span class="line">  KEEP (*(.init_array*))</span><br><span class="line">  PROVIDE_HIDDEN (__init_array_end = .);</span><br><span class="line">&#125; &gt;FLASH</span><br></pre></td></tr></table></figure></li>
<li>链接脚本中，可将用户函数指定链接到相应的init段，从而实现在main前的函数自动初始化？</li>
</ul>
</li>
<li>进入用户函数(main)<br></li>
</ul>
<p><strong>复位中断代码如下所示，注意：其中所用变量_sidata、_edata等等须 搭配链接脚本内容 共同参阅</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Reset_Handler:  </span><br><span class="line">  movs  r1, #0              /* 将寄存器r1清零，作为数据段复制的偏移量 */</span><br><span class="line">  b  LoopCopyDataInit       /* 无条件跳转到LoopCopyDataInit标签处 */</span><br><span class="line"></span><br><span class="line">CopyDataInit:</span><br><span class="line">  ldr  r3, =_sidata         /* 将_sidata的地址加载到寄存器r3，表示初始化数据段的起始地址 */</span><br><span class="line">  ldr  r3, [r3, r1]         /* 从初始化数据段中取出一个32位数据，存储在寄存器r3中。 */</span><br><span class="line">  str  r3, [r0, r1]         /* 将寄存器r3中的数据存储到数据段当前位置。 */</span><br><span class="line">  adds  r1, r1, #4         /* 增加偏移量，移动到下一个32位数据的位置。 */</span><br><span class="line">    </span><br><span class="line">LoopCopyDataInit:</span><br><span class="line">  ldr  r0, =_sdata         /* 将_sdata的地址加载到寄存器r0，表示数据段的起始地址。*/</span><br><span class="line">  ldr  r3, =_edata         /* 将_edata的地址加载到寄存器r3，表示数据段的结束地址。 */</span><br><span class="line">  adds  r2, r0, r1         /* 计算数据段当前位置的地址，存储在寄存器r2中。*/</span><br><span class="line">  cmp  r2, r3         /* 比较当前位置与数据段结束地址的大小关系 */</span><br><span class="line">  bcc  CopyDataInit         /* 如果当前位置小于结束地址，则跳转到CopyDataInit标签处 */</span><br><span class="line">  </span><br><span class="line">  /*清零未初始化数据段（bss segment）*/</span><br><span class="line">  ldr  r2, =_sbss         /* 将_sbss的地址加载到寄存器r2，表示未初始化数据段的起始地址 */</span><br><span class="line">  b  LoopFillZerobss         /* 无条件跳转到LoopFillZerobss标签处 */</span><br><span class="line">FillZerobss:</span><br><span class="line">  movs  r3, #0      /* 将寄存器r3清零，作为未初始化数据段的填充值 */</span><br><span class="line">  str  r3, [r2], #4      /* 将寄存器r3中的数据存储到当前位置，并将当前位置向后移动4个字节 */</span><br><span class="line">    </span><br><span class="line">LoopFillZerobss:         </span><br><span class="line">  ldr  r3, = _ebss        /* 将_ebss的地址加载到寄存器r3，表示未初始化数据段的结束地址 */</span><br><span class="line">  cmp  r2, r3         /* 比较当前位置与结束地址的大小关系 */</span><br><span class="line">  bcc  FillZerobss         /* 如果当前位置小于结束地址，则跳转到FillZerobss标签处 */</span><br><span class="line"></span><br><span class="line">  bl  SystemInit            /* 调用系统时钟初始化函数 */</span><br><span class="line">  bl __libc_init_array         /* 调用静态构造函数 */</span><br><span class="line">  bl  main            /* 应用程序的入口函数 */</span><br><span class="line">  bx  lr            /* 通过bx lr指令返回到调用者（通常是启动代码的入口处） */</span><br><span class="line">.size  Reset_Handler, .-Reset_Handler</span><br></pre></td></tr></table></figure>


]]></content>
      <categories>
        <category>嵌入式</category>
        <category>MCU</category>
      </categories>
      <tags>
        <tag>STM32</tag>
        <tag>MCU</tag>
      </tags>
  </entry>
  <entry>
    <title>WSL小贴士</title>
    <url>/2023/11/28/WSL%E5%B0%8F%E8%B4%B4%E5%A3%AB/</url>
    <content><![CDATA[<h4 id="在windows下访问WSL的目录"><a href="#在windows下访问WSL的目录" class="headerlink" title="在windows下访问WSL的目录"></a>在windows下访问WSL的目录</h4><ul>
<li>在wwindows的资源管理器输入<code>\\wsl$</code>即可</li>
<li>直接访问WSL的根路径<ul>
<li><code>C:\Users\xxx\AppData\Local\Packages\CanonicalGroupLimited.Ubuntu18.04onWindows_79rhkp1fndgsc\LocalState\rootfs</code></li>
<li>其中<code>xxx</code>为个人用户名</li>
<li>如果过程中没有找到对应的文件夹，请勾选<code>资源管理器-&gt;查看-&gt;隐藏的项目</code><br></li>
</ul>
</li>
</ul>
<h4 id="重启WSL2"><a href="#重启WSL2" class="headerlink" title="重启WSL2"></a>重启WSL2</h4><ul>
<li>打开PowerShell<ul>
<li>输入命令<code>Get-Service LxssManager | Restart-Service</code>，重启LxssManager服务</li>
<li>停止LxssManager服务&#x2F;关机：<code>net stop LxssManager</code></li>
<li>启动LxssManager服务&#x2F;开机：<code>net start LxssManager</code></li>
</ul>
</li>
</ul>
<h4 id="WSL下通过命令执行Windows下的bat脚本"><a href="#WSL下通过命令执行Windows下的bat脚本" class="headerlink" title="WSL下通过命令执行Windows下的bat脚本"></a>WSL下通过命令执行Windows下的bat脚本</h4><ul>
<li><code>wsl.exe -d Ubuntu-18.04 cmd.exe /c &quot;C:\Users\program.bat&quot;</code><ul>
<li>系统发行版本与脚本路径 替换为实际的即可</li>
</ul>
</li>
</ul>
<h4 id="开启WSL的NFS服务"><a href="#开启WSL的NFS服务" class="headerlink" title="开启WSL的NFS服务"></a>开启WSL的NFS服务</h4><pre><code>实现u-boot通过nfs服务挂载WSL下的根文件系统
</code></pre>
]]></content>
      <categories>
        <category>WSL</category>
      </categories>
  </entry>
  <entry>
    <title>国民技术N32G452芯片移植RT-Thread-Nano</title>
    <url>/2023/11/28/%E5%9B%BD%E6%B0%91%E6%8A%80%E6%9C%AFN32%E8%8A%AF%E7%89%87%E7%A7%BB%E6%A4%8DRT-Thread-Nano%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[<h2 id="移植rt-thread，程序编译成功但无法运行的注意点"><a href="#移植rt-thread，程序编译成功但无法运行的注意点" class="headerlink" title="移植rt-thread，程序编译成功但无法运行的注意点"></a>移植rt-thread，程序编译成功但无法运行的注意点</h2><h3 id="rt-thread的内存布局之堆的初始化"><a href="#rt-thread的内存布局之堆的初始化" class="headerlink" title="rt-thread的内存布局之堆的初始化"></a>rt-thread的内存布局之堆的初始化</h3><ul>
<li>rt-thread的堆初始化，以__bss_end为起始地址、以芯片的内存顶部地址为结束地址，其内存布局顺序有别于常规的<code>rw-data -&gt; bss -&gt; heap -&gt; stack</code>顺序</li>
<li>因此需要关注rt-thread链接脚本的内存布局与其的堆初始化是否对应无误</li>
</ul>
]]></content>
      <categories>
        <category>MCU</category>
        <category>RTOS</category>
      </categories>
  </entry>
  <entry>
    <title>嵌入式MCU开发问题记录</title>
    <url>/2023/12/19/%E5%B5%8C%E5%85%A5%E5%BC%8FMCU%E5%BC%80%E5%8F%91%E9%97%AE%E9%A2%98%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[<h3 id="添加了bootloader的LiteOS应用程序没能跑起来"><a href="#添加了bootloader的LiteOS应用程序没能跑起来" class="headerlink" title="添加了bootloader的LiteOS应用程序没能跑起来"></a>添加了bootloader的LiteOS应用程序没能跑起来</h3><ul>
<li>原因分析：<ul>
<li>首先检查bootloader和app的链接脚本是否有误，确认bootloader跳转地址为0x8004000，而app链接脚本的FLASH起始地址为0x8004000无误</li>
<li>确认app中断向量SCB-&gt;VTOR配置正确</li>
<li>使用此前的bootloader + LiteOS APP、 gcc环境下的bootloader + 重构前的rt-thread App分别观察现象；发现gcc bootloader + rt-thread App能够正常运行，说明问题出现在LiteOS的应用程序上</li>
<li><strong>可能是中断向量仍然未配置好、可能是程序实际并未以0x8004000为起始地址链接、可能是App程序中关于IAP部分程序没配置好地址、可能是LiteOS自定义的中断向量没有8字节对齐、可能是App部分大小超过FLASH限制</strong></li>
<li>另外，不排除bootloader中有读取擦除指定地址数据的操作，当App程序过大越界可能会被擦除部分数据</li>
<li>不排除bootloader与app的系统时钟初始化可能存在不一致问题，如两者工程的启动文件都会调用到<code>system_stm32xxxx.c</code>里的系统时钟初始化函数，进入到main函数，通常用户会再次初始化系统时钟。需要保持其时钟源配置一致，避免出现初始化错误</li>
<li>另外发现，<code>Reset_handler</code>函数的hex文件地址解析 比 在map文件中的地址加1.</li>
<li>在检查链接脚本时，发现如下代码：<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">_estack = 0x20024000;    /* end of RAM */</span><br><span class="line">_sstack = 0x20000000;    /* start of RAM */</span><br></pre></td></tr></table></figure>
突发奇想，将栈顶指针<code>_estack = 0x20024000;</code>删减掉，加到如下链接脚本处，发现bootloader可以正常跳转运行了<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">._user_heap_stack :</span><br><span class="line">&#123;</span><br><span class="line">  . = ALIGN(4);</span><br><span class="line">  PROVIDE ( end = . );</span><br><span class="line">  PROVIDE ( _end = . );</span><br><span class="line">  . = . + _Min_Heap_Size;</span><br><span class="line">  . = . + _Min_Stack_Size;</span><br><span class="line">  _estack = .;</span><br><span class="line">  . = ALIGN(4);</span><br><span class="line">&#125; &gt;RAM</span><br></pre></td></tr></table></figure>
问题最终分析：</li>
<li>app的栈顶指针指向芯片的最大RAM地址，在无bootloader时，程序能够正常运行；加上了bootloader后，存在问题</li>
<li>此两者区别是：一个是由链接器自动计算堆栈的大小，一个是自己指定栈顶地址</li>
<li>进一步分析二者对程序可能的影响：</li>
</ul>
</li>
</ul>
<hr>
<h3 id="J-Link仿真器正常，但点击烧录就工作异常"><a href="#J-Link仿真器正常，但点击烧录就工作异常" class="headerlink" title="J-Link仿真器正常，但点击烧录就工作异常"></a>J-Link仿真器正常，但点击烧录就工作异常</h3><pre><code>- MCU及元件上电工作，J-Link供电不足 -- 插上电源
- 芯片在读写保护状态，需要用特定软件解除读保护后，才可烧录
</code></pre>
<hr>
<h3 id="移植TIMER-PWM驱动时-发现电机出现高频异响"><a href="#移植TIMER-PWM驱动时-发现电机出现高频异响" class="headerlink" title="移植TIMER-PWM驱动时, 发现电机出现高频异响"></a>移植TIMER-PWM驱动时, 发现电机出现高频异响</h3><ul>
<li>原因分析:<ul>
<li>经检查,高频异响是电机驱动输入pwm频率过高引起的</li>
<li>而该PWM驱动默认了MCU主频是72MHz, PWM时钟分频为72分频, 以1MHz的定时器时钟来作为基准</li>
<li>所以当该驱动移植到芯片主频为144MHz时, 使得PWM的频率为之前的两倍</li>
</ul>
</li>
<li>解决:<ul>
<li>修改该PWM驱动, 以使其更具通用性, 将时钟分频修改为变量形式 &#x3D; 系统时钟频率 &#x2F;1MHz,即可实现在不同主频芯片中的定时器基准频率为1MHz</li>
</ul>
</li>
</ul>
<hr>
<h3 id="应用LiteOS的定时器，出现bug：在软件定时器回调函数中给一个句柄写入消息，其中涉及到阻塞1s申请互斥量操作，但却一直无法写入"><a href="#应用LiteOS的定时器，出现bug：在软件定时器回调函数中给一个句柄写入消息，其中涉及到阻塞1s申请互斥量操作，但却一直无法写入" class="headerlink" title="应用LiteOS的定时器，出现bug：在软件定时器回调函数中给一个句柄写入消息，其中涉及到阻塞1s申请互斥量操作，但却一直无法写入"></a>应用LiteOS的定时器，出现bug：在软件定时器回调函数中给一个句柄写入消息，其中涉及到阻塞1s申请互斥量操作，但却一直无法写入</h3><ul>
<li>原因分析：<ul>
<li>首先检查日志，发现每次回调函数能被正常执行，但写入操作一直失败。其中为延时获取互斥量失败，但实际上没有延时</li>
<li>怀疑可能是软件定时器本身就设计不支持阻塞接口操作，所以直接返回；可能是软件设计不当导致的互斥量一直被其它任务所占用</li>
<li>排查缩小范围</li>
</ul>
</li>
</ul>
<hr>
<h3 id="应用LiteOS信号量，阻塞100tick-等待，程序却卡死在阻塞等待处，另，串口UART4中断函数异常跑飞"><a href="#应用LiteOS信号量，阻塞100tick-等待，程序却卡死在阻塞等待处，另，串口UART4中断函数异常跑飞" class="headerlink" title="应用LiteOS信号量，阻塞100tick 等待，程序却卡死在阻塞等待处，另，串口UART4中断函数异常跑飞"></a>应用LiteOS信号量，阻塞100tick 等待，程序却卡死在阻塞等待处，另，串口UART4中断函数异常跑飞</h3><p>  部分源代码节选如下：<br>  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">static uint32_t comm_board_sem;</span><br><span class="line"></span><br><span class="line">LOS_SemCreate(0, &amp;comm_board_sem);</span><br><span class="line"></span><br><span class="line">LOS_SemPost(comm_board_sem);        // 在串口接收空闲中断处调用</span><br><span class="line">  </span><br><span class="line">logVerbose(&quot;init&quot;);</span><br><span class="line">    result = LOS_SemPend(comm_board_sem, LOS_MS2Tick(100));</span><br><span class="line">logVerbose(&quot;init&quot;); // 未能执行到此处</span><br></pre></td></tr></table></figure><br>  通过断点调试，程序卡死在如下处：<br>  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/* ****************************************************************************</span><br><span class="line">Function    : ArchSysExit</span><br><span class="line">Description : Task exit function</span><br><span class="line">**************************************************************************** */</span><br><span class="line">LITE_OS_SEC_TEXT_MINOR VOID ArchSysExit(VOID)</span><br><span class="line">&#123;</span><br><span class="line">    (VOID)LOS_IntLock();</span><br><span class="line">    while (1) &#123;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br>  原因分析与解决步骤：<br>    1. 将该线程注释掉，系统能够正常运作，说明是此线程的设计问题<br>    2. 考虑可能是此处的设计导致了系统线程调度出现问题，在该线程入口函数的for循环内加上断点打印，注射掉<code>LOS_SemPend(comm_board_sem, LOS_MS2Tick(100));</code>，再行调试<br>    3. 发现程序又卡死在<code>LOS_TaskDelay()</code>中，此时精简后的线程入口函数源码如下：<br>       <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">static void comm_board_rx_task(void *parameter)</span><br><span class="line"> &#123;</span><br><span class="line">     logVerbose(&quot;xxx&quot;);</span><br><span class="line">     uint8_t recv_from_rb[256];</span><br><span class="line">     int16_t recv_ret;</span><br><span class="line">     uint8_t interpret_buf[256];</span><br><span class="line">     err_enum unpack_ret = err_ok;</span><br><span class="line">     for (;;)</span><br><span class="line">     &#123;</span><br><span class="line">         logVerbose(&quot;xxx&quot;);</span><br><span class="line">         LOS_TaskDelay(100);</span><br><span class="line">         logVerbose(&quot;xxx&quot;);      // 无法执行到此处，程序卡死</span><br><span class="line">     &#125;</span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure><br>    4. 此前该段入口函数在rt-thread中运行无误，在LiteOS中却无法运行，推测可能是在for循环之前定义了几个大数组，其存储位置在线程上下文即 线程栈中<br>    5. 而系统崩掉的原因应该是线程调度时没有考虑到如此大的数据入栈出栈，从而导致出错<br>    6. 经过排查和不断的代码注释运行，最终发现是串口的DMA收发初始化，导致程序无法运行 – 判断是uart的驱动问题<br>    7. 再次排查此前已经验证通过的uart驱动，代码对比无误，进行常规分析：可能是LiteOS的中断注册存在问题、可能是代码的驱动存在冲突、可能中断服务函数并没正确对应注册、可能是初始化步骤存在问题<br>    8. 经过验证，发现是UART4可能存在问题，另外发现一个奇葩现象，如果开启了UART1驱动的宏定义，UART4能够正常运行；直接现象是USART1的中断服务函数影响到了UART4的中断，但USART1中断函数从没被调用<br>    9.  毫不相关的USART1函数，却直接导致了UART4的使用问题；<br>    10. 突发奇想，把编译优化等级从O1改为O0，程序就能正常运行了，有可能是<strong>这部分驱动程序编写不够健壮，也可能是编译选项存在应用不当问题</strong><br>    11. 继续缩小代码范围，发现了注释与调用某一个变量，会导致程序能运行与否，另外查看其map文件对比，只一个变量的区别 – <strong>怀疑可能是字节对齐的问题</strong><br>    12. 此外，发现编译选项 <strong>-fdata-sections</strong> 的删减，可使程序正确运行，通过对比删减前后的map文件，发现在函数<code>drv_uart_init()</code>的text段大小差了4个字节；同时在当前文件bss段变量大小也差了4个字节<br>    13. 考虑代码可能存在以下几个问题：<br>        全局变量与外部变量重名，导致的链接错误；<br>        变量可能未被正确初始化，或存在一些漏洞</p>
<hr>
<h3 id="从N32L406芯片的验证通过的-DMA-USART-TX-RX驱动到N32WB452上-串口DMA发送没能跑起来"><a href="#从N32L406芯片的验证通过的-DMA-USART-TX-RX驱动到N32WB452上-串口DMA发送没能跑起来" class="headerlink" title="从N32L406芯片的验证通过的 DMA-USART TX RX驱动到N32WB452上, 串口DMA发送没能跑起来"></a>从N32L406芯片的验证通过的 DMA-USART TX RX驱动到N32WB452上, 串口DMA发送没能跑起来</h3><ul>
<li>原因分析:<ul>
<li>通过GDB调试发现DMA_GetCurrDataCounter()的数值一直没变,说明是DMA通道的数据没能发送出去</li>
<li>查看芯片手册确认DMA通道映射无误,对比确认DMA初始化流程一致</li>
<li>不同点在于N32WB452加上了LiteOS,在DMA RX驱动处加上了 添加DMA接收完成和UART空闲中断服务函数</li>
</ul>
</li>
<li>问题解决步骤:<ul>
<li>重复比对初始化步骤,以及断点调试,查看DMA的寄存器配置信息,发现DMA寄存器没有被正确初始化</li>
<li>确定为驱动初始化的原因, 再次重复确认DMA-UART的结构体初始化信息,发现为信息结构体的位置错位了<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">struct drv_uart_dev uart2 =</span><br><span class="line">&#123;</span><br><span class="line">    USART2,</span><br><span class="line">    USART2_IRQn,</span><br><span class="line">    &#123;</span><br><span class="line">        DMA1_CH6,</span><br><span class="line">        DMA1,</span><br><span class="line">        DMA1_FLAG_GL6,</span><br><span class="line">        DMA1_Channel6_IRQn,</span><br><span class="line">        0,                      // 此处缺少一个 0 变量,导致下面的变量错位</span><br><span class="line">        DMA1_CH7,</span><br><span class="line">        DMA1,</span><br><span class="line">    &#125;,</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">struct drv_uart_dev</span><br><span class="line">&#123;</span><br><span class="line">    USART_Module *uart_device;</span><br><span class="line">    IRQn_Type irq;</span><br><span class="line">    struct drv_uart_dma</span><br><span class="line">    &#123;</span><br><span class="line">        DMA_ChannelType *rx_ch;</span><br><span class="line">        DMA_Module *rx_dma_type;</span><br><span class="line">        uint32_t rx_gl_flag;</span><br><span class="line">        uint8_t rx_irq_ch;</span><br><span class="line">        uint32_t setting_dma_flag;</span><br><span class="line">        uint16_t last_recv_index;</span><br><span class="line"></span><br><span class="line">        DMA_ChannelType *tx_ch;</span><br><span class="line">        DMA_Module *tx_dma_type;</span><br><span class="line">    &#125; dma;</span><br><span class="line">    void (*user_cb)(enum uart_dev_name uart_num, uint16_t length);</span><br><span class="line">    structUsertData rx_buf_dev;</span><br><span class="line">    structUserTxData tx_buf_dev;</span><br><span class="line">    enum uart_dev_name uart_num;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
</ul>
<hr>
<h3 id="移植letter-shell组件和LiteOS-开机上电立即打印了letter-shell版本信息-但其后的打印信息阻塞延时了几秒钟-在这期间内-shell完全没任何反应"><a href="#移植letter-shell组件和LiteOS-开机上电立即打印了letter-shell版本信息-但其后的打印信息阻塞延时了几秒钟-在这期间内-shell完全没任何反应" class="headerlink" title="移植letter_shell组件和LiteOS, 开机上电立即打印了letter_shell版本信息, 但其后的打印信息阻塞延时了几秒钟, 在这期间内, shell完全没任何反应"></a>移植letter_shell组件和LiteOS, 开机上电立即打印了letter_shell版本信息, 但其后的打印信息阻塞延时了几秒钟, 在这期间内, shell完全没任何反应</h3><ul>
<li>现象分析:<ul>
<li>系统时基是从延时后才开始累计, 说明在此前Systick没有跑起来</li>
<li>一上电就能输出信息</li>
</ul>
</li>
<li>问题解决步骤:<ul>
<li>在上电正常输出日志处, 和延时处添加日志打印,不断往中间逼近,查找是哪个函数导致的阻塞延时</li>
<li>通过日志逼近打印,发现是上电初始化时与充电ic的iic通信导致的阻塞延时,并且iic没有通信成功</li>
<li>iic与SGM41528初始化时序时败,阻塞占用了接近2s的时间,接下来需要排查为何移植LiteOS的 iic通信初始化 超时失败</li>
<li>通过比较排查,已知: iic通信在刚上电时通信失败,但上电一段时间后,通信正常; 但与移植LiteOS前相比,iic的初始化调用逻辑并没有改变, 接下来需要排查为什么刚上电时的iic初始化失败</li>
<li>通过调试发现iic初始化时是有ACK回复的,但耗时高</li>
<li>最后发现是iic驱动中的微秒级延时代码存在问题, 而LiteOS在初始化Systick会配置Systick-&gt;load &#x3D; 0xFFFFFF, 之后启动系统时再次配置为 SystemCoreClock&#x2F;1000, 从而使得问题暴露<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">static void n32_udelay(uint32_t us)</span><br><span class="line">&#123;</span><br><span class="line">    uint32_t ticks;</span><br><span class="line">    uint32_t told, tnow, tcnt = 0;</span><br><span class="line">    uint32_t reload = SysTick-&gt;LOAD;</span><br><span class="line"></span><br><span class="line">    // ticks = us * reload / (1000000 / DRV_TICK_PER_SECOND);</span><br><span class="line">    ticks = us * (SystemCoreClock / 1000000); //需要的节拍数</span><br><span class="line">    told = SysTick-&gt;VAL;</span><br><span class="line">    while (1)</span><br><span class="line">    &#123;</span><br><span class="line">        tnow = SysTick-&gt;VAL;</span><br><span class="line">        if (tnow != told)</span><br><span class="line">        &#123;</span><br><span class="line">            if (tnow &lt; told)</span><br><span class="line">            &#123;</span><br><span class="line">                tcnt += told - tnow;</span><br><span class="line">            &#125;</span><br><span class="line">            else</span><br><span class="line">            &#123;</span><br><span class="line">                tcnt += reload - tnow + told;</span><br><span class="line">            &#125;</span><br><span class="line">            told = tnow;</span><br><span class="line">            if (tcnt &gt;= ticks)</span><br><span class="line">            &#123;</span><br><span class="line">                break;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
其中<code>ticks = us * reload / (1000000 / DRV_TICK_PER_SECOND);</code>更新为:<code>ticks = us * (SystemCoreClock / 1000000);</code></li>
</ul>
</li>
</ul>
<hr>
<h3 id="硬件调试"><a href="#硬件调试" class="headerlink" title="硬件调试"></a>硬件调试</h3><ul>
<li>不同批次、相同原理的板子调试时，出现异常<br>  烧录相同程序，如果结果不一，则首先排查硬件问题 – 板厂的模块芯片有无贴错、元件贴错等</li>
</ul>
]]></content>
      <categories>
        <category>MCU</category>
      </categories>
  </entry>
  <entry>
    <title>嵌入式自动初始化</title>
    <url>/2023/12/22/%E5%B5%8C%E5%85%A5%E5%BC%8FMCU%E8%87%AA%E5%8A%A8%E5%88%9D%E5%A7%8B%E5%8C%96/</url>
    <content><![CDATA[<h2 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h2><h3 id="普通驱动初始化方式"><a href="#普通驱动初始化方式" class="headerlink" title="普通驱动初始化方式"></a>普通驱动初始化方式</h3><ul>
<li>常见的编写方式为 每个模块在其文件内部编写初始化函数实例，然后通过头文件导出初始化函数接口；主任务包含各模块的头文件，然后逐一调用其初始化函数</li>
<li>这种初始化操作有以下特点<ul>
<li>显式的初始化调用较为直观，可以清楚地掌握程序的初始化流程顺序</li>
<li>头文件处理繁琐、不利于模块间的解耦合</li>
<li>编码难度较低</li>
</ul>
</li>
</ul>
<h3 id="自动初始化方式"><a href="#自动初始化方式" class="headerlink" title="自动初始化方式"></a>自动初始化方式</h3><ul>
<li>与常用初始化相对的即是隐式初始化，隐式初始化不用暴露模块的初始化接口，可以更好地解耦合</li>
<li>但相对应的，其初始化函数的调用、以及执行顺序不好把握</li>
<li>另外，这种隐式的声明给审阅代码、移植工程也带来一定不便，开发者需要花费时间掌握工程的隐式初始化接口</li>
</ul>
<h4 id="Linux下的驱动初始化"><a href="#Linux下的驱动初始化" class="headerlink" title="Linux下的驱动初始化"></a>Linux下的驱动初始化</h4><ul>
<li>Linux使用模块机制来实现驱动程序的自动初始化<ul>
<li>模块是Linux内核中的一个可加载组件，其可以包含驱动程序、函数库或其它代码</li>
<li>驱动程序模块加载到内核中时，内核会调用其初始化函数执行初始化工作，如：注册驱动程序的设备节点、初始化驱动程序的资源等</li>
<li>Linux提供了两个宏：<code>module_init()</code>、<code>module_exit()</code>分别用于注册驱动程序的初始化和卸载函数<ul>
<li>如<code>module_init(init_function);</code></li>
<li>如：<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">static int __init my_driver_init(void)</span><br><span class="line">&#123;</span><br><span class="line">    // 完成驱动程序的初始化工作</span><br><span class="line"></span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br><span class="line">module_init(my_driver_init);</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
</ul>
</li>
<li><code>module_init()</code><strong>宏将驱动初始化函数的地址存储到一个全局变量中</strong></li>
<li>内核启动时会调用<code>kernel_init()-&gt;init_module()-&gt;遍历执行所有通过module_init()注册的初始化函数</code></li>
</ul>
<h2 id="前提知识"><a href="#前提知识" class="headerlink" title="前提知识"></a>前提知识</h2><ul>
<li><code>__attribute__((used))</code>是一个函数属性，其告诉编译器保留函数在目标文件中，即使其没被调用</li>
<li><code>__attribute__((section(&quot;name&quot;)))</code>属性用于将变量或函数放置在指定的段</li>
</ul>
<h2 id="Keil环境下的自动初始化"><a href="#Keil环境下的自动初始化" class="headerlink" title="Keil环境下的自动初始化"></a>Keil环境下的自动初始化</h2><ul>
<li><p>Keil的 ARM链接器会生成用于标识段首末地址的变量，如<code>initcall0init$$Base</code>为段名<code>initcall0init</code>的首地址；<code>initcall0init$$Limit</code>表示末地址</p>
</li>
<li><p>段的声明会将函数指针存储在输出文件的Data段中，属性为RO只读</p>
</li>
</ul>
<p><strong>应用代码示例如下：</strong></p>
<ul>
<li>以下为实现自动初始化的头文件<ul>
<li><code>act_auto_initialize()</code>是用以实现自动初始化的调用</li>
<li>4个宏定义是对需要被调用的函数指针定义了4个显式执行次序<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#define  __used  __attribute__((__used__))</span><br><span class="line"></span><br><span class="line">typedef void (*initcall_t)(void);</span><br><span class="line"></span><br><span class="line">#define __define_initcall(fn, id) \</span><br><span class="line">    static const initcall_t __initcall_##fn##id __used \</span><br><span class="line">    __attribute__((__section__(&quot;initcall&quot; #id &quot;init&quot;))) = fn; </span><br><span class="line"></span><br><span class="line">#define INIT_SYSTEM_EXPORT(fn)      __define_initcall(fn, 0)</span><br><span class="line">#define INIT_BOARD_EXPORT(fn)       __define_initcall(fn, 1)</span><br><span class="line">#define INIT_DEVICE_EXPORT(fn)      __define_initcall(fn, 2)</span><br><span class="line">#define INIT_APP_EXPORT(fn)         __define_initcall(fn, 3)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">void act_auto_initialize(void);</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li>以下为自动初始化的实现<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void act_auto_initialize(void)</span><br><span class="line">&#123;</span><br><span class="line">    extern initcall_t initcall0init$$Base[];</span><br><span class="line">    extern initcall_t initcall0init$$Limit[];</span><br><span class="line">    extern initcall_t initcall1init$$Base[];</span><br><span class="line">    extern initcall_t initcall1init$$Limit[];</span><br><span class="line">    extern initcall_t initcall2init$$Base[];</span><br><span class="line">    extern initcall_t initcall2init$$Limit[];</span><br><span class="line">    extern initcall_t initcall3init$$Base[];</span><br><span class="line">    extern initcall_t initcall3init$$Limit[];</span><br><span class="line">    </span><br><span class="line">    initcall_t *fn;</span><br><span class="line">    </span><br><span class="line">    for (fn = initcall0init$$Base;</span><br><span class="line">            fn &lt; initcall0init$$Limit;</span><br><span class="line">            fn++)</span><br><span class="line">    &#123;</span><br><span class="line">        if(fn)</span><br><span class="line">            (*fn)();</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    for (fn = initcall1init$$Base;</span><br><span class="line">            fn &lt; initcall1init$$Limit;</span><br><span class="line">            fn++)</span><br><span class="line">    &#123;</span><br><span class="line">        if(fn)</span><br><span class="line">            (*fn)();</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    for (fn = initcall2init$$Base;</span><br><span class="line">            fn &lt; initcall2init$$Limit;</span><br><span class="line">            fn++)</span><br><span class="line">    &#123;</span><br><span class="line">        if(fn)</span><br><span class="line">            (*fn)();</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    for (fn = initcall3init$$Base;</span><br><span class="line">            fn &lt; initcall3init$$Limit;</span><br><span class="line">            fn++)</span><br><span class="line">    &#123;</span><br><span class="line">        if(fn)</span><br><span class="line">            (*fn)();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="适用于GCC环境下的自动初始化"><a href="#适用于GCC环境下的自动初始化" class="headerlink" title="适用于GCC环境下的自动初始化"></a>适用于GCC环境下的自动初始化</h2><ul>
<li><p>GCC环境下，可以采用事先定义两个静态函数的段，将需要初始化的函数指针声明到这个段的中间，通过执行两个静态函数地址之间的所有函数指针即可。</p>
</li>
<li><p><strong>以下源码示例片段摘选自rt-thread</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#define SECTION(x)                  __attribute__((section(x)))</span><br><span class="line">typedef int (*init_fn_t)(void);</span><br><span class="line">#define INIT_EXPORT(fn, level)                                                       \</span><br><span class="line">   RT_USED const init_fn_t __rt_init_##fn SECTION(&quot;.rti_fn.&quot; level) = fn      // 定义一个函数指针常量，并声明到指定的段名中</span><br><span class="line"></span><br><span class="line">static int rti_start(void)</span><br><span class="line">&#123;</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br><span class="line">INIT_EXPORT(rti_start, &quot;0&quot;);</span><br><span class="line">static int rti_board_start(void)</span><br><span class="line">&#123;</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br><span class="line">INIT_EXPORT(rti_board_start, &quot;0.end&quot;);</span><br><span class="line">static int rti_board_end(void)</span><br><span class="line">&#123;</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br><span class="line">INIT_EXPORT(rti_board_end, &quot;1.end&quot;);</span><br><span class="line">static int rti_end(void)</span><br><span class="line">&#123;</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br><span class="line">INIT_EXPORT(rti_end, &quot;6.end&quot;);</span><br><span class="line"></span><br><span class="line">// 系统在初始化时调用此函数</span><br><span class="line">void rt_components_init(void)  </span><br><span class="line">&#123;</span><br><span class="line">  volatile const init_fn_t *fn_ptr;</span><br><span class="line"></span><br><span class="line">  for (fn_ptr = &amp;__rt_init_rti_board_start; fn_ptr &lt; &amp;__rt_init_rti_board_end; fn_ptr++)</span><br><span class="line">  &#123;</span><br><span class="line">      (*fn_ptr)();</span><br><span class="line">  &#125;</span><br><span class="line">  </span><br><span class="line">  volatile const init_fn_t *fn_ptr;</span><br><span class="line"></span><br><span class="line">  for (fn_ptr = &amp;__rt_init_rti_board_start; fn_ptr &lt; &amp;__rt_init_rti_board_end; fn_ptr++)</span><br><span class="line">  &#123;</span><br><span class="line">      (*fn_ptr)();</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>以下为初始化函数应用接口，宏定义声明传入函数指针(函数名)即可<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#define INIT_BOARD_EXPORT(fn)           INIT_EXPORT(fn, &quot;1&quot;)</span><br><span class="line">#define INIT_PREV_EXPORT(fn)            INIT_EXPORT(fn, &quot;2&quot;)</span><br><span class="line">#define INIT_DEVICE_EXPORT(fn)          INIT_EXPORT(fn, &quot;3&quot;)</span><br><span class="line">#define INIT_COMPONENT_EXPORT(fn)       INIT_EXPORT(fn, &quot;4&quot;)</span><br><span class="line">#define INIT_ENV_EXPORT(fn)             INIT_EXPORT(fn, &quot;5&quot;)</span><br><span class="line">#define INIT_APP_EXPORT(fn)             INIT_EXPORT(fn, &quot;6&quot;)</span><br><span class="line"></span><br><span class="line">//应用示例</span><br><span class="line">void function_need_to_init(void)</span><br><span class="line">&#123;</span><br><span class="line">  // xxx</span><br><span class="line">&#125;</span><br><span class="line">INIT_BOARD_EXPORT(function_need_to_init);</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
</ul>
<h2 id="注意项"><a href="#注意项" class="headerlink" title="注意项"></a>注意项</h2><ul>
<li><p>不同的段名在内存映射中的排序</p>
<ul>
<li>通常，map文件中的段是按照首字母顺序排序的</li>
<li>另，依照rt-thread方式所编译生成<code>.map</code>文件的段排序如下，供参考：<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">.rti_fn.0                                0x08018e6c   Section        4  components.o(.rti_fn.0)</span><br><span class="line">__tagsym$$used                           0x08018e6c   Number         0  components.o(.rti_fn.0)</span><br><span class="line">.rti_fn.0.end                            0x08018e70   Section        4  components.o(.rti_fn.0.end)</span><br><span class="line">__tagsym$$used                           0x08018e70   Number         0  components.o(.rti_fn.0.end)</span><br><span class="line">.rti_fn.1                                0x08018e74   Section        4  user_board_interface.o(.rti_fn.1)</span><br><span class="line">__tagsym$$used                           0x08018e74   Number         0  user_board_interface.o(.rti_fn.1)</span><br><span class="line">.rti_fn.1.end                            0x08018e78   Section        4  components.o(.rti_fn.1.end)</span><br><span class="line">__tagsym$$used                           0x08018e78   Number         0  components.o(.rti_fn.1.end)</span><br><span class="line">.rti_fn.6                                0x08018e7c   Section        4  shell.o(.rti_fn.6)</span><br><span class="line">__tagsym$$used                           0x08018e7c   Number         0  shell.o(.rti_fn.6)</span><br><span class="line">.rti_fn.6.end                            0x08018e80   Section        4  components.o(.rti_fn.6.end)</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p>初始化函数指针声明在同一个段中的顺序</p>
<ul>
<li>实际上，此种自动初始化是函数指针的段声明，本质上是变量的声明</li>
<li>同一源文件中，其声明顺序按照它们在源代码中的布局顺序进行排列</li>
<li>不同源文件中，为按照源文件的顺序<ul>
<li>编译时可以使用<code>-M</code>选项，生成包含目标文件依赖关系的文件，通过该本文可以查看源文件顺序</li>
<li>另外，可以通过<code>.map</code>文件查看段的声明顺序，从而知道初始化顺序</li>
</ul>
</li>
</ul>
</li>
<li><p>编译选项之 函数分段 对初始化顺序的影响</p>
</li>
</ul>
]]></content>
  </entry>
  <entry>
    <title>嵌入式Qt环境搭建概述</title>
    <url>/2023/11/28/%E5%B5%8C%E5%85%A5%E5%BC%8FQt%E7%8E%AF%E5%A2%83%E6%90%AD%E5%BB%BA%E6%A6%82%E8%BF%B0/</url>
    <content><![CDATA[<p>1、下载安装通用交叉编译器<br>2、获取和编译tslib<br>    嵌入式Qt要支持触摸插件则需要编译tslib<br>3、编译ARM平台Qt5.12.9源码<br>4、移植Qt库到文件系统<br>5、在ubuntu上搭建 ARM平台的Qt开发环境</p>
<p>总结：<br>1、需要下载交叉编译器并安装、使能相应的环境变量<br>2、在ubuntu上交叉编译tslib库和ARM Qt库源码后，将编译生成的库文件移植到嵌入式平台文件系统中<br>3、在ubuntu下编写Qt应用程序，并使用交叉工具链编译，然后拷贝到嵌入式平台上运行即可</p>
]]></content>
      <categories>
        <category>Linux</category>
        <category>环境搭建</category>
      </categories>
      <tags>
        <tag>Qt</tag>
      </tags>
  </entry>
  <entry>
    <title>无刷电机调试方法简述</title>
    <url>/2023/12/08/%E6%97%A0%E5%88%B7%E7%94%B5%E6%9C%BA%E8%B0%83%E8%AF%95%E6%96%B9%E6%B3%95%E7%AE%80%E8%BF%B0/</url>
    <content><![CDATA[<h2 id="前提知识基础"><a href="#前提知识基础" class="headerlink" title="前提知识基础"></a>前提知识基础</h2><ol>
<li>需要对PID控制算法有一定了解</li>
<li>了解FOC算法原理即可，产品开发者无需深入了解FOC算法</li>
<li>需要熟悉高级定时器的应用</li>
<li>熟悉电机控制基本概念，对电机极对数、反电动势、电阻采样、DQ轴等等基本知识链需要有一定的认知</li>
</ol>
<h2 id="电机调试流程简述"><a href="#电机调试流程简述" class="headerlink" title="电机调试流程简述"></a>电机调试流程简述</h2><p>本文主要基于峰岹科技的两种产品进行开发简述，分别是三相ASIC和MCU。</p>
<h3 id="基于FT8215三相ASIC-–-图形化配置开发"><a href="#基于FT8215三相ASIC-–-图形化配置开发" class="headerlink" title="基于FT8215三相ASIC – 图形化配置开发"></a>基于FT8215三相ASIC – 图形化配置开发</h3><h3 id="基于FU6815的MCU-–-Keil开发"><a href="#基于FU6815的MCU-–-Keil开发" class="headerlink" title="基于FU6815的MCU – Keil开发"></a>基于FU6815的MCU – Keil开发</h3>]]></content>
      <categories>
        <category>无刷电机</category>
      </categories>
  </entry>
  <entry>
    <title>编译原理之我见</title>
    <url>/2023/12/01/%E7%BC%96%E8%AF%91%E5%8E%9F%E7%90%86%E4%B9%8B%E6%88%91%E8%A7%81/</url>
    <content><![CDATA[<h2 id="构建C工程中的一些疑惑解答"><a href="#构建C工程中的一些疑惑解答" class="headerlink" title="构建C工程中的一些疑惑解答"></a>构建C工程中的一些疑惑解答</h2><ul>
<li>#include “.c”文件 可行吗？</li>
<li>GNU C是个啥？其编译跟ARM CC有哪些方面的区别？</li>
<li>链接脚本与汇编源文件、C源文件的联系</li>
</ul>
<hr>
<ul>
<li>四字节对齐：要求数据的存放起始地址必须是4的倍数<br>作用：<br>1、数据结构按照4字节对齐，使得处理器更有效访问，32位系统可以一次读完一个4字节长度变量<br>2、某些硬件要求数据需要在特定的边界上对齐，否则可能出现异常<br>  如STM32，Flash的写入、DMA访问、结构体成员、线程栈PSP都需要考虑字节对齐<br>3、结构体本身没有4字节对齐，进行特定的跳转读取，可能导致异常</li>
</ul>
<hr>
<h2 id="C程序开发注意点"><a href="#C程序开发注意点" class="headerlink" title="C程序开发注意点"></a>C程序开发注意点</h2><h3 id="头文件包含"><a href="#头文件包含" class="headerlink" title="头文件包含"></a>头文件包含</h3><ul>
<li>避免冗余的头文件包含，只有当用到时才添加其头文件</li>
<li>头文件在预处理阶段会被复制到源文件中，过多包含不必要头文件会使得编译时间增加</li>
<li>头文件中定义了相同变量、函数等，可能会引起不必要的冲突；或会使得链接器产出些潜在的错误链接问题</li>
<li>尽量不要在头文件中包含头文件</li>
</ul>
<h3 id="修改编译优化级别"><a href="#修改编译优化级别" class="headerlink" title="修改编译优化级别"></a>修改编译优化级别</h3><ul>
<li><p>改变编译等级可能会导致程序运行出错</p>
<ul>
<li>提高优化级别时出错，可考虑以下：<blockquote>
<p>程序中没有放置正确的内存屏障，优化导致的乱序执行可能有问题<br>  部分代码设计需要严谨考虑时序性<br>  可能会忽略了链接脚本中指定的四字节对齐</p>
</blockquote>
</li>
<li>降低优化级别时出错：<blockquote>
<p>线程的堆栈可能偏小，导致栈溢出</p>
</blockquote>
</li>
</ul>
</li>
<li><p>修改编译等级后导致运行出错的问题排查思路</p>
</li>
</ul>
<h2 id="C编译过程"><a href="#C编译过程" class="headerlink" title="C编译过程"></a>C编译过程</h2><p>编译器的主要工作流程如下：</p>
<blockquote>
<p>源程序(source code)→预处理器(preprocessor)→编译器(compiler)→汇编程序(assembler)→目标程序(object code)→连接器(链接器，Linker)→可执行程序(executables)</p>
</blockquote>
<ul>
<li>配置</li>
<li>确定标准库和头文件位置</li>
<li>确定依赖关系</li>
<li>头文件预编译</li>
<li>预处理</li>
<li>编译</li>
<li>链接</li>
<li>安装</li>
</ul>
]]></content>
      <categories>
        <category>嵌入式</category>
      </categories>
      <tags>
        <tag>C语言</tag>
      </tags>
  </entry>
  <entry>
    <title>OpenHarmony-V4.0-LiteOS-M鸿蒙轻内核开发调试</title>
    <url>/2023/12/20/OpenHarmony-V4-0%E5%BA%94%E7%94%A8%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[<h2 id="轻量系统开发的前期流程"><a href="#轻量系统开发的前期流程" class="headerlink" title="轻量系统开发的前期流程"></a>轻量系统开发的前期流程</h2><ul>
<li>参考Gitee的OpenHarmony项目搭建命令行开发环境，熟悉基本的操作流程</li>
<li>熟悉GN、ninja的文件编写、和源码构建过程，了解子系统、组件等基本概念<ul>
<li>子系统是逻辑概念，其具体由对应的组件构成</li>
<li>组件是可复用的软件单元，其包含源码、配置文件、资源文件和编译脚本</li>
<li>通过配置文件、编译链接脚本，使得同一组件可以在不同的目标架构上运行</li>
</ul>
</li>
<li>参考社区的移植流程，根据其工程范例进一步了解轻量系统的构建开发原理</li>
</ul>
<h2 id="目标工程的编译构建流程"><a href="#目标工程的编译构建流程" class="headerlink" title="目标工程的编译构建流程"></a>目标工程的编译构建流程</h2><h2 id="编译纠错"><a href="#编译纠错" class="headerlink" title="编译纠错"></a>编译纠错</h2><ul>
<li><p>编译成功，但生成的可执行bin文件明显偏小，查看Kconfig配置文件是否有选项没写对，导致的没编译链接</p>
</li>
<li><p>提示以下信息：进入<code>kernel/liteos_m</code>，然后make menuconfig，选中兼容项，失能musl库，改为new lib。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[OHOS ERROR] arm-none-eabi-gcc: fatal error: /home/jd_chen/Downloads/gcc-arm-none-eabi-10-2020-q4-major/bin/../lib/gcc/arm-none-eabi/10.2.1/../../../../arm-none-eabi/lib/nano.specs: attempt to rename spec &#x27;link&#x27; to already defined spec &#x27;nano_link&#x27;</span><br><span class="line">[OHOS ERROR] compilation terminated.</span><br></pre></td></tr></table></figure>


</li>
<li><p>出现如下编译错误，在vfs_fs.c添加头文件&lt;stdbool.h&gt;包含即可</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[OHOS ERROR] [56/340] gcc cross compiler obj/kernel/liteos_m/components/fs/vfs/vfs.vfs_fs.o</span><br><span class="line">[OHOS ERROR] FAILED: obj/kernel/liteos_m/components/fs/vfs/vfs.vfs_fs.o </span><br><span class="line">...</span><br><span class="line">[OHOS ERROR] ../../../kernel/liteos_m/components/fs/vfs/vfs_fs.c:214:43: error: unknown type name &#x27;bool&#x27;</span><br><span class="line">[OHOS ERROR]   214 | static int VfsPathCheck(const char *path, bool isFile)</span><br><span class="line">[OHOS ERROR]       |                                           ^~~~</span><br><span class="line">[OHOS ERROR] ../../../kernel/liteos_m/components/fs/vfs/vfs_fs.c: In function &#x27;VfsOpen&#x27;:</span><br><span class="line">[OHOS ERROR] ../../../kernel/liteos_m/components/fs/vfs/vfs_fs.c:244:9: error: implicit declaration of function &#x27;VfsPathCheck&#x27; [-Werror=implicit-function-declaration]</span><br><span class="line">[OHOS ERROR]   244 |     if (VfsPathCheck(path, TRUE) != LOS_OK) </span><br><span class="line">[OHOS ERROR]       |         ^~~~~~~~~~~~</span><br><span class="line">[OHOS ERROR] cc1: all warnings being treated as errors</span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="添加系统引导、更新升级功能"><a href="#添加系统引导、更新升级功能" class="headerlink" title="添加系统引导、更新升级功能"></a>添加系统引导、更新升级功能</h2><h2 id="轻量系统应用兼容性测试适配"><a href="#轻量系统应用兼容性测试适配" class="headerlink" title="轻量系统应用兼容性测试适配"></a>轻量系统应用兼容性测试适配</h2><ul>
<li>在vendor目录下产品的config.json文件中，添加XTS子系统组件定义</li>
</ul>
<h3 id="XTS子系统编译流程"><a href="#XTS子系统编译流程" class="headerlink" title="XTS子系统编译流程"></a>XTS子系统编译流程</h3><ul>
<li>通过hb工具读取系统配置文件(即vendor目录下产品的config.json)，获取参与编译的子系统以及部件信息</li>
<li>通过gn工具会读取XTS子系统BUILD.gn–all_features会读取轻量系统全部acts测试套件<ul>
<li>判断是否debug版本<code>if (ohos_build_type == &quot;debug&quot; &amp;&amp; ohos_test_args != &quot;notest&quot;) </code>，非debug版本XTS不参与编译</li>
</ul>
</li>
<li>将轻量系统全部acts测试套件与产品的config.json子系统组件对比，存在的子系统部件参与最终编译</li>
<li>最终生成静态库.a归档路径：<code>out/.../.../libs</code><ul>
<li>XTS子系统: libmodule_xxx.a，测试框架：libhctest.a</li>
</ul>
</li>
<li>.a库链接操作</li>
</ul>
<h3 id="如何添加XTS子系统"><a href="#如何添加XTS子系统" class="headerlink" title="如何添加XTS子系统"></a>如何添加XTS子系统</h3><ul>
<li><p><code>hb build -b debug</code></p>
</li>
<li><p>目前已知在&#x2F;&#x2F;out&#x2F;xxx&#x2F;xxx&#x2F;libs有.a库文件</p>
<ul>
<li>如何将其链接进bin文件，并使其得以运行呢</li>
<li>在何处添加链接选项，添加怎样的链接选项呢？官方的链接选项会报错</li>
<li>考虑在config.gni添加合适的链接选项，同时是否需要考虑在何处添加函数调用？</li>
</ul>
</li>
<li><p>在工程配置<code>target_config.h</code>宏定义中使能Test测试项</p>
<ul>
<li>使能系统启动会进行相关函数调用</li>
</ul>
</li>
<li><p>在<code>//kernel/liteos_m</code>中make menuconfig中，使能测试相关配置</p>
</li>
<li><p>在<code>//vendor/xxx/config.json</code>添加合适的子系统配置</p>
</li>
<li><p>在<code>//device/board/xxx/liteos_m/config.gni</code>添加适配的头文件路径</p>
<ul>
<li>链接选项中增添合适的静态库链接，以实现链接测试用例相关库，其中测试静态库位于<code>//out/$&#123;company&#125;//$&#123;product&#125;//libs</code></li>
<li>还须添加源文件路径？</li>
</ul>
</li>
<li><p>在<code>//device/board/xxx/xxx.ld</code>链接脚本中添加OpenHarmony特有的测试段</p>
</li>
</ul>
<h3 id="XTS是如何测试的"><a href="#XTS是如何测试的" class="headerlink" title="XTS是如何测试的"></a>XTS是如何测试的</h3><h3 id="XTS子系统测试项"><a href="#XTS子系统测试项" class="headerlink" title="XTS子系统测试项"></a>XTS子系统测试项</h3><p>轻量系统的全部测试项包含有：应用管理(Ability和Bundle)、网络通信(LwIP)、文件系统、系统参数、Wi-Fi IOT、分布式数据管理、安全、日志、事件、分布式调度、系统更新、系统引导</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#//test/xts/acts/build_lite/BUILD.gn</span><br><span class="line">    if (ohos_kernel_type == &quot;liteos_m&quot;)</span><br><span class="line">      all_features += [</span><br><span class="line">        &quot;//test/xts/acts/ability_lite/ability_hal:ActsAbilityMgrTest&quot;,</span><br><span class="line">        &quot;//test/xts/acts/appexecfwk_lite/appexecfwk_hal:ActsBundleMgrTest&quot;,</span><br><span class="line">        &quot;//test/xts/acts/communication_lite/lwip_hal:ActsLwipTest&quot;,</span><br><span class="line"></span><br><span class="line">        #&quot;//test/xts/acts/communication_lite/wifiservice_hal:ActsWifiServiceTest&quot;,</span><br><span class="line">        &quot;//test/xts/acts/commonlibrary_lite/file_hal:ActsUtilsFileTest&quot;,</span><br><span class="line">        &quot;//test/xts/acts/startup_lite/syspara_hal:ActsParameterTest&quot;,</span><br><span class="line">        &quot;//test/xts/acts/iothardware_lite/peripheral_hal:ActsWifiIotTest&quot;,</span><br><span class="line">        &quot;//test/xts/acts/distributeddatamgr_lite/kv_store_hal:ActsKvStoreTest&quot;,</span><br><span class="line">        &quot;//test/xts/acts/security_lite/huks/liteos_m_adapter:ActsHuksHalFunctionTest&quot;,</span><br><span class="line">        &quot;//test/xts/acts/hiviewdfx_lite/hilog_hal:ActsDfxFuncTest&quot;,</span><br><span class="line">        &quot;//test/xts/acts/hiviewdfx_lite/hievent_hal:ActsHieventLiteTest&quot;,</span><br><span class="line">        &quot;//test/xts/acts/distributed_schedule_lite/system_ability_manager_hal:ActsSamgrTest&quot;,</span><br><span class="line">        &quot;//test/xts/acts/update_lite/dupdate_hal:ActsUpdaterFuncTest&quot;,</span><br><span class="line">        &quot;//test/xts/acts/startup_lite/bootstrap_hal:ActsBootstrapTest&quot;,</span><br><span class="line">      ]</span><br></pre></td></tr></table></figure>
<ul>
<li>ActsAbilityMgrTest: 测试Ability管理器相关功能，包括Ability的启动、停止、生命周期管理等。<br></li>
<li>ActsBundleMgrTest: 测试Bundle管理器相关功能，包括Bundle的安装、卸载、查询等。<br></li>
<li>ActsLwipTest: 测试LwIP网络协议栈相关功能，包括TCP&#x2F;IP连接、数据传输等。<br></li>
<li>ActsUtilsFileTest: 测试文件系统相关功能，包括文件的创建、读取、写入、删除等。<br></li>
<li>ActsParameterTest: 测试系统参数读取和设置功能。<br></li>
<li>ActsWifiIotTest: 测试Wi-Fi IOT相关功能，包括AP模式、Station模式、Socket连接等。<br></li>
<li>ActsKvStoreTest: 测试分布式数据管理相关功能，包括KV存储的读写、订阅等。<br></li>
<li>ActsHuksHalFunctionTest: 测试HUKS安全框架的HAL层功能，包括密钥管理、加解密等。<br></li>
<li>ActsDfxFuncTest: 测试HiLog日志框架功能。<br></li>
<li>ActsHieventLiteTest: 测试HiEvent事件框架功能。<br></li>
<li>ActsSamgrTest: 测试分布式调度功能，包括Ability间通信、服务发布和订阅等。<br></li>
<li>ActsUpdaterFuncTest: 测试系统更新功能。<br></li>
<li>隐藏测试项: ActsWifiServiceTest: 测试Wi-Fi服务相关功能，但被注释掉（以#开头）。<br></li>
<li>ActsBootstrapTest: 测试系统引导功能。<br></li>
</ul>
<h2 id="参考站点"><a href="#参考站点" class="headerlink" title="参考站点"></a>参考站点</h2><ul>
<li><a href="https://blog.csdn.net/Via6666/article/details/132093613">OpenHarmony L0级设备XTS适配经验分享</a></li>
</ul>
]]></content>
      <categories>
        <category>LiteOS</category>
      </categories>
  </entry>
  <entry>
    <title>Git使用小贴士</title>
    <url>/2023/12/21/git%E4%BD%BF%E7%94%A8%E5%B0%8F%E8%B4%B4%E5%A3%AB/</url>
    <content><![CDATA[<h2 id="常用命令表"><a href="#常用命令表" class="headerlink" title="常用命令表"></a>常用命令表</h2><table>
<thead>
<tr>
<th>命令名称</th>
<th>命令</th>
<th>备注</th>
</tr>
</thead>
<tbody><tr>
<td>克隆工程</td>
<td>git clone xxx</td>
<td>xxx为克隆链接</td>
</tr>
<tr>
<td>暂存文件修改、删除</td>
<td>git add -u</td>
<td>暂存文件修改、删除</td>
</tr>
<tr>
<td>暂存文件修改、新建</td>
<td>git add .</td>
<td>暂存文件修改、新建</td>
</tr>
<tr>
<td>暂存文件修改、删除、新建</td>
<td>git add -A</td>
<td>暂存文件修改、删除、新建</td>
</tr>
<tr>
<td>暂存文件</td>
<td>git add xxx</td>
<td>xxx可为目录、文件，为.时暂存所有</td>
</tr>
<tr>
<td>取消所有已暂存文件</td>
<td>git reset HEAD .</td>
<td>放弃所有暂存，不会取消修改</td>
</tr>
<tr>
<td>取消指定文件的暂存</td>
<td>git reset HEAD filepathname</td>
<td>放弃指定文件的暂存</td>
</tr>
<tr>
<td>放弃未暂存的指定文件的修改</td>
<td>git checkout – filepathname</td>
<td>放弃未暂存的文件修改</td>
</tr>
<tr>
<td>放弃未暂存的文件修改</td>
<td>git checkout .</td>
<td>不会删除新建的，未被git跟踪的文件</td>
</tr>
<tr>
<td>显示将要被删除的文件</td>
<td>git clean -n</td>
<td>显示将要被删除的文件</td>
</tr>
<tr>
<td>删除工作区新增的文件、文件夹</td>
<td>git clean -df</td>
<td>删除新增的文件和新增的文件夹</td>
</tr>
<tr>
<td>删除工作区新增的文件</td>
<td>git clean -f</td>
<td>删除当前目录下所有没被track过的文件，.gitignore指定的文件和文件夹不会被删除</td>
</tr>
<tr>
<td>提交</td>
<td>git commit -m “xxxxx”</td>
<td>xxx为备注信息</td>
</tr>
<tr>
<td>查看修改的地方</td>
<td>git diff xxx</td>
<td>查看修改的地方</td>
</tr>
<tr>
<td>查看工作区、暂存区</td>
<td>git status</td>
<td>查看工作区、暂存区</td>
</tr>
<tr>
<td>拉取</td>
<td>git pull</td>
<td>在主目录下操作</td>
</tr>
<tr>
<td>合并分支</td>
<td>git merge dev</td>
<td>把dev分支合并到当前分支</td>
</tr>
<tr>
<td>合并目标分支的指定文件</td>
<td>git merge dev –no-ff filepathname</td>
<td>合并目标分支的指定文件</td>
</tr>
<tr>
<td>推送</td>
<td>git push</td>
<td>推送</td>
</tr>
<tr>
<td>强制推送</td>
<td>git push -f origin master</td>
<td>适用于本地回退版本后修改推送，master为分支名</td>
</tr>
<tr>
<td>更新远程分支列表</td>
<td>git remote update origin –prune</td>
<td>与远程保持同步</td>
</tr>
<tr>
<td>更新子模块</td>
<td>git submodule update –init</td>
<td>首次后可不带–init</td>
</tr>
<tr>
<td>切换到某一本地分支</td>
<td>git checkout dev</td>
<td>dev为分支名</td>
</tr>
<tr>
<td>查看本地分支</td>
<td>git branch</td>
<td>查看本地分支</td>
</tr>
<tr>
<td>切换到本地某个分支</td>
<td>git checkout dev</td>
<td>切换到本地某个分支</td>
</tr>
<tr>
<td>查看本地分支与远程分支的关联情况</td>
<td>git branch -vv</td>
<td>查看本地分支与远程分支的关联情况</td>
</tr>
<tr>
<td>查看远程分支</td>
<td>git branch -a</td>
<td>查看远程分支</td>
</tr>
<tr>
<td>创建本地分支并切换</td>
<td>git checkout -b V1.1</td>
<td>V1.1为分支名</td>
</tr>
<tr>
<td>创建远程分支</td>
<td>git push –set-upstream origin V1.1</td>
<td>建立本地分支与新建远程分支的关联</td>
</tr>
<tr>
<td>新建本地分支并跟踪远程分支</td>
<td>git branch dev origin&#x2F;dev</td>
<td>dev为分支名称</td>
</tr>
<tr>
<td>新建本地分支并跟踪远程分支且切换</td>
<td>git checkout -b dev origin&#x2F;dev</td>
<td>本地新建分支 dev ，跟踪远程的同名分支 dev</td>
</tr>
<tr>
<td>本地新建分支 dev ，跟踪远程的同名分支 dev</td>
<td>git checkout –track origin&#x2F;dev</td>
<td>本地新建分支 dev ，跟踪远程的同名分支 dev</td>
</tr>
<tr>
<td>获取远程地址及通信方式</td>
<td>git remote -v</td>
<td>以git开头为ssh协议</td>
</tr>
<tr>
<td>更新本地仓库的remote地址</td>
<td>git remote set-url origin xxx</td>
<td>xxx为远程地址</td>
</tr>
<tr>
<td>回退到上一个版本</td>
<td>git reset –hard HEAD^</td>
<td>删除工作区的改动代码，撤销commit、add</td>
</tr>
<tr>
<td>撤销提交（commit）</td>
<td>git reset –soft HEAD^</td>
<td>不删除工作区的改动代码，不撤销add</td>
</tr>
<tr>
<td>查看代码提交记录</td>
<td>git log</td>
<td>查看代码提交记录</td>
</tr>
<tr>
<td>删除本地分支</td>
<td>git branch -d Chapater6</td>
<td>Chapater6为分支名</td>
</tr>
<tr>
<td>删除远程分支</td>
<td>git push origin –delete Chapater6</td>
<td></td>
</tr>
<tr>
<td>删除本地标签</td>
<td>git tag -d rel1</td>
<td>rel1为标签名</td>
</tr>
<tr>
<td>查看所有本地标签</td>
<td>git tag -l</td>
<td></td>
</tr>
<tr>
<td>删除远程标签</td>
<td>git push –delete origin prod1.0</td>
<td></td>
</tr>
<tr>
<td>查看远程仓库信息</td>
<td>git remote show origin</td>
<td>查看远程仓库信息</td>
</tr>
</tbody></table>
<h2 id="注意项解答"><a href="#注意项解答" class="headerlink" title="注意项解答"></a>注意项解答</h2><h3 id="执行git-pull会覆盖本地的修改吗？"><a href="#执行git-pull会覆盖本地的修改吗？" class="headerlink" title="执行git pull会覆盖本地的修改吗？"></a>执行git pull会覆盖本地的修改吗？</h3><ul>
<li>没有冲突的情况下，远端会直接更新至本地上，但不会改变本地未提交的变动；如果本地修改已提交，则会执行一个远端分支和本地分支的合并</li>
</ul>
<h2 id="基础非典型操作"><a href="#基础非典型操作" class="headerlink" title="基础非典型操作"></a>基础非典型操作</h2><h3 id="本地git配置"><a href="#本地git配置" class="headerlink" title="本地git配置"></a>本地git配置</h3><ul>
<li><p>配置本地与远端的SSH密钥连接流程</p>
<ul>
<li>本地生成SSH公钥和私钥(如果没有的话，另，linux下公钥通常存放于<code>~/.ssh/*.pub</code>)<ul>
<li><code>ssh-keygen -t rsa -b 4096</code></li>
</ul>
</li>
<li>复制公钥，添加至远端平台的SSH设置上</li>
</ul>
</li>
<li><p>查看本地配置：</p>
<ul>
<li><code>git config --list</code>查看当前项目的所有配置</li>
<li><code>git config --global --list</code>查看全局配置</li>
</ul>
</li>
<li><p>修改用户名(全局&#x2F;当前项目)</p>
<ul>
<li>修改全局用户名：<code>git config --global user.name &quot;xxx&quot;</code>，影响用户的所有仓库</li>
<li>修改当前路径项目的用户名：<code>git config user.name &quot;xxx&quot;</code></li>
<li>查看全局用户名：<code>git config user.name</code></li>
</ul>
</li>
</ul>
<h3 id="标签相关操作"><a href="#标签相关操作" class="headerlink" title="标签相关操作"></a>标签相关操作</h3><ul>
<li>克隆远程仓库上指定标签对应的工程：<ul>
<li>模板：<code>git clone -b v1.0.0 https://github.com/example/my_repo</code>–克隆仓库 <a href="https://github.com/example/my_repo">https://github.com/example/my_repo</a> 中标签 v1.0.0 对应的源码到当前目录</li>
<li>如：<code>git clone -b OpenHarmony-v4.0-Release https://gitee.com/openharmony/kernel_liteos_m.git</code></li>
</ul>
</li>
</ul>
<h3 id="本地仓库同时推送远端的多个平台（推送gitlab、github）"><a href="#本地仓库同时推送远端的多个平台（推送gitlab、github）" class="headerlink" title="本地仓库同时推送远端的多个平台（推送gitlab、github）"></a>本地仓库同时推送远端的多个平台（推送gitlab、github）</h3><ul>
<li>方法一：为远程仓库指定多个地址<ul>
<li>此情况下，拉取时会默认拉取第一个地址的仓库</li>
<li>当然，也可以手动拉取指定仓库，步骤略<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git remote set-url origin git@github.com:username/repo.git</span><br><span class="line">git remote set-url origin https://gitee.com/username/repo.git</span><br><span class="line"></span><br><span class="line">// 将本地仓库的master分支推送到所有远程仓库</span><br><span class="line">git push origin master</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
</ul>
<h3 id="gitignore忽略规则"><a href="#gitignore忽略规则" class="headerlink" title=".gitignore忽略规则"></a>.gitignore忽略规则</h3><pre><code>- 在git项目根目录下创建.gitignore文件，然后添加需要忽略跟踪的文件选项
- 此前已经被跟踪的文件，添加.gitignore无效
    &gt;执行`git rm --cached /path/to/remove`取消跟踪，然后提交推送

- 强行跟踪被.gitignore忽略的某个指定文件
`git add -f /path/to/add`或者 在.gitignore文件后添加`!path/to/track`(`!`表示覆盖前面的忽略规则)
如：`git add -f hello.bin` or 添加`!hello.bin`
</code></pre>
<p><strong>附上一个自用的.gitignore文件：</strong><br>    <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">.vscode</span><br><span class="line">.history</span><br><span class="line">build</span><br><span class="line">Listings</span><br><span class="line">Objects</span><br><span class="line"></span><br><span class="line"># Listing Files</span><br><span class="line">*.i</span><br><span class="line">*.lst</span><br><span class="line">*.map</span><br><span class="line"></span><br><span class="line"># Object Files</span><br><span class="line">*.axf</span><br><span class="line">*.b[0-2][0-9]</span><br><span class="line">*.b3[0-1]</span><br><span class="line">*.bak</span><br><span class="line">*.build_log.htm</span><br><span class="line">*.crf</span><br><span class="line">*.d</span><br><span class="line">*.dep</span><br><span class="line">*.elf</span><br><span class="line">*.htm</span><br><span class="line">*.iex</span><br><span class="line">*.lnp</span><br><span class="line">*.o</span><br><span class="line">*.obj</span><br><span class="line">*.sbr</span><br><span class="line"></span><br><span class="line"># Firmware Files</span><br><span class="line">*.bin</span><br><span class="line">*.h86</span><br><span class="line">*.hex</span><br><span class="line"></span><br><span class="line"># Debugger Files</span><br><span class="line">.ini</span><br></pre></td></tr></table></figure></p>
<h3 id="版本发布"><a href="#版本发布" class="headerlink" title="版本发布"></a>版本发布</h3><h2 id="进阶操作"><a href="#进阶操作" class="headerlink" title="进阶操作"></a>进阶操作</h2><h3 id="钩子配置-Git-Hooks"><a href="#钩子配置-Git-Hooks" class="headerlink" title="钩子配置(Git Hooks)"></a>钩子配置(Git Hooks)</h3><ul>
<li>Hook 即在执行某个事件之前或之后进行一些其他额外的操作，如下<ul>
<li>自动部署代码</li>
<li>自动进行代码审查</li>
<li>安全审查</li>
<li>日志记录</li>
<li>通知用户</li>
</ul>
</li>
<li>Git有许多的事件（commit、push 等等），每个事件也应了有不同的钩子（如 commit 前，commit 后）</li>
<li>使用：暂略</li>
</ul>
<h3 id="子模块"><a href="#子模块" class="headerlink" title="子模块"></a>子模块</h3><ul>
<li><p>对于需要流水线自动化作业编译的项目，可以将其工具链单独存放，作为子模块被调用</p>
</li>
<li><p>删除项目内已有的子模块配置，将其转化为本项目内容</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">cd ./path/to/submodule</span><br><span class="line">rm -rf .git</span><br><span class="line"></span><br><span class="line"># at project root directory</span><br><span class="line">git rm --cached ./path/to/submodule</span><br><span class="line">git add --all</span><br><span class="line">git commit -m &quot;xxx&quot;</span><br><span class="line">git push</span><br></pre></td></tr></table></figure>
</li>
<li><p>克隆包含子模块的仓库后，子模块的拉取<br>  <code>git submodule update --init --recursive</code></p>
</li>
</ul>
]]></content>
      <categories>
        <category>Git</category>
      </categories>
  </entry>
  <entry>
    <title>心得：LiteOS初体验</title>
    <url>/2023/11/29/%E5%BF%83%E5%BE%97%EF%BC%9ALiteOS%E5%88%9D%E4%BD%93%E9%AA%8C/</url>
    <content><![CDATA[<h3 id="环境搭建"><a href="#环境搭建" class="headerlink" title="环境搭建"></a>环境搭建</h3><h3 id="开发简述"><a href="#开发简述" class="headerlink" title="开发简述"></a>开发简述</h3><h3 id="槽点"><a href="#槽点" class="headerlink" title="槽点"></a>槽点</h3><h4 id="shell交互-uart-dma中断服务函数的配置"><a href="#shell交互-uart-dma中断服务函数的配置" class="headerlink" title="shell交互-uart-dma中断服务函数的配置"></a>shell交互-uart-dma中断服务函数的配置</h4><ul>
<li>芯片上电执行<code>LOS_KernelInit()</code>，其中会重新定义中断向量，但在这个过程之前和之后都会有日志打印</li>
<li>如果日志打印是用uart + DMA完成的，其中需要注册中断服务函数。</li>
<li>但是uart的初始化打开应该在系统初始化前执行，但中断服务函数要在系统初始化后才能注册。给编程带来不便</li>
</ul>
<h4 id="软件定时器的使用"><a href="#软件定时器的使用" class="headerlink" title="软件定时器的使用"></a>软件定时器的使用</h4><ul>
<li><p>鸡肋的软件定时器，只能在初始化时定好时间周期，后续不可更改。</p>
</li>
<li><p>其提供的API过少，只有初始化、开始、结束的有限几个接口。用户无法获取当前定时器剩余tick，无法重新设置定时周期，无法在使用过程中更改定时器的 单次或者周期定时等属性</p>
</li>
<li><p>该软件定时器中，不支持申请互斥量</p>
<ul>
<li>在写私有消息队列过程中，涉及临界区操作，需要申请互斥量，但在定时器的回调函数中申请互斥量会直接返回错误。</li>
</ul>
</li>
<li><p>定时器任务创建源码如下:<br>  如下所示：其中系统在创建定时器线程时会或上<code>OS_TASK_FLAG_SYSTEM_TASK</code>这个标志，然后在申请互斥量、信号量、事件时，都会检查当前是否运行在定时器任务中，如果为真，则直接返回错误</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">LITE_OS_SEC_TEXT_INIT UINT32 OsSwtmrTaskCreate(VOID)</span><br><span class="line">&#123;</span><br><span class="line">    UINT32 ret;</span><br><span class="line">    TSK_INIT_PARAM_S swtmrTask;</span><br><span class="line"></span><br><span class="line">    // Ignore the return code when matching CSEC rule 6.6(4).</span><br><span class="line">    (VOID)memset_s(&amp;swtmrTask, sizeof(TSK_INIT_PARAM_S), 0, sizeof(TSK_INIT_PARAM_S));</span><br><span class="line"></span><br><span class="line">    swtmrTask.pfnTaskEntry    = (TSK_ENTRY_FUNC)OsSwtmrTask;</span><br><span class="line">    swtmrTask.uwStackSize     = LOSCFG_BASE_CORE_TSK_SWTMR_STACK_SIZE;</span><br><span class="line">    swtmrTask.pcName          = &quot;Swt_Task&quot;;</span><br><span class="line">    swtmrTask.usTaskPrio      = 0;</span><br><span class="line">    ret = LOS_TaskCreate(&amp;g_swtmrTaskID, &amp;swtmrTask);</span><br><span class="line">    if (ret == LOS_OK) &#123;</span><br><span class="line">        OS_TCB_FROM_TID(g_swtmrTaskID)-&gt;taskStatus |= OS_TASK_FLAG_SYSTEM_TASK;</span><br><span class="line">    &#125;</span><br><span class="line">    return ret;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>互斥量操作时，会进行以下检查：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">STATIC_INLINE UINT32 OsMuxValidCheck(LosMuxCB *muxPended)</span><br><span class="line">&#123;</span><br><span class="line">    if (muxPended-&gt;muxStat == OS_MUX_UNUSED) &#123;</span><br><span class="line">        return LOS_ERRNO_MUX_INVALID;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    if (OS_INT_ACTIVE) &#123;</span><br><span class="line">        return LOS_ERRNO_MUX_IN_INTERR;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    if (g_losTaskLock) &#123;</span><br><span class="line">        PRINT_ERR(&quot;!!!LOS_ERRNO_MUX_PEND_IN_LOCK!!!\n&quot;);</span><br><span class="line">        return LOS_ERRNO_MUX_PEND_IN_LOCK;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    if (g_losTask.runTask-&gt;taskStatus &amp; OS_TASK_FLAG_SYSTEM_TASK) &#123;</span><br><span class="line">        return LOS_ERRNO_MUX_PEND_IN_SYSTEM_TASK;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    return LOS_OK;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h4 id="空闲任务函数"><a href="#空闲任务函数" class="headerlink" title="空闲任务函数"></a>空闲任务函数</h4><pre><code>查看LiteOS之原版api，是存在创建空闲任务钩子函数的，但最新版代码已经删除，代码如下：
</code></pre>
<p>由以下代码可知，空闲任务作用仅为回收结束任务资源，以及可选的电源管理。<br>    - 个人操作：增设一个专门的低优先级任务来执行一些空闲操作</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">LITE_OS_SEC_TEXT VOID OsIdleTask(VOID)</span><br><span class="line">&#123;</span><br><span class="line">    while (1) &#123;</span><br><span class="line">        OsRecycleFinishedTask();</span><br><span class="line"></span><br><span class="line">        if (PmEnter != NULL) &#123;</span><br><span class="line">            PmEnter();</span><br><span class="line">        &#125; else &#123;</span><br><span class="line">            (VOID)ArchEnterSleep();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">LITE_OS_SEC_TEXT_INIT UINT32 OsIdleTaskCreate(VOID)</span><br><span class="line">&#123;</span><br><span class="line">    UINT32 retVal;</span><br><span class="line">    TSK_INIT_PARAM_S taskInitParam;</span><br><span class="line">    // Ignore the return code when matching CSEC rule 6.6(4).</span><br><span class="line">    (VOID)memset_s((VOID *)(&amp;taskInitParam), sizeof(TSK_INIT_PARAM_S), 0, sizeof(TSK_INIT_PARAM_S));</span><br><span class="line">    taskInitParam.pfnTaskEntry = (TSK_ENTRY_FUNC)OsIdleTask;</span><br><span class="line">    taskInitParam.uwStackSize = LOSCFG_BASE_CORE_TSK_IDLE_STACK_SIZE;</span><br><span class="line">    taskInitParam.pcName = &quot;IdleCore000&quot;;</span><br><span class="line">    taskInitParam.usTaskPrio = OS_TASK_PRIORITY_LOWEST;</span><br><span class="line">    retVal = LOS_TaskCreateOnly(&amp;g_idleTaskID, &amp;taskInitParam);</span><br><span class="line">    if (retVal != LOS_OK) &#123;</span><br><span class="line">        return retVal;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    OsSchedSetIdleTaskSchedParam(OS_TCB_FROM_TID(g_idleTaskID));</span><br><span class="line">    return LOS_OK;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>


<h1 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h1>]]></content>
      <categories>
        <category>LiteOS</category>
        <category>心得</category>
      </categories>
      <tags>
        <tag>LiteOS</tag>
        <tag>OpenHarmony</tag>
      </tags>
  </entry>
  <entry>
    <title>IIC通信调试回顾</title>
    <url>/2023/11/30/IIC%E9%80%9A%E4%BF%A1%E8%B0%83%E8%AF%95%E5%9B%9E%E9%A1%BE/</url>
    <content><![CDATA[<h1 id="概要"><a href="#概要" class="headerlink" title="概要"></a>概要</h1><ul>
<li>不同的IIC模块，其通信读写时序存在区别，须细读芯片手册</li>
<li></li>
</ul>
<h1 id="普通IO软件模拟IIC"><a href="#普通IO软件模拟IIC" class="headerlink" title="普通IO软件模拟IIC"></a>普通IO软件模拟IIC</h1><h1 id="调试方法、步骤"><a href="#调试方法、步骤" class="headerlink" title="调试方法、步骤"></a>调试方法、步骤</h1><ul>
<li>示波器和逻辑分析仪的使用</li>
</ul>
<h1 id="调试问题回顾"><a href="#调试问题回顾" class="headerlink" title="调试问题回顾"></a>调试问题回顾</h1>]]></content>
      <categories>
        <category>MCU</category>
      </categories>
  </entry>
  <entry>
    <title>浅淡三相无刷电机驱动原理</title>
    <url>/2023/12/01/%E6%B5%85%E6%B7%A1%E4%B8%89%E7%9B%B8%E6%97%A0%E5%88%B7%E7%94%B5%E6%9C%BA%E9%A9%B1%E5%8A%A8%E5%8E%9F%E7%90%86/</url>
    <content><![CDATA[<h1 id="何为三相无刷电机？"><a href="#何为三相无刷电机？" class="headerlink" title="何为三相无刷电机？"></a>何为三相无刷电机？</h1><h1 id="如何让三相无刷电机动起来？"><a href="#如何让三相无刷电机动起来？" class="headerlink" title="如何让三相无刷电机动起来？"></a>如何让三相无刷电机动起来？</h1><h1 id="无刷电机驱动原理分析"><a href="#无刷电机驱动原理分析" class="headerlink" title="无刷电机驱动原理分析"></a>无刷电机驱动原理分析</h1><h2 id="FOC算法浅析"><a href="#FOC算法浅析" class="headerlink" title="FOC算法浅析"></a>FOC算法浅析</h2><h2 id="PID算法"><a href="#PID算法" class="headerlink" title="PID算法"></a>PID算法</h2><h1 id="无刷电机驱动设计及分析"><a href="#无刷电机驱动设计及分析" class="headerlink" title="无刷电机驱动设计及分析"></a>无刷电机驱动设计及分析</h1><h1 id="个人对无刷电机理解的一些困惑"><a href="#个人对无刷电机理解的一些困惑" class="headerlink" title="个人对无刷电机理解的一些困惑"></a>个人对无刷电机理解的一些困惑</h1>]]></content>
      <categories>
        <category>无刷电机</category>
      </categories>
  </entry>
  <entry>
    <title>C语言知识点记录</title>
    <url>/2023/12/04/C%E8%AF%AD%E8%A8%80%E7%9F%A5%E8%AF%86%E7%82%B9%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[<ul>
<li>段的详解<ul>
<li>常规的段：代码段、数据段、BSS段、堆、栈</li>
<li>init段、符号表段、重定位段、调试信息段、通过attribute指定的段<br>可通过编程将指定的函数链接到指定的段，如init段，实现在main前的初始化，等一系列自定义操作</li>
<li>链接器在链接过程中会自动生成特殊的符号，用于代表各段的首尾地址<br>如：段名init_array，那么相应的会有<code>__init_array_start</code>或者<code>__start_init_array</code>表示该段的首地址，<code>__init_array_end</code>或<code>__stop_init_array</code>表示该段的末尾地址，不同编译器的表示方式有区别</li>
</ul>
</li>
</ul>
<h2 id="预处理"><a href="#预处理" class="headerlink" title="预处理"></a>预处理</h2><ul>
<li>重复宏定义？<br>宏定义的本质是替换，如果同一个宏名定义了两个不同的量，则在编译时可能会替换两次，最终输出的为最后一次宏定义处理的值</li>
</ul>
<h2 id="关键字"><a href="#关键字" class="headerlink" title="关键字"></a>关键字</h2><ul>
<li>__attribute__ 关键字<ul>
<li>__attribute__((weak))</li>
<li>__attribute__((constructor))</li>
<li>__attribute__((__used__)) 告知编译器：即使修饰的对象在编译过程中没有被引用，也要保留<br>建议在C源文件中的汇编函数前加上,以避免高优化等级时其被优化掉 </li>
<li>__attribute__((__section__(“my_section”))) 表示将修饰的对象放在名为<code>my_section</code>的段中<br>在C文件中编程，一般可以通过特殊的符号来获取attribute声明的段的首末地址，其由链接器在链接过程中自动定义<br><br>  示例1：  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#define  __used  __attribute__((__used__))</span><br><span class="line"></span><br><span class="line">typedef void (*initcall_t)(void);</span><br><span class="line"></span><br><span class="line">#define __define_initcall(fn) \</span><br><span class="line">    static const initcall_t __initcall_##fn##id __used \</span><br><span class="line">    __attribute__((__section__(&quot;initcall_system_init&quot;))) = fn; </span><br><span class="line"></span><br><span class="line">#define INIT_SYSTEM_EXPORT(fn)      __define_initcall(fn)</span><br><span class="line"></span><br></pre></td></tr></table></figure>
  应用示例2：定义系列 赋值为函数指针的静态常量，将其声明到指定的段中  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">extern char __start_my_section[];</span><br><span class="line">extern char __stop_my_section[];</span><br><span class="line"></span><br><span class="line">char *start = __start_my_section;</span><br><span class="line">char *end = __stop_my_section;</span><br><span class="line"></span><br><span class="line">typedef void (*func_ptr)(void);</span><br><span class="line"></span><br><span class="line">for (func_ptr *f = (func_ptr *)__start_my_section; f &lt; (func_ptr *)__stop_my_section; ++f) &#123;</span><br><span class="line">    (*f)();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
</ul>
<h2 id="变量"><a href="#变量" class="headerlink" title="变量"></a>变量</h2><ul>
<li>静态局部变量，能被外界所访问吗？<blockquote>
<p>将其地址传递出去即可，因为静态变量的地址在编译时已经确定，其生存在整个程序运行期间</p>
</blockquote>
</li>
</ul>
<h2 id="函数"><a href="#函数" class="headerlink" title="函数"></a>函数</h2><ul>
<li>switch case<blockquote>
<p>当在case语句内定义变量时，需要在case语句内 加花括号限定变量的作用域，否则编译会报错</p>
</blockquote>
</li>
</ul>
]]></content>
      <categories>
        <category>C语言</category>
      </categories>
  </entry>
  <entry>
    <title>GitLab CI-CD</title>
    <url>/2023/12/04/GitLab-CI-CD/</url>
    <content><![CDATA[]]></content>
  </entry>
  <entry>
    <title>嵌入式软件架构设计</title>
    <url>/2023/12/04/%E5%B5%8C%E5%85%A5%E5%BC%8F%E8%BD%AF%E4%BB%B6%E6%9E%B6%E6%9E%84%E8%AE%BE%E8%AE%A1/</url>
    <content><![CDATA[<h2 id="嵌入式MCU开发-裸机-RTOS的优缺点"><a href="#嵌入式MCU开发-裸机-RTOS的优缺点" class="headerlink" title="嵌入式MCU开发-裸机&#x2F;RTOS的优缺点"></a>嵌入式MCU开发-裸机&#x2F;RTOS的优缺点</h2><h2 id="裸机程序的特性如下："><a href="#裸机程序的特性如下：" class="headerlink" title="  裸机程序的特性如下："></a>  裸机程序的特性如下：</h2><p>  RTOS的特性如下：</p>
<ul>
<li>对于单核MCU来说，RTOS虽不是真正的并行执行，只是宏观上达到多程序并行的效果</li>
<li>RTOS能较好地实现模块的分离和隔离，降低耦合，每个任务都有自己的任务栈，这对于业务开发、移植、维护较为友好</li>
<li>实时性比裸机要强，任务间通信，如事件、消息队列、信号等API阻塞 + Systick调度中断，可以使得任务执行 的实时性大大提高，而不是裸机程序的轮询执行</li>
</ul>
<h2 id="程序设计原则"><a href="#程序设计原则" class="headerlink" title="程序设计原则"></a>程序设计原则</h2><ul>
<li><p>层级之间的调用和信息上报</p>
<ul>
<li>下层提供直接接口调用，提供注册回调函数，禁止调用上层接口</li>
</ul>
</li>
<li><p>设计模式的应用</p>
<ul>
<li>状态机</li>
<li>命令模式<br> 事件与匹配函数执行的数组集</li>
</ul>
</li>
</ul>
<h2 id="裸机开发"><a href="#裸机开发" class="headerlink" title="裸机开发"></a>裸机开发</h2><ul>
<li><p>模块间通信</p>
<ul>
<li>实时性要求不高的情况下，可以应用 类消息队列 的方式，尽量使用避免跨文件的全局变量</li>
<li>高实时性要求下，可以采用 订阅-发布模式</li>
</ul>
</li>
<li><p>软件定时器的使用</p>
<ul>
<li>以Systick为全局时基</li>
</ul>
</li>
</ul>
<h2 id="RTOS开发"><a href="#RTOS开发" class="headerlink" title="RTOS开发"></a>RTOS开发</h2><h3 id="代码业务设计"><a href="#代码业务设计" class="headerlink" title="代码业务设计"></a>代码业务设计</h3><ul>
<li><p>消息队列的应用</p>
<ul>
<li>阻塞式消息队列</li>
</ul>
</li>
<li><p>事件的应用</p>
</li>
</ul>
<h3 id="多线程设计注意点"><a href="#多线程设计注意点" class="headerlink" title="多线程设计注意点"></a>多线程设计注意点</h3><ul>
<li>临界区问题</li>
<li>高实时性要求场合</li>
<li></li>
</ul>
]]></content>
      <categories>
        <category>MCU</category>
        <category>RTOS</category>
      </categories>
      <tags>
        <tag>设计模式</tag>
      </tags>
  </entry>
  <entry>
    <title>汇编语言基础</title>
    <url>/2023/12/05/%E6%B1%87%E7%BC%96%E8%AF%AD%E8%A8%80%E5%9F%BA%E7%A1%80/</url>
    <content><![CDATA[<h2 id="汇编语法"><a href="#汇编语法" class="headerlink" title="汇编语法"></a>汇编语法</h2><p><code>label：instruction @ comment</code></p>
<ul>
<li>label是标号，表示地址。可以通过标号得到指令的地址，任何以“：”结尾的标识符都会被识别为一个标号</li>
<li>instruction 即指令，也就是汇编指令或伪指令</li>
<li>@符号，表示后面的是注释</li>
</ul>
<p>示例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">add:</span><br><span class="line">    MOVS R0, #0X12 @设置 R0=0X12</span><br></pre></td></tr></table></figure>


<h2 id="伪操作"><a href="#伪操作" class="headerlink" title="伪操作"></a>伪操作</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">.word       用于将立即数直接存储到内存中</span><br><span class="line">.weak       弱定义，可以被其他同名符号覆盖</span><br><span class="line">.thumb_set  在 Thumb 指令集中设置符号地址</span><br><span class="line">.byte       定义单字节数据，比如.byte 0x12。</span><br><span class="line">.short      定义双字节数据，比如.short 0x1234。</span><br><span class="line">.long       定义一个 4 字节数据，比如.long 0x12345678。</span><br><span class="line">.equ        赋值语句，格式为：.equ 变量名，表达式，比如.equ num, 0x12，表示 num=0x12。</span><br><span class="line">.align      数据字节对齐，比如：.align 4 表示 4 字节对齐。</span><br><span class="line">.end        表示源文件结束。</span><br><span class="line">.global     定义一个全局符号，格式为：.global symbol，比如：.global _start。</span><br></pre></td></tr></table></figure>


<h2 id="注意点"><a href="#注意点" class="headerlink" title="注意点"></a>注意点</h2><ul>
<li>指令、伪指令、伪操作、寄存器名可以全部大写，也可以全部小写，不能大小写混用</li>
<li></li>
</ul>
<h2 id="一些疑问解答补充说明"><a href="#一些疑问解答补充说明" class="headerlink" title="一些疑问解答补充说明"></a>一些疑问解答补充说明</h2><ul>
<li>GCC和ARM CC编译环境下对汇编语法的要求有何区别？</li>
<li>如何在.c文件中编写汇编函数？</li>
<li>如何在.s文件中编写可被.c调用的 汇编函数？</li>
<li>.s文件可以包含.h文件吗</li>
<li>.s和.c文件可以互相引用对方声明的全局变量吗？</li>
</ul>
]]></content>
      <categories>
        <category>汇编</category>
      </categories>
  </entry>
  <entry>
    <title>设计模式--适配器模式</title>
    <url>/2023/12/04/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E9%80%82%E9%85%8D%E5%99%A8%E6%A8%A1%E5%BC%8F/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>C语言</category>
        <category>设计模式</category>
      </categories>
  </entry>
  <entry>
    <title>嵌入式ld链接脚本详解</title>
    <url>/2023/12/20/ld%E9%93%BE%E6%8E%A5%E8%84%9A%E6%9C%AC%E5%AD%A6%E4%B9%A0/</url>
    <content><![CDATA[<h2 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h2><p>链接脚本的主要作用：描述如何将输入文件(.o目标文件)中的段 映射到 输出文件(.bin&#x2F;.hex&#x2F;…)中的段，并控制输出文件的内存布局(地址分配)。</p>
<pre><code>注意：输入文件的段和输出文件的段是独立的，主要分为`.text`, `.data`, `.bss`, `heap`, `stack`段

输入文件的段是链接器从输入文件中读取的内容，输出文件的段是链接器将输入文件的段链接到一起后生成的内容。
</code></pre>
<p>链接脚本用于指导链接器如何将输入文件的段链接到一起，其主要由<strong>MEMORY</strong>和<strong>SECTIONS</strong>块组成</p>
<ul>
<li><code>SECTIONS</code>是必须的</li>
<li><code>MEMORY</code>可选的，如果没有，链接器则认为所有输入文件都位于同一个内存区域，并且从0x0开始</li>
</ul>
<h2 id="基本知识点补充"><a href="#基本知识点补充" class="headerlink" title="基本知识点补充"></a>基本知识点补充</h2><ul>
<li>.ld文件是GNU链接器(ld)使用的标准链接脚本文件，可使用GNU链接器的所有功能，包括符号解析、重定位、符号表生成等</li>
<li>.lds文件是旧式的链接脚本文件，其仅支持GNU链接器的部分功能，不推荐使用</li>
</ul>
<h2 id="块"><a href="#块" class="headerlink" title="块"></a>块</h2><ul>
<li><strong>MEMORY</strong><br>为链接器提供系统内存的布局信息，并确定内存区域的访问权限。链接器根据MEMORY的信息，将编译生成的.o目标文件中的代码、数据、符号等分配到不同的内存区域。<br>示例代码如下：<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">MEMORY</span><br><span class="line">&#123;</span><br><span class="line">FLASH (rx)      : ORIGIN = 0x8000000, LENGTH = 128K</span><br><span class="line">RAM (xrw)      : ORIGIN = 0x20000000, LENGTH = 32K</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
代码描述了两个内存区域：<br>  标号为<code>FLASH</code>的内存区域权限为：可读只执行，起始地址为0x8000000，长度为128K；<br>  标号为<code>RAM</code>的内存区域权限为：可读可写可执行，起始地址为0x20000000，长度为32K。<br><br><strong>而后，链接脚本可声明将指定的代码放到对应的memory区域——以下链接脚本代码将<code>.text</code>段存放到标号为<code>RAM</code>的内存区域，将<code>.data</code>段存放到标号为<code>FLASH</code>的内存区域</strong><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">SECTIONS</span><br><span class="line">&#123;</span><br><span class="line">  .text &gt;RAM</span><br><span class="line">  .data &gt;FLASH</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<hr>
<ul>
<li><strong>SECTIONS</strong><br>用于定义目标文件中各个段，包括名称、存储位置、对齐方式、内容，并且可以确定其访问权限。<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">SECTIONS</span><br><span class="line">&#123;</span><br><span class="line">  // 将可执行代码段（.text 段）存放到 RAM 区域，对齐到 4 字节边界</span><br><span class="line">  .text &gt;RAM : ALIGN(4)</span><br><span class="line"></span><br><span class="line">  // 将全局变量段（.data 段）存放到 FLASH 区域，对齐到 4 字节边界</span><br><span class="line">  .data &gt;FLASH : ALIGN(4)</span><br><span class="line"></span><br><span class="line">  // 将堆（heap）段存放到 RAM 区域，对齐到 8 字节边界</span><br><span class="line">  .heap &gt;RAM : ALIGN(8)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
另，<strong>链接脚本中的内存存放顺序是从上到下</strong></li>
</ul>
<hr>
<h2 id="命令"><a href="#命令" class="headerlink" title="命令"></a>命令</h2><ul>
<li><strong>ENTRY</strong><br>将某个入口点函数指定为起始地址，如果没有指定<code>ENTRY</code>，链接器会默认使用第一个可执行section作为程序入口点<br><code>ENTRY(Reset_Handler)</code>将Reset_Handler函数设为程序的入口点，链接器需要知道程序的入口点，才能正确地组织可执行文件的时序，才能正确地执行编译、链接、优化代码<br><strong>注意：在cortex-m4中，ENTRY(Reset_Handler)并没有将Reset_Handler实体链接到0x8000004地址，其地址由链接器根据实际链接</strong><br><br><strong>链接脚本规定了起始段放置的是isr_vector，而启动文件 规定了向量中第一个字是指定存放 栈顶地址，第二个字即0x8000004存放的则是Reset_Handler的地址</strong><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">SECTIONS</span><br><span class="line">&#123;</span><br><span class="line">  .isr_vector :</span><br><span class="line">  &#123;</span><br><span class="line">    . = ALIGN(4);</span><br><span class="line">    KEEP(*(.isr_vector))</span><br><span class="line">    . = ALIGN(4);</span><br><span class="line">  &#125; &gt;FLASH</span><br><span class="line"></span><br><span class="line">  .text :</span><br><span class="line">  &#123;</span><br><span class="line">  _stext = .;</span><br><span class="line">  . = ALIGN(4);</span><br><span class="line">  *(.text)           /* .text sections (code) */</span><br><span class="line">  xxx&#125;&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<hr>
<ul>
<li><strong>&#x2F;DISCARD&#x2F;</strong><br>用于指定链接器应丢弃的section<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/DISCARD/ :</span><br><span class="line">&#123;</span><br><span class="line">  libc.a ( * )</span><br><span class="line">  libm.a ( * )</span><br><span class="line">  libgcc.a ( * )</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
上述代码，指示链接器将C标准库、数学库、GCC库中的section丢弃，在生成的可执行文件中不需要保留这些段。<br><strong>如果有调用到以下函数，则需要保留：</strong><ul>
<li>libm.a 包含了用于实现数学函数的符号和数据，例如 sin()、cos()、sqrt() 等</li>
<li>libgcc.a 包含了用于实现 GCC 扩展的符号和数据，例如 __builtin_add()、__builtin_div() 等</li>
<li>libc.a 是 C 标准库的库文件，包含了许多实现 C 标准库函数的符号和数据，诸如 printf()、scanf()、malloc()、free() 等常用函数。如果使用了微库，也可以去掉该section</li>
</ul>
</li>
</ul>
<hr>
<ul>
<li><strong>KEEP</strong><br>用于防止链接器丢弃未被引用的段和符号<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 强制连接器保留 .text 和 .data 两个 section 中的所有符号</span><br><span class="line">SECTIONS &#123;</span><br><span class="line">.text : &#123; KEEP(*(.text.*)) &#125;</span><br><span class="line">.data : &#123; KEEP(*(.data.*)) &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li><code>*</code>表示任意多个字符，<code>.text.*</code>表示所有以 .text 开头的 section</li>
<li><code>*(.text.*)</code> 表示所有以 .text 开头的 section 中的所有符号</li>
</ul>
</li>
</ul>
<hr>
<ul>
<li>LOADADDR<br>用于指定段的加载地址<br></li>
<li>PROVIDE<br>用于在链接时提供一个符号，如果符号未被定义，则使用其提供的值<br>如：<code>PROVIDE ( end = . );</code><br></li>
<li>PROVIDE_HIDDEN<br>作用类似于PROVIDE，但提供的符号不会被其它模块看到<br>如：<code>PROVIDE_HIDDEN (__fini_array_start = .);</code></li>
</ul>
<h2 id="语句、片段示例解释"><a href="#语句、片段示例解释" class="headerlink" title="语句、片段示例解释"></a>语句、片段示例解释</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">SECTIONS</span><br><span class="line">&#123;</span><br><span class="line">. = 0x10000;  // 将地址指针设为0x10000，表示.text段从地址0x10000开始</span><br><span class="line">.text : &#123; *(.text) &#125;  // 表示将输入文件的.text段复制到输出文件的.text段中</span><br><span class="line">. = 0x8000000;        //将地址指针设为0x8000000，表示.data段从地址0x8000000开始</span><br><span class="line">.data : &#123; *(.data) &#125;</span><br><span class="line">.bss : &#123; *(.bss) &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">._user_heap_stack :</span><br><span class="line">&#123;</span><br><span class="line">    . = ALIGN(4);       // 将当前地址调整为4字节对齐，确保堆和栈数据的正确访问</span><br><span class="line">    PROVIDE ( end = . );</span><br><span class="line">    PROVIDE ( _end = . );</span><br><span class="line">    . = . + _Min_Heap_Size;</span><br><span class="line">    . = . + _Min_Stack_Size;</span><br><span class="line">    _estack = .;      // 设置 _estack 符号的值为当前地址</span><br><span class="line">    . = ALIGN(4);</span><br><span class="line">&#125; &gt;RAM</span><br></pre></td></tr></table></figure>



<h2 id="链接脚本完整示例解释"><a href="#链接脚本完整示例解释" class="headerlink" title="链接脚本完整示例解释"></a>链接脚本完整示例解释</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ENTRY(Reset_Handler)</span><br><span class="line"></span><br><span class="line">_Min_Heap_Size = 0x200;      /* required amount of heap  */</span><br><span class="line">_Min_Stack_Size = 0x1500; /* required amount of stack */</span><br><span class="line"></span><br><span class="line">MEMORY</span><br><span class="line">&#123;</span><br><span class="line">FLASH (rx)      : ORIGIN = 0x8000000, LENGTH = 128K</span><br><span class="line">RAM (xrw)      : ORIGIN = 0x20000000, LENGTH = 32K</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">SECTIONS</span><br><span class="line">&#123;</span><br><span class="line">  .isr_vector :</span><br><span class="line">  &#123;</span><br><span class="line">    . = ALIGN(4);</span><br><span class="line">    KEEP(*(.isr_vector)) /* Startup code */</span><br><span class="line">    . = ALIGN(4);</span><br><span class="line">  &#125; &gt;FLASH</span><br><span class="line"></span><br><span class="line">  .text :</span><br><span class="line">  &#123;</span><br><span class="line">    . = ALIGN(4);</span><br><span class="line">    *(.text)           /* .text sections (code) */</span><br><span class="line">    *(.text*)          /* .text* sections (code) */</span><br><span class="line">    *(.glue_7)         /* glue arm to thumb code */</span><br><span class="line">    *(.glue_7t)        /* glue thumb to arm code */</span><br><span class="line">    *(.eh_frame)</span><br><span class="line"></span><br><span class="line">    KEEP (*(.init))</span><br><span class="line">    KEEP (*(.fini))</span><br><span class="line">	</span><br><span class="line">	. = ALIGN(4);</span><br><span class="line">	__fsymtab_start = .;</span><br><span class="line">	KEEP(*(FSymTab))</span><br><span class="line">	__fsymtab_end = .;</span><br><span class="line"></span><br><span class="line">	. = ALIGN(4);</span><br><span class="line">	__vsymtab_start = .;</span><br><span class="line">	KEEP(*(VSymTab))</span><br><span class="line">	__vsymtab_end = .;</span><br><span class="line"></span><br><span class="line">	. = ALIGN(4);</span><br><span class="line">	__rt_init_start = .;</span><br><span class="line">	KEEP(*(SORT(.rti_fn*)))</span><br><span class="line">	__rt_init_end = .;</span><br><span class="line"></span><br><span class="line">    . = ALIGN(4);</span><br><span class="line">    _etext = .;        /* define a global symbols at end of code */</span><br><span class="line">    _exit = .;</span><br><span class="line">  &#125; &gt;FLASH</span><br><span class="line"></span><br><span class="line">  .rodata :</span><br><span class="line">  &#123;</span><br><span class="line">    . = ALIGN(4);</span><br><span class="line">    *(.rodata)         /* .rodata sections (constants, strings, etc.) */</span><br><span class="line">    *(.rodata*)        /* .rodata* sections (constants, strings, etc.) */</span><br><span class="line">    . = ALIGN(4);</span><br><span class="line"></span><br><span class="line">  _shell_command_start = .;</span><br><span class="line">  KEEP (*(shellCommand))</span><br><span class="line">  _shell_command_end = .;</span><br><span class="line">  &#125; &gt;FLASH</span><br><span class="line"></span><br><span class="line">  .ARM.extab   : &#123; *(.ARM.extab* .gnu.linkonce.armextab.*) &#125; &gt;FLASH</span><br><span class="line">  .ARM : &#123;</span><br><span class="line">    __exidx_start = .;</span><br><span class="line">    *(.ARM.exidx*)</span><br><span class="line">    __exidx_end = .;</span><br><span class="line">  &#125; &gt;FLASH</span><br><span class="line"></span><br><span class="line">  .preinit_array     :</span><br><span class="line">  &#123;</span><br><span class="line">    PROVIDE_HIDDEN (__preinit_array_start = .);</span><br><span class="line">    KEEP (*(.preinit_array*))</span><br><span class="line">    PROVIDE_HIDDEN (__preinit_array_end = .);</span><br><span class="line">  &#125; &gt;FLASH</span><br><span class="line">  .init_array :</span><br><span class="line">  &#123;</span><br><span class="line">    PROVIDE_HIDDEN (__init_array_start = .);</span><br><span class="line">    KEEP (*(SORT(.init_array.*)))</span><br><span class="line">    KEEP (*(.init_array*))</span><br><span class="line">    PROVIDE_HIDDEN (__init_array_end = .);</span><br><span class="line">  &#125; &gt;FLASH</span><br><span class="line">  .fini_array :</span><br><span class="line">  &#123;</span><br><span class="line">    PROVIDE_HIDDEN (__fini_array_start = .);</span><br><span class="line">    KEEP (*(SORT(.fini_array.*)))</span><br><span class="line">    KEEP (*(.fini_array*))</span><br><span class="line">    PROVIDE_HIDDEN (__fini_array_end = .);</span><br><span class="line">  &#125; &gt;FLASH</span><br><span class="line"></span><br><span class="line">  _sidata = LOADADDR(.data);</span><br><span class="line"></span><br><span class="line">  .data : </span><br><span class="line">  &#123;</span><br><span class="line">    . = ALIGN(4);</span><br><span class="line">    _sdata = .;        /* create a global symbol at data start */</span><br><span class="line">    *(.data)           /* .data sections */</span><br><span class="line">    *(.data*)          /* .data* sections */</span><br><span class="line"></span><br><span class="line">    . = ALIGN(4);</span><br><span class="line">    _edata = .;        /* define a global symbol at data end */</span><br><span class="line">  &#125; &gt;RAM AT&gt; FLASH</span><br><span class="line">  </span><br><span class="line">  . = ALIGN(4);</span><br><span class="line">  .bss :</span><br><span class="line">  &#123;</span><br><span class="line">    /* This is used by the startup in order to initialize the .bss secion */</span><br><span class="line">    _sbss = .;         /* define a global symbol at bss start */</span><br><span class="line">    __bss_start__ = _sbss;</span><br><span class="line">    *(.bss)</span><br><span class="line">    *(.bss*)</span><br><span class="line">    *(COMMON)</span><br><span class="line"></span><br><span class="line">    . = ALIGN(4);</span><br><span class="line">    _ebss = .;         /* define a global symbol at bss end */</span><br><span class="line">    __bss_end__ = _ebss;</span><br><span class="line">  &#125; &gt;RAM</span><br><span class="line"></span><br><span class="line">  /* User_heap_stack section, used to check that there is enough RAM left */</span><br><span class="line">  ._user_heap_stack :</span><br><span class="line">  &#123;</span><br><span class="line">    . = ALIGN(4);</span><br><span class="line">    PROVIDE ( end = . );</span><br><span class="line">    PROVIDE ( _end = . );</span><br><span class="line">    . = . + _Min_Heap_Size;</span><br><span class="line">    . = . + _Min_Stack_Size;</span><br><span class="line">    _estack = .;</span><br><span class="line">    . = ALIGN(4);</span><br><span class="line">  &#125; &gt;RAM</span><br><span class="line"></span><br><span class="line">  /* Remove information from the standard libraries */</span><br><span class="line">  /DISCARD/ :</span><br><span class="line">  &#123;</span><br><span class="line">    libc.a ( * )</span><br><span class="line">    libm.a ( * )</span><br><span class="line">    libgcc.a ( * )</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  .ARM.attributes 0 : &#123; *(.ARM.attributes) &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
  </entry>
  <entry>
    <title>ARM架构及相关概念</title>
    <url>/2023/12/04/ARM%E6%9E%B6%E6%9E%84%E5%8F%8A%E7%9B%B8%E5%85%B3%E6%A6%82%E5%BF%B5/</url>
    <content><![CDATA[<h2 id="ARM指令集"><a href="#ARM指令集" class="headerlink" title="ARM指令集"></a>ARM指令集</h2><h3 id="Thumb指令集"><a href="#Thumb指令集" class="headerlink" title="Thumb指令集"></a>Thumb指令集</h3><p>其是ARM指令集的一个子集，16位的代码宽度</p>
<h2 id="哈佛架构与冯诺依曼架构"><a href="#哈佛架构与冯诺依曼架构" class="headerlink" title="哈佛架构与冯诺依曼架构"></a>哈佛架构与冯诺依曼架构</h2><h2 id="CPU寄存器"><a href="#CPU寄存器" class="headerlink" title="CPU寄存器"></a>CPU寄存器</h2><h4 id="glue-7、glue-7t-和-eh-frame"><a href="#glue-7、glue-7t-和-eh-frame" class="headerlink" title="glue_7、glue_7t 和 eh_frame"></a>glue_7、glue_7t 和 eh_frame</h4><ul>
<li><p>glue_7 和 glue_7t 是 ARM 架构中用于将 ARM 代码和 Thumb 代码粘合在一起的代码。</p>
</li>
<li><p>ARM 架构支持两种指令集：ARM 指令集和 Thumb 指令集。ARM 指令集是 32 位指令集，而 Thumb 指令集是 16 位指令集。为了兼容两种指令集，ARM 架构中引入了一种称为 Thumb 嵌入 的机制。</p>
</li>
<li><p>Thumb 嵌入允许 ARM 代码中嵌入 Thumb 指令，反之亦然。当 ARM 代码中嵌入 Thumb 指令时，连接器会生成 glue_7 或 glue_7t  section 来将 ARM 代码和 Thumb 代码粘合在一起。</p>
</li>
<li><p>eh_frame 是异常处理框架（Exception Handling Frame）的缩写。异常处理框架是用于支持异常处理的一种结构。当程序发生异常时，异常处理框架会保存程序的运行状态，并将程序转到异常处理程序。</p>
</li>
<li><p>异常处理框架通常包含以下信息：<br>  程序的堆栈指针（SP）<br>  程序的返回地址（LR）<br>  异常发生时的寄存器值</p>
</li>
<li><p>eh_frame section 包含了异常处理框架的信息。当程序发生异常时，连接器会将 eh_frame section 加载到内存中，以便异常处理程序可以访问异常处理框架的信息</p>
</li>
</ul>
]]></content>
  </entry>
  <entry>
    <title>Makefile相关学习笔记</title>
    <url>/2023/12/05/Makefile%E7%9B%B8%E5%85%B3%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<h2 id="基础学习"><a href="#基础学习" class="headerlink" title="基础学习"></a>基础学习</h2><h3 id="符号"><a href="#符号" class="headerlink" title="符号"></a>符号</h3><ul>
<li>符号<code>$</code><br>表示引用定义变量的值 </li>
<li>符号<code>$@</code><br>目标的集合</li>
<li>符号<code>$&lt;</code><br>依赖文件集合中的第一个文件；通常依赖文件是以(“%”)定义的，所以其表示为符合模式的一系列文件</li>
</ul>
<h3 id="Makefile格式"><a href="#Makefile格式" class="headerlink" title="Makefile格式"></a>Makefile格式</h3><ol>
<li>直接输入命令make，系统会默认以第一条规则的目标为最终目标; 也可以指定生成目标，比如make main、make clean</li>
</ol>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">目标....： 依赖文件集合...</span><br><span class="line">    命令1</span><br><span class="line">    命令2</span><br><span class="line">    ......</span><br></pre></td></tr></table></figure>

<h3 id="规则模式与语法"><a href="#规则模式与语法" class="headerlink" title="规则模式与语法"></a>规则模式与语法</h3><ul>
<li><p>模式规则</p>
</li>
<li><p>常用函数</p>
</li>
</ul>
<h2 id="实际应用"><a href="#实际应用" class="headerlink" title="实际应用"></a>实际应用</h2><ul>
<li><p>在Makefile中，是如何将每个需要编译生成.o目标文件与.c源文件关联起来的</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># addprefix用于在每个元素前面添加一个前缀：(OUTPUT_DIR)/</span><br><span class="line"># (C_SOURCES:.c=.o)将C源文件的后缀.c替换为.o，使得C_SOURCES中的每个.c都对应相应的.o文件</span><br><span class="line">C_OBJECTS = $(addprefix $(OUTPUT_DIR)/, $(C_SOURCES:.c=.o))</span><br><span class="line"></span><br><span class="line">$(OUTPUT_DIR)/%.o: %.c              # 模式规则，要生成.o文件，则要找到对应的.c</span><br><span class="line">    mkdir -p $(dir $@)              # 如果目录不存在，则创建；(dir@)表示目标文件$@的目录部分</span><br><span class="line">    $(CC) $(INCLUDE) $(CFLAGS) -MMD -c $&lt; -o $@</span><br></pre></td></tr></table></figure>
  <br></li>
<li><p>.&#x2F;configure命令<br>  configure脚本由autoconf工具生成，在make命令之前先执行配置；执行此命令，配置编译器、库、工具等参数，并根据此参数生成一个Makefile</p>
  <br>
  </li>
<li><p>编译、包含依赖文件，避免重复编译、提高效率<br>  在编译命令中加入<code>-MMD</code>选项，告诉编译器生成依赖关系文件<br>  在Makefile里的目标文件编译规则命令之后，加入<code>-include $(wildcard $(OUTPUT_DIR)/*/*.d)</code></p>
</li>
</ul>
]]></content>
      <categories>
        <category>Linux</category>
      </categories>
  </entry>
  <entry>
    <title>PID算法</title>
    <url>/2023/12/05/PID%E7%AE%97%E6%B3%95/</url>
    <content><![CDATA[<h2 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h2><ul>
<li>PID算法，即比例、积分、微分控制算法</li>
<li>其表达式为：<code>u(t) = Kp * e(t) + Ki * ∫ e(t) dt + Kd * de(t)/dt</code><ul>
<li>其中，u(t)为输出</li>
<li>e(t)是偏差，即当前值与目标值的差值</li>
</ul>
</li>
</ul>
]]></content>
      <categories>
        <category>无刷电机</category>
      </categories>
  </entry>
  <entry>
    <title>嵌入式MCU的bootloader设计</title>
    <url>/2023/11/28/%E5%B5%8C%E5%85%A5%E5%BC%8FMCU%E7%9A%84bootloader%E8%AE%BE%E8%AE%A1/</url>
    <content><![CDATA[<h2 id="重点疑问解答"><a href="#重点疑问解答" class="headerlink" title="重点疑问解答"></a>重点疑问解答</h2><h3 id="如何保证bootloader升级失败不会导致设备变砖？"><a href="#如何保证bootloader升级失败不会导致设备变砖？" class="headerlink" title="如何保证bootloader升级失败不会导致设备变砖？"></a>如何保证bootloader升级失败不会导致设备变砖？</h3>]]></content>
  </entry>
  <entry>
    <title>Linux gpio子系统</title>
    <url>/2023/12/19/Linux-gpio%E5%AD%90%E7%B3%BB%E7%BB%9F/</url>
    <content><![CDATA[<h2 id="gpio子系统基本概念简述"><a href="#gpio子系统基本概念简述" class="headerlink" title="gpio子系统基本概念简述"></a>gpio子系统基本概念简述</h2><ul>
<li>首先，需要区分pinctrl和gpio子系统<ul>
<li>pinctrl子系统是设置引脚(PIN&#x2F;PAD)的复用属性和电气属性，一个引脚如果被复用为GPIO属性，那么就需要用到gpio子系统</li>
<li>gpio子系统，提供设置和使用GPIO的系列API，如：配置输入输出方向、设置输出电平、读取输入电平、设置输出速度、注册GPIO中断服务函数等等</li>
</ul>
</li>
</ul>
]]></content>
      <categories>
        <category>Linux</category>
        <category>I.MX6ULL</category>
      </categories>
  </entry>
  <entry>
    <title>GN和Ninja构建工具的基础入门、进阶和应用开发</title>
    <url>/2023/12/24/GN%E5%92%8Cninja%E7%9A%84%E5%8C%BA%E5%88%AB%E3%80%81%E8%81%94%E7%B3%BB%E5%92%8C%E5%BA%94%E7%94%A8%E5%BC%80%E5%8F%91/</url>
    <content><![CDATA[<ul>
<li>本文是个人对GN文件的理解，可能知识体系上有些凌乱分散，与官方表述上有些许出入</li>
<li>文章主要为谈及GN文件的常见重要概念，并基于此引申出代码示例及解释</li>
</ul>
<h2 id="基本介绍"><a href="#基本介绍" class="headerlink" title="基本介绍"></a>基本介绍</h2><ul>
<li>GN文件使用GN语言编写，其是一种基于脚本的语言，用于定义构建系统的规则和目标<ul>
<li>gn即Generate ninja，gn与ninja的关系 类似于 cmake与make</li>
<li>GN文件可以用于构建各种类型的项目，包括C++、Java、Python项目<br></li>
</ul>
</li>
<li>ninja用于执行GN文件定义的规则和目标，其是一个C++编写的构建工具，可以生成高效的构建命令，提高构建速度</li>
</ul>
<h2 id="基本概念与语法"><a href="#基本概念与语法" class="headerlink" title="基本概念与语法"></a>基本概念与语法</h2><ul>
<li>.gn是源文件，.gni是头文件，源文件可通过import引用头文件，也可使用import导入源文件<blockquote>
<p>如：<code>import(&quot;//build/config/c++/c++.gni&quot;)</code></p>
</blockquote>
</li>
<li>GN的基本概念包括：<strong>变量、函数、目标、规则、配置项、模块</strong><ul>
<li>变量：用于存储值</li>
<li>函数：用于执行操作</li>
<li>目标：用于表示构建过程中的某个对象，例如文件、库或可执行文件；<strong>目标是GN构建系统的基本单位</strong></li>
<li>规则：用于定义目标的构建方式</li>
<li>配置项：用于控制构建过程的行为</li>
<li>模块：一种组织和管理代码的方式，可以包含变量、函数、目标、规则等，便于模块化地管理和复用构建配置</li>
</ul>
</li>
</ul>
<h3 id="变量"><a href="#变量" class="headerlink" title="变量"></a>变量</h3><blockquote>
<p>GN中的变量可以使用set_default_variable()函数进行定义。变量的值可以是字符串、数字、布尔值或列表<br>变量可以存储构建过程中使用的常量值，如 参数、目标类型、选项<br>如<code>set_default_variable(&quot;target_os&quot;, &quot;linux&quot;)</code>，设置操作系统为linux</p>
</blockquote>
<ul>
<li><p>内置变量：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">target_os：当前目标的操作系统</span><br><span class="line">target_cpu：当前目标的处理器架构</span><br><span class="line">target_toolchain：当前目标的工具链</span><br><span class="line">target_out_dir：当前目标的输出目录，默认值是//out/target_name/</span><br><span class="line">target_gen_dir：当前目标的生成目录</span><br><span class="line">root_build_dir：构建系统的根目录</span><br><span class="line">buildconfig：构建系统的配置文件</span><br></pre></td></tr></table></figure>
</li>
<li><p>变量类型：数字、字符串、布尔值、列表、映射</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">x = &quot;Hello, world!&quot;</span><br><span class="line">y = 10</span><br><span class="line">z = true</span><br><span class="line"></span><br><span class="line">// 定义一个名为 list 的列表变量</span><br><span class="line">list = [ &quot;a&quot;, &quot;b&quot;, &quot;c&quot; ]</span><br><span class="line"></span><br><span class="line">// 定义一个名为 map 的映射变量</span><br><span class="line">map = &#123;</span><br><span class="line">&quot;key1&quot; = &quot;value1&quot;,</span><br><span class="line">&quot;key2&quot; = &quot;value2&quot;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p><code>$&#123;&#125;</code>–字符串插值操作符用于在字符串中插入变量、表达式或函数调用的值，如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">x = 10</span><br><span class="line">println(&quot;The value of x is $&#123;x&#125;&quot;)</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="函数"><a href="#函数" class="headerlink" title="函数"></a>函数</h3><ul>
<li><p>GN的函数形式与C语言的类似，由函数名、参数列表和函数体组成</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">function_name(parameter_list) &#123;</span><br><span class="line">// 函数体</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>用户可以使用内置函数或自定义函数</p>
</li>
<li><p>内置函数包括有如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">assert()：断言函数，如果条件不成立，则抛出异常</span><br><span class="line">concat()：字符串拼接函数</span><br><span class="line">defined()：判断变量是否定义了的函数</span><br><span class="line">dirname()：获取目录名的函数</span><br><span class="line">file()：获取文件名的函数</span><br><span class="line">join()：字符串连接函数</span><br><span class="line">path()：获取路径名的函数</span><br><span class="line">read_file()：读取文件的函数</span><br><span class="line">write_file()：写入文件的函数</span><br></pre></td></tr></table></figure></li>
<li><p>其中，read_file()示例如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># config.json</span><br><span class="line">&#123;</span><br><span class="line">&quot;product_name&quot;: &quot;My Product&quot;,</span><br><span class="line">&quot;product_version&quot;: &quot;1.0.0&quot;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">---</span><br><span class="line"># 读取product_config_path目录下的config.json文件，并将其内容以JSON格式返回</span><br><span class="line">product_cfg = read_file(&quot;$&#123;product_config_path&#125;/config.json&quot;, &quot;json&quot;)</span><br><span class="line"></span><br><span class="line"># 将product_name属性的值赋值给变量product_name</span><br><span class="line">product_name = product_cfg[&quot;product_name&quot;]</span><br></pre></td></tr></table></figure>
</li>
<li><p>用户自定义函数以及调用，示例如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// a.gn</span><br><span class="line">function add(x, y) &#123;</span><br><span class="line">return x + y;</span><br><span class="line">&#125;</span><br><span class="line">---</span><br><span class="line">// b.gn</span><br><span class="line">import(&quot;a.gn&quot;)</span><br><span class="line"></span><br><span class="line">println(&quot;The sum of 1 and 2 is $&#123;add(1, 2)&#125;&quot;)</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="目标"><a href="#目标" class="headerlink" title="目标"></a>目标</h3><ul>
<li><p>目标是构建系统要生成的文件或目录，其可以是可执行文件、库、源代码文件、文档等</p>
</li>
<li><p>BUILD.gn是GN构建系统默认使用的文件名</p>
</li>
<li><p>目标使用<code>target()</code>语句定义，语法如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">target(target_name) &#123;</span><br><span class="line">// 目标配置</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>其名称必须唯一，可以有以下属性配置项定义：deps-依赖关系、type-目标类型、output-目标输出、public-目标是否公开、configs-目标配置</p>
<ul>
<li>子目标可以使用deps属性指定<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">target(&quot;my_executable&quot;) &#123;</span><br><span class="line">type = &quot;executable&quot;       // 指定目标类型为可执行文件</span><br><span class="line">sources = [&quot;main.cc&quot;]     // 指定目标所需要的源文件列表</span><br><span class="line">deps = [&quot;my_library&quot;]     // 依赖关系，系统先构建此子目标</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p>GN目标可以使用<code>build</code>命令构建</p>
<ul>
<li>如：在当前路径下有BUILD.gn文件，其定义了名为<code>my_executable</code>的目标</li>
<li>则在命令行输入<code>gn build my_executable</code>表示构建该目标</li>
<li><code>gn build</code>表示构建所有目标</li>
<li><code>gn build my_executable:my_sub</code>表示构建目标的子目标my_sub</li>
</ul>
</li>
</ul>
<h3 id="配置项"><a href="#配置项" class="headerlink" title="配置项"></a>配置项</h3><ul>
<li><p>配置项用于配置GN构建系统的行为，其可以控制：构建目标的类型和输出、构建规则的行为、构建系统的环境</p>
</li>
<li><p>其可以分为：<strong>目标配置项、规则配置项、环境配置项</strong></p>
<ul>
<li>其与目标的联系为：配置项用于定义目标的行为，<strong>目标的配置项可以定义在目标的gn配置文件中</strong></li>
</ul>
</li>
<li><p>常用的配置项如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">target_type：定义目标的类型</span><br><span class="line">output：定义目标的输出</span><br><span class="line">deps：定义目标的依赖关系</span><br><span class="line">public：定义目标是否公开</span><br><span class="line">configs：定义目标的配置</span><br><span class="line">rule_type：定义规则的类型</span><br><span class="line">deps：定义规则的依赖关系</span><br><span class="line">inputs：定义规则的输入</span><br><span class="line">outputs：定义规则的输出</span><br><span class="line">action：定义规则的操作</span><br><span class="line">script：定义规则的脚本</span><br><span class="line">args：定义规则的参数</span><br><span class="line">vars：定义规则的变量</span><br></pre></td></tr></table></figure>
</li>
<li><p><code>config()</code>函数用于创建配置，其可以指定<strong>配置名称、编译选项、链接选项、依赖关系、宏定义、头文件目录、公开的配置、可见性</strong>等</p>
</li>
<li><p>代码应用示例如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">config(name) &#123;</span><br><span class="line">  options = []</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">config(&quot;my_config&quot;) &#123;</span><br><span class="line">  cflags = [&quot;-Wall&quot;]</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"># 将 my_config 配置应用于 my_target 目标</span><br><span class="line">executable(&quot;my_target&quot;) &#123;</span><br><span class="line">  configs = [&quot;my_config&quot;]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="规则"><a href="#规则" class="headerlink" title="规则"></a>规则</h3><p>  GN中的规则可以使用rule()函数进行定义。规则可以指定以下属性：</p>
<p>  inputs：规则需要的输入文件。<br>  outputs：规则的输出文件。<br>  action：规则执行的操作。<br>  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">rule(&quot;my_rule&quot;) &#123;</span><br><span class="line">inputs = [&quot;input.txt&quot;]</span><br><span class="line">outputs = [&quot;output.txt&quot;]</span><br><span class="line">action = &quot;echo &#x27;Hello, world!&#x27; &gt; $out&quot;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<h3 id="模块"><a href="#模块" class="headerlink" title="模块"></a>模块</h3><ul>
<li><p>模块可以是任何类型的代码，其可以包含源文件、头文件、库文件等，GN使用模块来组织代码和构建文件。</p>
<ul>
<li>如：一个模块可以包含一个应用程序的所有源文件，另一个模块可以包含一个库的所有源文件</li>
</ul>
</li>
<li><p>GN可使用模块来表示构建过程中的依赖关系</p>
<ul>
<li>如：一个模块可以依赖另一个模块的源文件或者库文件</li>
</ul>
</li>
<li><p>GN中的模块可以分为几种类型：<strong>executable(可执行文件)、library(库文件)、test(测试文件)、group(组模块)、action(动作模块)</strong></p>
</li>
<li><p>模块可以应用在GN构建系统的任何地方，包括但不限于组织代码、表示依赖关系、执行操作等</p>
</li>
<li><p>以下示例创建了一个名为my_module的模块，其包含源文件my_module.c，而后创建了一个名为my_executable的可执行文件，其依赖my_module模块</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">module(&quot;my_module&quot;) &#123;</span><br><span class="line">  sources = [&quot;my_module.c&quot;]</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">executable(&quot;my_executable&quot;) &#123;</span><br><span class="line">  sources = [&quot;my_executable.c&quot;]</span><br><span class="line">  deps = [&quot;:my_module&quot;]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<blockquote>
<p>kernel_module() 函数用于创建内核模块。内核模块是运行在操作系统内核中的模块。<br>module() 函数用于创建普通模块。普通模块可以运行在操作系统内核中，也可以运行在操作系统用户空间中</p>
</blockquote>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">kernel_module(name) &#123;</span><br><span class="line">  sources = []</span><br><span class="line">  deps = []</span><br><span class="line">  ldflags = []</span><br><span class="line">  cflags = []</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">module(name) &#123;</span><br><span class="line">  sources = []</span><br><span class="line">  deps = []</span><br><span class="line">  ldflags = []</span><br><span class="line">  cflags = []</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


<h3 id="基本语法"><a href="#基本语法" class="headerlink" title="基本语法"></a>基本语法</h3><ul>
<li><p>注释：在注释句子的行首加上<code>#</code>即可</p>
</li>
<li><p>条件语句</p>
</li>
<li><p>循环语句</p>
</li>
</ul>
<h2 id="应用实践范例"><a href="#应用实践范例" class="headerlink" title="应用实践范例"></a>应用实践范例</h2><ul>
<li><p>在构建项目时，GN会首先解析GN文件，并将工程的根目录作为起点，开始构建项目</p>
<ul>
<li>目录路径中<code>//xxx/xxx/xxx</code>中双斜杠表示工程源码的根</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">rule my_rule &#123;</span><br><span class="line"># 定义规则的输入</span><br><span class="line">input: &quot;input.cpp&quot;</span><br><span class="line"># 定义规则的输出</span><br><span class="line">output: &quot;output.o&quot;</span><br><span class="line"></span><br><span class="line"># 定义规则的执行逻辑</span><br><span class="line">action &#123;</span><br><span class="line">    command: &quot;gcc -c input.cpp -o output.o&quot;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="GN进阶操作"><a href="#GN进阶操作" class="headerlink" title="GN进阶操作"></a>GN进阶操作</h2><h3 id="自定义模板"><a href="#自定义模板" class="headerlink" title="自定义模板"></a>自定义模板</h3><ul>
<li>模板是用于重复使用代码的工具。模板可以用于创建目标、配置、或其他 GN 对象</li>
<li>自定义模板使用<code>template()</code>语句<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">template(name) &#123;</span><br><span class="line">  options = []</span><br><span class="line">  output = &quot;&quot;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li>template() 函数的输出是模板的配置</li>
<li>代码应用示例如下：<ul>
<li>名为 my_executable 的可执行文件，其使用 my_template 模板，包含其模板的所有配置（源文件、依赖）<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">template(&quot;my_template&quot;) &#123;</span><br><span class="line">  sources = [&quot;my_template.c&quot;]</span><br><span class="line">  deps = [&quot;:my_module&quot;]</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">executable(&quot;my_executable&quot;) &#123;</span><br><span class="line">  sources = [&quot;my_executable.c&quot;]</span><br><span class="line">  template = &quot;:my_template&quot;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
</ul>
<h3 id="group元目标"><a href="#group元目标" class="headerlink" title="group元目标"></a>group元目标</h3><ul>
<li><p>group不是目标，目标是指可以生成可执行文件、库文件或其它产物的构建单元</p>
</li>
<li><p>group是gn工具提供的一种元目标，其可以用于组织其它目标，但它本身并不生成任何产物</p>
</li>
<li><p>其用途包括：</p>
<ul>
<li>组织具有相同功能或目的的目标：例如，可以将所有驱动程序目标组织到一个group中，将所有库文件目标组织到另一个group中</li>
<li>定义依赖关系：例如，可以将一个group定义为另一个group的依赖，这样，如果第一个group中的任何一个目标被编译，则第二个group中的所有目标也会被编译</li>
<li>提供文档：group可以包含文档，这些文档将在gn工具生成的输出中显示。</li>
</ul>
</li>
<li><p>一个应用示例如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">group(&quot;drivers&quot;) &#123;</span><br><span class="line">  deps = [</span><br><span class="line">    &quot;//drivers/usb/host&quot;,</span><br><span class="line">    &quot;//drivers/ethernet&quot;,</span><br><span class="line">    &quot;//drivers/storage&quot;,</span><br><span class="line">  ]</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">group(&quot;libraries&quot;) &#123;</span><br><span class="line">  deps = [</span><br><span class="line">    &quot;//libraries/lib1&quot;,</span><br><span class="line">    &quot;//libraries/lib2&quot;,</span><br><span class="line">    &quot;//libraries/lib3&quot;,</span><br><span class="line">  ]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>以上代码定义了两个group，drivers目标依赖于&#x2F;&#x2F;drivers&#x2F;usb&#x2F;host、&#x2F;&#x2F;drivers&#x2F;ethernet和&#x2F;&#x2F;drivers&#x2F;storage三个目标。libraries目标依赖于&#x2F;&#x2F;libraries&#x2F;lib1、&#x2F;&#x2F;libraries&#x2F;lib2和&#x2F;&#x2F;libraries&#x2F;lib3三个目标</li>
<li>在gn工具生成的输出中，drivers和libraries两个group将显示为一个单独的目标</li>
</ul>
</li>
</ul>
<h2 id="补充说明以及疑惑解答"><a href="#补充说明以及疑惑解答" class="headerlink" title="补充说明以及疑惑解答"></a>补充说明以及疑惑解答</h2><h3 id="GN文件中如何包含工程源码的头文件路径？"><a href="#GN文件中如何包含工程源码的头文件路径？" class="headerlink" title="GN文件中如何包含工程源码的头文件路径？"></a>GN文件中如何包含工程源码的头文件路径？</h3><ul>
<li>可以使用<code>include_dirs</code>属性指定C头文件的路径，此属性是一个列表，代码示例如下：<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">target(&quot;my_executable&quot;) &#123;</span><br><span class="line">type = &quot;executable&quot;</span><br><span class="line">sources = [&quot;main.c&quot;]</span><br><span class="line">include_dirs = [&quot;/usr/include/my_headers&quot;]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li>也可使用<code>public_configs</code>属性，此属性列表包含了<code>include_dirs</code>属性的配置<ul>
<li>以下代码示例，指示构建系统在<code>//configs:my_config</code>配置中搜索<code>include_dirs</code>配置<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">target(&quot;my_executable&quot;) &#123;</span><br><span class="line">type = &quot;executable&quot;</span><br><span class="line">sources = [&quot;main.c&quot;]</span><br><span class="line">public_configs = [&quot;//configs:my_config&quot;]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><code>include_dirs</code>属性应用示例如下：<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 指定一个目录：</span><br><span class="line">include_dirs = [&quot;/usr/include/my_headers&quot;]</span><br><span class="line">// 指定多个目录：</span><br><span class="line">include_dirs = [&quot;/usr/include/my_headers&quot;, &quot;/usr/include/other_headers&quot;]</span><br><span class="line">// 指定相对路径：</span><br><span class="line">include_dirs = [&quot;../include&quot;]</span><br><span class="line">// 指定通配符：</span><br><span class="line">include_dirs = [&quot;**/*.h&quot;]</span><br></pre></td></tr></table></figure></li>
</ul>
]]></content>
      <categories>
        <category>LiteOS</category>
      </categories>
  </entry>
  <entry>
    <title>OpenHarmony-LiteOS-M内核之GN设计编译构建过程解析</title>
    <url>/2023/12/29/Harmony-LiteOS-M%E5%86%85%E6%A0%B8%E4%B9%8BGN%E8%AE%BE%E8%AE%A1%E8%A7%A3%E6%9E%90/</url>
    <content><![CDATA[<p>阅读本文，须先参阅<a href="/2023/12/24/GN%E5%92%8Cninja%E7%9A%84%E5%8C%BA%E5%88%AB%E3%80%81%E8%81%94%E7%B3%BB%E5%92%8C%E5%BA%94%E7%94%A8%E5%BC%80%E5%8F%91"><strong>GN与Ninja入门</strong></a></p>
<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><h3 id="记于2023-12-29"><a href="#记于2023-12-29" class="headerlink" title="记于2023-12-29"></a>记于2023-12-29</h3><ul>
<li>就目前而言，网上关于LiteOS-M的相关开发移植资料都是相当的少，官方文档也是写得不清不楚的，如果不是项目需求，估计没人愿意来做LiteOS-M的移植适配工作。。</li>
<li>鄙人初次接触，此前参考了社区的STM32工程移植示例，成功移植LiteOS-M并在gcc+make开发环境运行</li>
<li>但由于政策或者是项目需要，单纯的内核源码移植其实没什么用，跟别的RTOS内核没啥两样，由此，接触了一个新概念：<strong>XTS子系统</strong>，要获得开放原子基金会的认证，那么就需要通过这个XTS兼容性测评</li>
<li>要把这个XTS测试组件编译进工程里面，鄙人目前一头雾水，特别还是基于国民技术N32G452芯片移植并编译测试，目前还在熟悉状态，考虑两种方式：<ul>
<li>基于 <a href="https://gitee.com/openharmony/kernel_liteos_m">https://gitee.com/openharmony/kernel_liteos_m</a> 所拉取的源码工程，进行熟悉构建开发</li>
<li>基于 <a href="https://gitee.com/link?target=https://repo.huaweicloud.com/openharmony/os/4.0-Release/code-v4.0-Release.tar.gz">https://gitee.com/link?target=https%3A%2F%2Frepo.huaweicloud.com%2Fopenharmony%2Fos%2F4.0-Release%2Fcode-v4.0-Release.tar.gz</a> 整个OpenHarmony全量代码工程进行熟悉开发</li>
</ul>
</li>
<li>但其社区移植工程都已经过时，不适配Harmony4.0 Release，编译无法通过，要想移植，只能自己摸索、熟悉GN+Ninja的构建方式，从头理顺开发过程<ul>
<li>此前已经做了一部分的工作，包括命令行开发环境搭建、各种依赖搭建，但没有系统地进行整理，可以说是个遗憾</li>
<li>另外感觉<strong>移植编译XTS子系统、通过兼容性测评</strong>这个时间周期会较长，而且是没有任何指导文档下的独立开发，本人对此预研项目持悲观态度，目前也不能把过多时间精力投入此处，且做且看吧！</li>
</ul>
</li>
</ul>
<hr>
<h3 id="记于2024-01-02"><a href="#记于2024-01-02" class="headerlink" title="记于2024-01-02"></a>记于2024-01-02</h3><ul>
<li>客观来说，理论上移植LiteOS-M到N32G452，并编译进行XTS子系统测试是可行的，不能畏惧不前</li>
<li>目前需要的知识储备为：<strong>熟悉GN、Ninja工具，有深入的理解编译及链接原理，熟悉cortex-m架构，了解Python，熟悉RTOS内核实现</strong></li>
<li>首要任务是：<strong>基于OpenHarmony全量工程的构建方式，把最简程序运行起来</strong></li>
</ul>
<hr>
<h3 id="记于2024-01-04"><a href="#记于2024-01-04" class="headerlink" title="记于2024-01-04"></a>记于2024-01-04</h3><ul>
<li>进展远超预期</li>
<li>截止今天已经完成了soc、board、vendor的移植适配工作，主要归功于华为云-连志安的博客，参考其对于GD32的移植范例及教程，快速地完成了国民技术的适配，包括底层驱动以及最简工程测试验证</li>
<li>不禁感叹，<strong>前期的文献检索调研</strong>是相当重要的</li>
<li>目前只剩下XTS子系统的编译链接，即可开始OpenHarmony兼容性测评认证工作</li>
<li>参照零星示例，尝试在vendor&#x2F;xxx&#x2F;config.json添加了xts子系统，同时在BUILD.gn添加链接选项，可看到xts组件有编译，但没链接进bin文件，对整个XTS子系统添加、编译链接流程思路还是不清晰，需要时间</li>
</ul>
<hr>
<h2 id="轻量系统编译构建-命令执行简述"><a href="#轻量系统编译构建-命令执行简述" class="headerlink" title="轻量系统编译构建-命令执行简述"></a>轻量系统编译构建-命令执行简述</h2><h3 id="hb命令"><a href="#hb命令" class="headerlink" title="hb命令"></a>hb命令</h3><p>&emsp;&emsp;hb工具是一个命令行工具，其使用Python语言编写，能够调用gn和ninja工具来生成OpenHarmony的二进制文件。<br></p>
<ul>
<li>hb的源代码在<code>build/lite/hb</code>路径下，其入口函数为<code>__main__.py中的main()</code>，支持命令有：<code>set、env、build、clean</code></li>
<li><strong>hb命令的具体实现位于<code>//build/lite/hb_internal</code>中</strong></li>
</ul>
<h3 id="hb-set"><a href="#hb-set" class="headerlink" title="hb set"></a>hb set</h3><ul>
<li><code>hb set</code>的作用为生成产品配置文件，其会在工程根目录下生成ohos_config.json文件</li>
<li>其会读取<code>//vendor/</code>的配置，以供选择</li>
</ul>
<h4 id="调用解决方案包Kconfig的具体步骤"><a href="#调用解决方案包Kconfig的具体步骤" class="headerlink" title="调用解决方案包Kconfig的具体步骤"></a>调用解决方案包Kconfig的具体步骤</h4><ul>
<li><code>hb set</code>根据配置项的名称找到对应的 Kconfig 配置项。</li>
<li>将配置项的值写入到 Kconfig 配置项的变量中。</li>
<li>使用 make menuconfig 命令生成 .config 文件。</li>
<li>.config 文件会包含配置项的值。</li>
<li>使用 gn 命令将 .config 文件中的配置项读取到 gn 文件中</li>
</ul>
<h3 id="hb-build-copy"><a href="#hb-build-copy" class="headerlink" title="hb build (copy)"></a>hb build (copy)</h3><ul>
<li>配置好产品解决方案、芯片开发板解决方案后，即可执行<code>hb set -&gt; hb build</code>进行编译</li>
<li>hb build的作用是根据配置信息生成编译指导文件build.ninja。</li>
<li>代码路径为：build&#x2F;lite&#x2F;hb&#x2F;build&#x2F;build.py<ul>
<li>调用 gn_build 使用 gn gen生成 *.ninja 文件</li>
<li>调用 ninja_build 使用 ninja -w dupbuild&#x3D;warn -C 生成 *.o *.so *.bin 等最后的文件</li>
</ul>
</li>
</ul>
<h3 id="build-lite-配置目录梳理"><a href="#build-lite-配置目录梳理" class="headerlink" title="build lite 配置目录梳理"></a>build lite 配置目录梳理</h3><p>复述自：<a href="https://bbs.huaweicloud.com/blogs/336722">https://bbs.huaweicloud.com/blogs/336722</a></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">build/lite</span><br><span class="line">├── components                  # 组件描述文件</span><br><span class="line">├── hb                          # hb pip安装包源码</span><br><span class="line">├── make_rootfs                 # 文件系统镜像制作脚本</span><br><span class="line">├── config                      # 编译配置项</span><br><span class="line">│   ├── component               # 组件相关的模板定义</span><br><span class="line">│   ├── kernel                  # 内核相关的编译配置</span><br><span class="line">│   └── subsystem               # 子系统编译配置</span><br><span class="line">├── platform                    # ld脚本</span><br><span class="line">├── testfwk                     # 测试编译框架</span><br><span class="line">└── toolchain                   # 编译工具链配置，包括：编译器路径、编译选项、链接选项等</span><br></pre></td></tr></table></figure>

<ul>
<li><p>build&#x2F;lite&#x2F;ohos_var.gni</p>
<ul>
<li>定义了所有部件的全局变量</li>
<li>用于读取产品解决方案的配置文件config.json中的配置项，解析为gn变量</li>
<li>被build\lite\config\BUILDCONFIG.gn包含导入import</li>
</ul>
</li>
<li><p>build\lite\config\BUILDCONFIG.gn</p>
<ul>
<li>用于配置编译构建，其会import导入产品解决方案和芯片开发板解决方案的配置文件</li>
<li>解析开发板配置文件config.gni</li>
</ul>
</li>
</ul>
<h2 id="LiteOS-M内核之GN编译构建流程总览"><a href="#LiteOS-M内核之GN编译构建流程总览" class="headerlink" title="LiteOS-M内核之GN编译构建流程总览"></a>LiteOS-M内核之GN编译构建流程总览</h2><p>编译构建过程参考：<a href="https://bbs.huaweicloud.com/blogs/336721">https://bbs.huaweicloud.com/blogs/336721</a></p>
<ul>
<li><code>hb build</code>-&gt;<code>build/lite/hb_internal/build/build.py</code></li>
<li>读取板子配置，包括编译工具链、编译链接选项等</li>
<li>读取的配置文件有：<ul>
<li><code>build/lite/ohos_var.gni</code>：定义应用于所有组件的全局变量</li>
<li><code>device/&#123;company&#125;/&#123;board&#125;/liteos_m/config.gni</code>：编译LiteOS-M内核的配置 – 需要前提配置好的芯片开发板解决方案</li>
<li><code>vendor/&#123;company&#125;/&#123;board&#125;/config.json</code>：板子所应用的全量配置表，包括子系统、组件等 – 需要提前配置好的产品解决方案</li>
</ul>
</li>
<li>调用gn gen命令，读取产品配置（板级配置、内核、组件等），生成out目录和ninja文件</li>
<li>调用<code>ninja -C out/&#123;company&#125;/&#123;product&#125;/</code>开始编译</li>
<li>打包、制作可执行文件</li>
</ul>
<hr>
<p>分析<code>build/lite/BUILD.gn</code>文件中<code>ohos</code>元目标如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">group(&quot;ohos&quot;) &#123;</span><br><span class="line">  deps = []</span><br><span class="line">  if (ohos_build_target == &quot;&quot;) &#123;</span><br><span class="line">    # Step 1: Read product configuration profile.</span><br><span class="line">    # 读取/vendor/&#123;company&#125;/&#123;board&#125;/config.json</span><br><span class="line">    product_cfg = read_file(&quot;$&#123;product_config_path&#125;/config.json&quot;, &quot;json&quot;)   </span><br><span class="line"></span><br><span class="line">    parts_targets_info = read_file(</span><br><span class="line">            &quot;$&#123;root_build_dir&#125;/build_configs/parts_info/parts_modules_info.json&quot;,</span><br><span class="line">            &quot;json&quot;)</span><br><span class="line"></span><br><span class="line">    # Step 2: Loop subsystems configured by product.</span><br><span class="line">    # 遍历板子的子系统配置</span><br><span class="line">    foreach(product_configed_subsystem, product_cfg.subsystems) &#123;</span><br><span class="line">      subsystem_name = product_configed_subsystem.subsystem</span><br><span class="line"></span><br><span class="line">      # 判断XTS子系统相关，暂未搞懂</span><br><span class="line">      if (build_xts || (!build_xts &amp;&amp; subsystem_name != &quot;xts&quot;)) &#123;</span><br><span class="line">        # Step 3: Read OS subsystems profile.</span><br><span class="line">        subsystem_parts_info = &#123;</span><br><span class="line">        &#125;</span><br><span class="line">        subsystem_parts_info = read_file(</span><br><span class="line">                &quot;$&#123;root_build_dir&#125;/build_configs/mini_adapter/$&#123;subsystem_name&#125;.json&quot;,</span><br><span class="line">                &quot;json&quot;)</span><br><span class="line"></span><br><span class="line">        # Step 4: Loop components configured by product.</span><br><span class="line">        foreach(product_configed_component,</span><br><span class="line">                product_configed_subsystem.components) &#123;</span><br><span class="line">          # Step 5: Check whether the component configured by product is exist.</span><br><span class="line">          component_found = false</span><br><span class="line"></span><br><span class="line">          foreach(part_name, subsystem_parts_info.parts) &#123;</span><br><span class="line">            if (product_configed_component.component == part_name) &#123;</span><br><span class="line">              component_found = true</span><br><span class="line">            &#125;</span><br><span class="line">          &#125;</span><br><span class="line"></span><br><span class="line">          assert(</span><br><span class="line">              component_found,</span><br><span class="line">              &quot;Component \&quot;$&#123;product_configed_component.component&#125;\&quot; not found&quot; + &quot;, please check your product configuration.&quot;)</span><br><span class="line"></span><br><span class="line">          # Step 6: Loop OS components and check validity of product configuration.</span><br><span class="line">          foreach(part_name, subsystem_parts_info.parts) &#123;</span><br><span class="line">            kernel_valid = true</span><br><span class="line"></span><br><span class="line">            # Step 6.1: Skip component which not configured by product.</span><br><span class="line">            if (part_name == product_configed_component.component) &#123;</span><br><span class="line">              # Step 6.1.1: Loop OS components adapted kernel type.</span><br><span class="line"></span><br><span class="line">              assert(</span><br><span class="line">                  kernel_valid,</span><br><span class="line">                  &quot;Invalid component configed, $&#123;subsystem_name&#125;:$&#123;product_configed_component.component&#125; &quot; + &quot;not available for kernel: $&#123;product_cfg.kernel_type&#125;!&quot;)</span><br><span class="line"></span><br><span class="line">              # Step 6.1.2: Add valid component for compiling.</span><br><span class="line">              # Skip kernel target for userspace only scenario.</span><br><span class="line">              ......</span><br><span class="line">            &#125;</span><br><span class="line">          &#125;</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    # Skip device target for userspace only scenario.</span><br><span class="line">    if (!ohos_build_userspace_only) &#123;</span><br><span class="line">      # Step 7: Add device and product target by default.</span><br><span class="line">      # liteos_m kernel organise device build targets, but not by default.</span><br><span class="line">      if (product_cfg.kernel_type != &quot;liteos_m&quot;) &#123;</span><br><span class="line">        deps += [ &quot;$&#123;device_path&#125;/../&quot; ]</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125; else &#123;</span><br><span class="line">    deps += string_split(ohos_build_target, &quot;&amp;&amp;&quot;)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="编译构建调用过程"><a href="#编译构建调用过程" class="headerlink" title="编译构建调用过程"></a>编译构建调用过程</h2><p>主要分为三部分：</p>
<ul>
<li><code>vendor/&#123;company&#125;/&#123;board&#125;/</code>–产品解决方案</li>
<li><code>device/&#123;company&#125;/&#123;board&#125;/</code>–芯片开发板解决方案</li>
<li><code>kernel/liteos_m</code>–轻量系统内核</li>
</ul>
<h3 id="产品解决方案代码"><a href="#产品解决方案代码" class="headerlink" title="产品解决方案代码"></a>产品解决方案代码</h3><h3 id="芯片开发板解决方案代码"><a href="#芯片开发板解决方案代码" class="headerlink" title="芯片开发板解决方案代码"></a>芯片开发板解决方案代码</h3><h3 id="内核代码"><a href="#内核代码" class="headerlink" title="内核代码"></a>内核代码</h3><h2 id="移植思路总结"><a href="#移植思路总结" class="headerlink" title="移植思路总结"></a>移植思路总结</h2><p>此部分内容直接转述自参考文章：<a href="https://developer.huawei.com/consumer/cn/blog/topic/03889745165640019">https://developer.huawei.com/consumer/cn/blog/topic/03889745165640019</a></p>
<p>移植类型可以分为三部分：ARCH、SoC、Board</p>
<ul>
<li>ARCH为架构级别移植，代码位于kernel&#x2F;liteos_m&#x2F;arch</li>
<li>SoC为对已支持的架构做芯片厂商级的移植，代码位于device&#x2F;soc</li>
<li>Board为对芯片下不同板子的移植，代码位于device&#x2F;board</li>
<li>另，还有厂商配置相关代码，位于vendor，其主要用于编译系统、HDF配置(驱动框架的配置描述文件)：用于描述驱动程序的硬件资源、功能特性，使用Key-Value格式-由属性节点与子节点组成</li>
</ul>
<p>移植路线：**&#x2F;&#x2F;vendor -&gt; &#x2F;&#x2F;device&#x2F;board -&gt; &#x2F;&#x2F;device&#x2F;soc -&gt; ARCH**</p>
<h2 id="参考站点"><a href="#参考站点" class="headerlink" title="参考站点"></a>参考站点</h2><ul>
<li><a href="https://bbs.huaweicloud.com/blogs/336721">移植案例与原理 - build lite编译构建过程</a></li>
<li><a href="https://bbs.huaweicloud.com/blogs/336722">移植案例与原理 - build lite配置目录全梳理</a></li>
<li><a href="https://developer.huawei.com/consumer/cn/blog/topic/03893164050570049">从零开始移植OpenHarmony轻量系统</a></li>
</ul>
]]></content>
      <categories>
        <category>LiteOS</category>
        <category>RTOS</category>
        <category>OpenHarmony</category>
      </categories>
  </entry>
  <entry>
    <title>Kconfig入门、常用语法学习笔记</title>
    <url>/2024/01/05/Kconfig%E5%85%A5%E9%97%A8%E3%80%81%E5%B8%B8%E7%94%A8%E8%AF%AD%E6%B3%95%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<h2 id="概况"><a href="#概况" class="headerlink" title="概况"></a>概况</h2><h2 id="基本语法"><a href="#基本语法" class="headerlink" title="基本语法"></a>基本语法</h2><h2 id="进阶操作"><a href="#进阶操作" class="headerlink" title="进阶操作"></a>进阶操作</h2><h2 id="示例解释"><a href="#示例解释" class="headerlink" title="示例解释"></a>示例解释</h2><h2 id="注意项及疑惑解答"><a href="#注意项及疑惑解答" class="headerlink" title="注意项及疑惑解答"></a>注意项及疑惑解答</h2><p>make menuconfig<br>一定是生成.config文件吗，这个文件名是否可改<br>make<br>Makefile<br>gn<br>OpenHarmony<br>Kconfig.xx.xx文件类型<br>如何构建</p>
<ul>
<li>.json文件的用法，其属性与Kconfig的联系？</li>
</ul>
<h2 id="参考站点"><a href="#参考站点" class="headerlink" title="参考站点"></a>参考站点</h2>]]></content>
      <categories>
        <category>Linux</category>
        <category>LiteOS</category>
      </categories>
  </entry>
  <entry>
    <title>OpenHarmony轻量系统--XTS子系统编译适配及测试</title>
    <url>/2024/01/08/OpenHarmony%E8%BD%BB%E9%87%8F%E7%B3%BB%E7%BB%9F-XTS%E5%AD%90%E7%B3%BB%E7%BB%9F%E7%BC%96%E8%AF%91%E9%80%82%E9%85%8D%E5%8F%8A%E6%B5%8B%E8%AF%95/</url>
    <content><![CDATA[<h2 id="轻量系统应用兼容性测试适配"><a href="#轻量系统应用兼容性测试适配" class="headerlink" title="轻量系统应用兼容性测试适配"></a>轻量系统应用兼容性测试适配</h2><ul>
<li>在vendor目录下产品的config.json文件中，添加XTS子系统组件定义</li>
</ul>
<h3 id="XTS子系统编译流程"><a href="#XTS子系统编译流程" class="headerlink" title="XTS子系统编译流程"></a>XTS子系统编译流程</h3><ul>
<li>通过hb工具读取系统配置文件(即vendor目录下产品的config.json)，获取参与编译的子系统以及部件信息</li>
<li>通过gn工具会读取XTS子系统BUILD.gn–all_features会读取轻量系统全部acts测试套件<ul>
<li>判断是否debug版本<code>if (ohos_build_type == &quot;debug&quot; &amp;&amp; ohos_test_args != &quot;notest&quot;) </code>，非debug版本XTS不参与编译</li>
</ul>
</li>
<li>将轻量系统全部acts测试套件与产品的config.json子系统组件对比，存在的子系统部件参与最终编译</li>
<li>最终生成静态库.a归档路径：<code>out/.../.../libs</code><ul>
<li>XTS子系统: libmodule_xxx.a，测试框架：libhctest.a</li>
</ul>
</li>
<li>.a库链接操作</li>
</ul>
<h3 id="如何添加XTS子系统"><a href="#如何添加XTS子系统" class="headerlink" title="如何添加XTS子系统"></a>如何添加XTS子系统</h3><ul>
<li><p><code>hb build -b debug</code></p>
</li>
<li><p>在工程配置<code>target_config.h</code>宏定义中使能Test测试项</p>
<ul>
<li>使能系统启动会进行相关函数调用</li>
</ul>
</li>
<li><p>在<code>//kernel/liteos_m</code>中make menuconfig中，使能测试相关配置</p>
</li>
<li><p>在<code>//vendor/xxx/config.json</code>添加合适的子系统配置</p>
</li>
<li><p>在<code>//device/board/xxx/liteos_m/config.gni</code>添加适配的头文件路径</p>
<ul>
<li>链接选项中增添合适的静态库链接，以实现链接测试用例相关库，其中测试静态库文件生成于<code>//out/$&#123;company&#125;//$&#123;product&#125;//libs</code></li>
<li>还须添加源文件路径？</li>
</ul>
</li>
<li><p>在<code>//device/board/xxx/xxx.ld</code>链接脚本中添加OpenHarmony特有的测试段</p>
</li>
</ul>
<h3 id="XTS是如何测试的"><a href="#XTS是如何测试的" class="headerlink" title="XTS是如何测试的"></a>XTS是如何测试的</h3><h3 id="XTS子系统测试项"><a href="#XTS子系统测试项" class="headerlink" title="XTS子系统测试项"></a>XTS子系统测试项</h3><p>轻量系统的全部测试项包含有：应用管理(Ability和Bundle)、网络通信(LwIP)、文件系统、系统参数、Wi-Fi IOT、分布式数据管理、安全、日志、事件、分布式调度、系统更新、系统引导</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#//test/xts/acts/build_lite/BUILD.gn</span><br><span class="line">    if (ohos_kernel_type == &quot;liteos_m&quot;)</span><br><span class="line">      all_features += [</span><br><span class="line">        &quot;//test/xts/acts/ability_lite/ability_hal:ActsAbilityMgrTest&quot;,</span><br><span class="line">        &quot;//test/xts/acts/appexecfwk_lite/appexecfwk_hal:ActsBundleMgrTest&quot;,</span><br><span class="line">        &quot;//test/xts/acts/communication_lite/lwip_hal:ActsLwipTest&quot;,</span><br><span class="line"></span><br><span class="line">        #&quot;//test/xts/acts/communication_lite/wifiservice_hal:ActsWifiServiceTest&quot;,</span><br><span class="line">        &quot;//test/xts/acts/commonlibrary_lite/file_hal:ActsUtilsFileTest&quot;,</span><br><span class="line">        &quot;//test/xts/acts/startup_lite/syspara_hal:ActsParameterTest&quot;,</span><br><span class="line">        &quot;//test/xts/acts/iothardware_lite/peripheral_hal:ActsWifiIotTest&quot;,</span><br><span class="line">        &quot;//test/xts/acts/distributeddatamgr_lite/kv_store_hal:ActsKvStoreTest&quot;,</span><br><span class="line">        &quot;//test/xts/acts/security_lite/huks/liteos_m_adapter:ActsHuksHalFunctionTest&quot;,</span><br><span class="line">        &quot;//test/xts/acts/hiviewdfx_lite/hilog_hal:ActsDfxFuncTest&quot;,</span><br><span class="line">        &quot;//test/xts/acts/hiviewdfx_lite/hievent_hal:ActsHieventLiteTest&quot;,</span><br><span class="line">        &quot;//test/xts/acts/distributed_schedule_lite/system_ability_manager_hal:ActsSamgrTest&quot;,</span><br><span class="line">        &quot;//test/xts/acts/update_lite/dupdate_hal:ActsUpdaterFuncTest&quot;,</span><br><span class="line">        &quot;//test/xts/acts/startup_lite/bootstrap_hal:ActsBootstrapTest&quot;,</span><br><span class="line">      ]</span><br></pre></td></tr></table></figure>
<ul>
<li>ActsAbilityMgrTest: 测试Ability管理器相关功能，包括Ability的启动、停止、生命周期管理等。<br></li>
<li>ActsBundleMgrTest: 测试Bundle管理器相关功能，包括Bundle的安装、卸载、查询等。<br></li>
<li>ActsLwipTest: 测试LwIP网络协议栈相关功能，包括TCP&#x2F;IP连接、数据传输等。<br></li>
<li>ActsUtilsFileTest: 测试文件系统相关功能，包括文件的创建、读取、写入、删除等。<br></li>
<li>ActsParameterTest: 测试系统参数读取和设置功能。<br></li>
<li>ActsWifiIotTest: 测试Wi-Fi IOT相关功能，包括AP模式、Station模式、Socket连接等。<br></li>
<li>ActsKvStoreTest: 测试分布式数据管理相关功能，包括KV存储的读写、订阅等。<br></li>
<li>ActsHuksHalFunctionTest: 测试HUKS安全框架的HAL层功能，包括密钥管理、加解密等。<br></li>
<li>ActsDfxFuncTest: 测试HiLog日志框架功能。<br></li>
<li>ActsHieventLiteTest: 测试HiEvent事件框架功能。<br></li>
<li>ActsSamgrTest: 测试分布式调度功能，包括Ability间通信、服务发布和订阅等。<br></li>
<li>ActsUpdaterFuncTest: 测试系统更新功能。<br></li>
<li>隐藏测试项: ActsWifiServiceTest: 测试Wi-Fi服务相关功能，但被注释掉（以#开头）。<br></li>
<li>ActsBootstrapTest: 测试系统引导功能。<br></li>
</ul>
<h2 id="参考站点"><a href="#参考站点" class="headerlink" title="参考站点"></a>参考站点</h2><ul>
<li><a href="https://blog.csdn.net/Via6666/article/details/132093613">OpenHarmony L0级设备XTS适配经验分享</a></li>
<li><a href="https://bbs.huaweicloud.com/blogs/336717">移植案例与原理 - XTS子系统之应用兼容性测试套件</a></li>
</ul>
]]></content>
      <categories>
        <category>LiteOS</category>
        <category>OpenHarmony</category>
      </categories>
  </entry>
  <entry>
    <title>杂项笔记</title>
    <url>/2023/11/29/%E6%9D%82%E9%A1%B9%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<h3 id="hexo主题相关"><a href="#hexo主题相关" class="headerlink" title="hexo主题相关"></a>hexo主题相关</h3><ul>
<li>hexo new “xxx”</li>
<li>hexo clean</li>
<li>hexo g</li>
<li>hexo d</li>
<li>hexo server</li>
</ul>
]]></content>
      <categories>
        <category>MISC</category>
      </categories>
  </entry>
</search>
